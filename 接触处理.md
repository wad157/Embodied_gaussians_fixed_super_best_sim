# SUPER 组织—器械接触与双目视觉力方案

## 已确定的目标

本方案服从以下约束：

1. 组织继续使用小的离散粒子作为力学节点。
2. 单颗粒不承担可变形表面几何，但粒子集合必须可以弹性拉伸、弯曲和压缩。
3. Gaussian 椭球绑定到附近多个粒子，不能再绑定到整个 tissue 刚体。
4. 粒子不直接充当最终器械碰撞表面。由外层粒子生成连续三角
   collision skin，器械只和这张 skin 接触。
5. 器械不得穿过 collision skin 进入组织内部。
6. 夹爪闭合、摩擦和上移时，组织必须能够被夹起，而不只是被整体推走。
7. 左右目视觉力都必须启用；左右目先分别归一化，再等权合并，总力预算不能
   因增加右目而翻倍。
8. 重建表面的原始凹凸是 rest shape。禁止用高斯、均值、中值或 Laplacian
   位置平滑把表面压平。

## 当前 v9 为什么不能直接修改参数

当前 `bodies_v9_dense_0p5mm_rigid_tissue/tissue.json` 的 `79567` 个球全部属于
同一个 Warp rigid body。内部粒子没有独立位置和速度，因此：

- 调低刚体 stiffness 不会产生局部形变；
- Gaussian 只能随整个组织平移或旋转；
- 夹爪接触的是大量离散球，不是连续组织表面；
- 即使打开碰撞，也不能得到局部压痕、拉伸或夹持形变。

必须把组织从“一个刚体上的很多 shape”改成“很多独立粒子自由度，由弹性约束
组成一个连续体”。粒子只保存质量、位置、速度和约束邻接关系；“弹性”存在于
粒子之间的连接关系，器械接触几何只来自粒子驱动的外表面三角 skin。

## 物理表示

### 粒子

每个粒子至少保存：

```text
position / velocity / mass
visual_radius
surface flag
neighbor/bond indices
tetrahedron or local-cluster membership
anchor/support weight
```

第一版继续以 `0.5 mm` 作为粒子采样尺度起点，但必须区分：

- `visual_radius`：调试时画粒子以及表达采样密度；
- collision skin：器械接触的唯一权威连续表面。

`visual_radius` 不是碰撞半径。正式器械接触求解中不得把粒子重新注册为 sphere
shape，也不得在 skin 接触之外叠加粒子—器械 penalty contact，否则离散球会再次
覆盖表面细节，并与 skin 产生重复接触力。

v9 的名义中心距约 `0.82 mm`，小于直径 `1.0 mm`。这些球当前可以重叠，是
因为同属一个刚体且内部碰撞被过滤。拆成独立粒子后不能直接开启全量
particle-particle collision，否则初始重叠会产生巨大排斥力。

第一版规则为：

1. 粒子之间默认不做 sphere 自碰撞；
2. 依靠 tetra 体积和防翻转约束阻止局部塌缩；
3. 器械只与 collision skin 接触；
4. 若以后确实需要组织折叠自接触，应直接实现 skin—skin 自碰撞，并增加相邻
   三角形排除和独立门禁，而不是退回粒子球自碰撞。

### 自适应粒子密度：表层不降采样，深层渐变稀疏

当前 v9 的粒子尺度为：

```text
visual radius          = 0.5 mm
nominal nearest pitch  = 0.82 mm
nearest distance P05/P50/P95 = 0.785/0.799/0.811 mm
```

新资产只继承这组**尺度和密度**，不继承 v9 的平滑高度场位置。所有节点位置仍从
无平滑多视图 rest surface 和其内部体积重新生成。

组织现有有效厚度约为 `3--15 mm`。为同时保留接触细节和控制 A800 运算量，采用
以下固定的深度 sizing field。这里的 `d` 是沿 rest contact skin 向组织内部的
几何距离，不是 table-frame 的绝对 `z`：

| 区域 | 内向深度 | 目标节点间距 | 节点显示尺度 | 规则 |
|---|---:|---:|---:|---|
| 接触表面 | `d = 0` | skin edge 约 `0.82 mm` | `0.5 mm` | 不平滑、不降采样 |
| 高密表下层 | `0 < d <= 1.5 mm` | `0.82 mm` | `0.5 mm` | 保持当前 v9 密度和大小，约两层节点 |
| 渐变层 | `1.5 < d <= 8 mm` | `0.82 -> 1.80 mm` 几何连续增长 | `0.5 mm` | 禁止尺寸突跳 |
| 深层核心 | `d > 8 mm` | `1.80 mm` | `0.5 mm` | 只降低节点密度 |

初始方案曾取 `3 mm` 高密层和 `3--5 mm` 过渡层。真实 TetGen/A800 实测表明该
设置需要约 `45.6k` 节点、`240.8k` tetra，纯物理约 `30 ms/frame`，而短
过渡层还会让长侧缘跨过完整尺寸梯度。最终资产保持 contact skin 和其下
`1.5 mm`（约两层）为 `0.82 mm`，随后用以真实三角 skin 的欧氏距离为自变量，
按几何级数连续增加到 `1.80 mm`。这项修改只减少深部力学节点，不移动或稀疏
任何已观测 contact-skin 顶点。

补充规则：

1. 高密区覆盖可见上表面及其欧氏距离 `1.5 mm` 内的上侧缘；底部支撑面允许
   使用渐变/深层密度。
2. 相邻 tetra 的目标边长比例不得突然超过 `1.35`；必要时延长渐变层，不能在
   `3 mm` 处直接拼接粗网格。
3. 深层仍采用同一连续材料模型，不能把粗节点改成刚体或独立大球。
4. 节点质量按 incident tetra 的 rest volume 和统一材料密度积分，不能按
   `0.5 mm` 显示球体积计算。这样深层变稀不会改变组织总质量或局部密度。
5. `visual_radius=0.5 mm` 只用于调试显示；深层稀疏不等于增大碰撞球，因为正式
   方案根本不创建粒子 collision sphere。
6. 当前深层已经使用允许的 `1.80 mm` 上限。后续性能优化优先减少求解器遍历、
   使用 tetra coloring/分块和优化 contact broad phase；不得再稀疏 contact
   skin。

### 自适应四面体质量门禁

从密到疏的 tetrahedralization 必须通过：

```text
inverted tetrahedra = 0
degenerate tetrahedra = 0
nonmanifold boundary faces = 0
connected components = 1
neighbor target-edge ratio <= 1.35
all rest volumes > 0
```

同时记录 tetra 最小二面角、radius ratio 和 deformation-gradient condition
number。质量不合格时只能调整采样/局部拓扑，不能移动已冻结的 contact-skin
顶点去做 Laplacian smoothing。

### 弹性约束

组织至少包含三类约束：

1. **stretch/shear**：相邻粒子的 rest edge 或 tetra 变形梯度；
2. **volume/material**：四面体 Neo-Hookean XPBD，保持组织厚度并阻止翻转；
3. **rest curvature**：外表面相邻三角形的 rest dihedral angle，目标是重建
   时的原始夹角，而不是零曲率。

因此这里不存在“为了稳定而把组织变平”的绝对平滑项。材料只惩罚相对于
rest shape 的形变，rest shape 中的凹凸应该在无外力时保持。

现有项目的 `MaterialTetrahedronXPBDProjector`、soft particle state、
tetra material gate 和防翻转线搜索可以复用。第一版从已验证的
`E=15 kPa`、`nu=0.45`、12 substeps、20 material/contact iterations 开始，
不能先通过无限降低 `E` 掩盖接触失败。

参考：

- Macklin et al., XPBD:
  <https://doi.org/10.1145/2994258.2994272>
- Smith et al., Stable Neo-Hookean Flesh Simulation:
  <https://www.tkim.graphics/NEO/StableNeoHookean2018.pdf>

## 外表面三角 collision skin

### 生成

collision skin 由“器械可能接触的外边界粒子”生成，包括可见上表面和可夹持
侧缘；底部支撑边界保持闭合，但可以使用渐变层允许的较粗拓扑：

1. 在 rest surface 上识别上表面、可夹持侧缘和底部闭合边界粒子；
2. 按无平滑多视图表面拓扑以约 `0.82 mm` 目标边长生成三角形；
3. 三角顶点直接引用粒子 id，不创建第二套动态自由度；
4. 删除退化、翻转、跨越大孔洞和 mask 外的三角形；
5. 保存一致朝向的外法线和闭合/边界信息；
6. 每个 physics substep 后，skin 顶点自然等于对应粒子位置。

collision skin 是器械—组织 signed distance 的权威来源。球粒子不再决定
“器械是否已经进入组织下面”。

skin 的密度不跟深层稀疏策略一起下降。生成后必须与无平滑 rest surface 做
双向距离和局部峰谷门禁：

```text
rest-surface distance P95 <= 0.10 mm
rest-surface distance max <= 0.25 mm
局部峰谷排序保持
表面高度谱不能系统性衰减
```

若性能不足，应优化接触 broad phase 或器械 collision proxy，不能通过减少
skin 三角形来牺牲表面凹凸。

### 单侧不可穿透约束

器械表面与 collision skin 必须满足：

```text
g(tool, skin) = signed_distance(tool, skin) - contact_margin >= 0
```

每个 substep 的顺序：

1. 按真实时间戳插值 PSM 的连续位姿和速度；
2. 用外力、重力和视觉力预测粒子位置；
3. 求解材料、体积、rest curvature 和支撑约束；
4. 对器械—skin 执行 broad phase；
5. 对候选执行连续 vertex-face 和 edge-edge 碰撞检测；
6. 投影违反的单侧约束，累计非负接触乘子；
7. 求解切向 Coulomb friction；
8. 最后再做一次 contact projection 和 signed-gap 审计。

仅使用 penalty force 不能满足“绝不进入组织以下”：高速或较大 timestep 下仍
可能一步穿透。第一版在 Warp 内实现 conservative CCD + XPBD unilateral
constraint；若后续要求数学意义的 intersection-free 保证，则迁移到 IPC/C-IPC
式 barrier/CCD。

参考：

- Incremental Potential Contact:
  <https://ipc-sim.github.io/>
- Codimensional IPC:
  <https://arxiv.org/abs/2012.04457>
- Tool-tissue interaction review:
  <https://pmc.ncbi.nlm.nih.gov/articles/PMC2813063/>

### 接触 margin

初始建议使用 `0.3--0.5 mm` 的 skin contact margin，但最终以 signed gap
门禁为准，不以肉眼不明显为准。目标：

```text
minimum signed gap >= -0.05 mm
CCD missed crossings = 0
initial interpenetrating frames = 0
```

正式开启前必须扫描全部 PSM 帧。如果 corrected/depth-then-visual pose 在某帧
已经位于 skin 内部，不能直接打开硬接触，否则 kinematic PSM 会把组织瞬间
推出。必须先修正 collision proxy、位姿或该帧初始状态。

## Gaussian 与多个粒子的绑定

每个组织 Gaussian 绑定到包含它的四面体或附近稳定的四粒子局部单元：

```text
x_g = sum_i(w_i * x_i),  i=0..3
```

`w_i` 在 rest pose 中计算并固定。Gaussian 的朝向和椭球尺度由局部变形梯度
更新：

```text
F = D_s * inverse(D_m)
Sigma_world = F * Sigma_rest * transpose(F)
```

从 `F` 的 polar/SVD 分解得到旋转和主轴尺度，并设置有限的尺度/条件数门禁。
不能只移动 Gaussian center 而永久保持错误的椭球方向；也不能让视觉 optimizer
通过修改颜色、透明度或尺度替代真实几何形变。

视觉 loss 产生的 Gaussian mean target 按相同权重反向散射到四个粒子，再由
材料约束传播。PSM 和 ground Gaussian 永远不进入 tissue particle force。

## 夹持和抬起

弹性材料是可夹起的必要条件，但不是充分条件。完整夹持依赖：

1. 两片 jaw 都与 collision skin 形成 opposing contact；
2. 闭合产生足够法向力；
3. 摩擦阻止组织从夹爪间滑出；
4. 组织具有体积约束，不能被压成零厚度；
5. 支撑/anchor 只位于合理边界，不能把整个底层完全锁死；
6. jaw 上移时 PSM 位姿连续，不允许帧间 teleport 穿透 skin。

验收动作固定为：

```text
接近 -> 闭合 -> 保持 -> 上提 -> 横向移动 -> 松开
```

首先使用纯 Coulomb contact。只有在两片 jaw 已同时接触同一连通区域、闭合量和
法向力超过门槛但仍因数据中不可解析的微观摩擦而滑脱时，才允许实验性
`grasp latch`。latch 必须在 jaw 张开时立即解除，并在报告中与纯接触结果分开，
不能默认把组织焊在夹爪上。

## 左右目视觉力

### 相机独立权重

每个相机独立使用自己的：

```text
native decoded timestamp
tissue mask
tool/occlusion mask（后续补齐）
highlight down-weight
stereo depth/confidence（后续接入）
```

禁止右目 mask 超出其时间覆盖后静默复用最后一帧。无有效 mask 的相机该帧权重
必须为零并记录为 inactive。

### 等权损失

左右目必须先分别归一化：

```text
L_left  = sum(w_left  * rho(r_left )) / sum(w_left)
L_right = sum(w_right * rho(r_right)) / sum(w_right)
L = mean(L_camera for active cameras)
```

这避免 mask 面积更大的一侧支配梯度。两目同时有效时即为
`0.5 * L_left + 0.5 * L_right`。视觉力仍经过原有 per-Gaussian、per-particle
和 tissue-total clamp；加入右目不修改总预算上限。

正式 soft tissue 版本中，loss 优先级为：

1. 有置信度的 expected-depth/projective residual；
2. tissue silhouette/alpha；
3. 去除器械和高光后的 robust RGB。

当前先完成左右目 masked RGB 接线；深度和 tool mask 在后续独立门禁中加入。

相关内窥镜多视图/动态重建依据：

- SuPer:
  <https://escholarship.org/uc/item/3kn531ch>
- EndoGaussian:
  <https://arxiv.org/abs/2401.12561>
- PhysGaussian:
  <https://openaccess.thecvf.com/content/CVPR2024/html/Xie_PhysGaussian_Physics-Integrated_3D_Gaussians_for_Generative_Dynamics_CVPR_2024_paper.html>

## 分阶段实施

### 当前实施状态（2026-07-25）

已完成阶段 0、无平滑双目 rest surface、阶段 A 自适应软体资产、阶段 B 静置
稳定性门禁，以及阶段 C 的第一版 triangle-skin 单边接触。用户已明确接受
`30.497 ms/frame` 的纯物理基线，因此不再用原 `20 ms/frame` 目标阻止接触
开发；正式 demo 默认现已切到 `adaptive_soft`，旧 v9 由
`--tissue-mode rigid_v9` 保留回退：

- demo 和两份启动脚本默认加载 `stereo_left,stereo_right`；
- 左右目各使用解码后原生时间戳和独立 packed tissue mask；
- 右目 `1441` 帧 SAM2 mask 已完整传播并通过门禁；
- 每个相机先在自身有效 mask 内归一化，随后对 active cameras 等权平均；
- mask 未覆盖的相机不再静默夹到最后一帧，而是权重归零；
- 双目真实反向传播已通过，左右目 loss 分别为 `0.0351834` 和
  `0.0579199`，合并值为等权均值 `0.0465516`；
- 原有线性总力 `0.005 N` 和力矩 `0.00005 Nm` clamp 保持不变。
- 6 对左右目参考帧已生成各自时间戳同步的 FoundationStereo 深度并融合；
  `6885` 个已用上表面顶点全部冻结，最大位移为 `0 mm`；
- 新资产包含 `37570` 个力学节点、`193845` 个正体积 tetra、`24196` 个闭合
  skin 三角和 `2863` 个四节点绑定 Gaussian；
- 上表面观测值没有经过空间平滑；45 个封闭遮挡缺口只在未观测格点内做拓扑
  补洞，已观测高度保持 bit-identical；
- A800 1000 步静置结果 finite、翻转 tetra=`0`、体积误差最大
  `0.6098%`、稳定性门禁通过；
- 同一测试的纯物理耗时为 `30.497 ms/frame`；该基线已由用户接受；
- adaptive runtime 使用 `12` substeps、`20` material iterations、
  `E=2 kPa`、`nu=0.45`、速度阻尼 `2/s`，粒子 collision radius 保持为 0；
- triangle skin 由 `24196` 个闭合三角、`12100` 个表面节点组成；器械代理共
  `4779` 个表面样本，未建立 `skin triangles x tool triangles` 笛卡尔积；
- 器械样本查询当前变形 skin BVH；法向不可穿透和 Coulomb 摩擦修正按三角
  barycentric weights 分发到三个力学节点，再对 `63794` 个 skin-incident
  tetra 做防翻转线搜索；
- 接触现在从首帧安全状态默认开启，并由 GUI 的 `PSM-tissue collision`
  开关显示真实状态；可用 `--no-psm-tissue-contact` 做关闭 A/B。重新开启前
  仍会做 signed-gap 预检，当前 gap 小于 `-1 mm` 时拒绝启用，避免已经发生
  的深穿透把组织瞬间推出。

正式启动脚本当前仍保守地设置 `--visual-force-iterations 0`，所以“双目数据、
mask 和反向力链路已接通”不等于默认回放已经给 v9 刚体施加视觉力。需要试运行时
显式传入：

```bash
bash scripts/run_demo_thinlinc.sh \
  --cameras stereo_left,stereo_right \
  --psm-pose-driver depth_then_visual \
  --psm-tissue-contact \
  --visual-force-iterations 1
```

当前启动脚本和 Python 入口默认都使用 `depth_then_visual`。这里的
`corrected` 是深度优化前的三部件时序矫正结果；`depth_then_visual` 才是在其
基础上完成双目深度微调和视觉矫正的版本。仍可显式传
`--psm-pose-driver corrected` 做 A/B 对照。

`Show Physics` 的显示尺度与碰撞尺度必须分离：自适应组织的力学粒子
collision radius 继续严格保持为 `0`，器械仍只接触 triangle skin；调试显示
使用资产里的 `particle_visual_radius=0.5 mm`，并额外绘制当前变形的
`24196` 个 collision-skin 三角。显示设置不会改变接触、材料或求解结果。

视觉优化仍每 3 个物理步更新一次，避免每步做双目 gsplat backward；两次更新
之间必须零阶保持最新的 post-clamp 粒子力。旧实现只在更新后的一个物理步消费
该力、其余两个物理步为零，导致有效平均载荷只有预期约三分之一。现在保持力会
连续作用到下一次视觉更新，Reset 会清空缓存。该轮安全上限为 `0.075 N`；
文末的厘米级大形变门禁通过后，当前值已更新为 `0.30 N`。

GUI 现在直接显示 contact ON/OFF、signed gap、候选数、最大穿透、安全缩放，
以及视觉目标最大位移、实际 applied/pre-clamp force budget、活跃节点数和左右
目 loss。传入 `--visual-force-iterations 1` 时默认显示实际 post-clamp 粒子
力箭头；只画箭头不会额外执行一次 gsplat 渲染。

### 接触冻结修正与当前软硬度

回放中“器械进入组织下方但表面没有接触痕迹”不只是材料偏硬。旧防翻转线搜索
对全部 `63794` 个 skin-incident tetra 使用一个全局缩放值，并且会检查本次根本
没有接触位移的 tetra。只要历史上有一个无关 tetra 已低于最低体积比 `0.05`，
它就可能把全局缩放写成 `0`，冻结之后所有接触区域。

现在防翻转线搜索执行两条局部性规则：

- 四个顶点本次接触增量全为零的 tetra 不参与本次安全缩放；
- 已低于最低体积比的 tetra 只阻止继续恶化，允许体积不变或恢复的修正。

CPU 回归门禁额外构造了一个体积比约 `0.02` 的无关压扁 tetra。结果为：
contacted tetra 正常移动 `0.400 mm`，无关 tetra 位移严格为零，接触后首个 tetra
体积比 `0.98845`，全部门禁通过。因此不再允许远处的坏单元冻结当前接触。

材料和接触默认值同时改为：

```text
Young's modulus                 2 kPa（原 15 kPa）
Poisson ratio                   0.45
velocity damping                2/s（原 12/s）
contact projection stride       1 substep（原 2）
maximum correction per pass     0.25 mm（原 0.40 mm）
contact margin                  1.50 mm
contact minimum volume ratio    0.01
```

降低 `E` 和阻尼会让局部压陷/回弹更明显；每个 substep 投影接触避免器械在两次
检测之间跨过薄表面；`1.50 mm` margin 用来覆盖可见 Gaussian 约
`1.35--2.00 mm` 的外观半径。该修正不重建、不平滑也不移动 rest tissue，
原始表面凹凸和纹理没有被改写。GUI 现在显示上述当前参数，启动日志也会打印
`adaptive tissue mechanics: E=2.0kPa ... contact_margin=1.50mm`。

上一轮 `E=5 kPa / margin=0.4 mm` 在
`depth_then_visual + -27 deg` 的状态 `895` 单步 CPU 定点门禁中，接触投影前
gap 为 `-0.454 mm`，一步后为 `+0.0485 mm`，最大节点位移 `0.893 mm`，
翻转 tetra=`0`，最低体积比 `0.05896`。这已证明接触能留下形变并把器械推出
到 skin 外侧；完整 A800 连续回放和夹持抬起仍需用更新后的 smoke/GUI 验收，
不能由单步门禁代替。

### 厘米级大形变与运动学防穿越

负体积 tetra 不能作为“更软”的近似。Neo-Hookean 能量包含 `log(J)`，
Gaussian 四点绑定、skin 法向和 signed-distance 也都假设 `J>0`。实测
`E=0.5/0.75/1.0 kPa` 在新网格仅受重力一步时分别出现 `2/1/1` 个翻转，因此
没有把最低刚度继续降到这些失败档。

当前采用两级、不同用途的正体积保护：

- 接触局部修正使用 `J>=0.01`，避免夹爪单次把薄表层 tetra 压扁；
- 一般材料/视觉力 substep 只使用极薄屏障 `J>=1e-4`。它先回退将要跨零的
  tetra 四节点邻域，迭代四轮；远处节点不受限制。只在局部修复仍失败时才用
  全局线搜索兜底。

全局屏障的实验版本虽然零翻转，却把12帧位移限制到 `2.01 mm`，已被拒绝。
最终局部屏障在正式 `37570` 节点、`193845` tetra 资产上的合成视觉力门禁为：

```text
E / damping                         2 kPa / 2/s
visual target step / kp             1.0 mm / 5
per Gaussian / particle cap         0.0002 N / 0.0002 N
total visual-force cap              0.30 N
loaded Gaussian patch               156 Gaussians, radius 8 mm
maximum displacement @ frame 8      10.414 mm
maximum displacement @ frame 12     16.929 mm
inverted tetrahedra                  0
minimum volume ratio @ frame 12      0.001004
total volume ratio @ frame 12        0.992429
```

这证明力学链路能承受 `1--2 cm`，但不代表每一段真实 RGB 梯度都会自动提供同一
方向的持续目标；GUI 会显示 target delta、实际力预算和从 Reset 起的最大组织
位移，三者要一起判断。

器械是离线位姿驱动的运动学物体，triangle-skin 接触本身只能移动 tissue，
不能反向改变器械轨迹。当局部组织达到压缩下限时，继续压 tissue 只能造成
穿越或翻转。因此当前增加运动学 no-crossing guard：接触开启时，每次应用
pose driver 后检查 fresh skin gap；若低于 `1.50 mm`，将全部 distal PSM link
沿世界 `+Z` 回退到 margin。这里利用了本组织是 table-frame 高度场、器械从
上方接近的事实；它不是任意曲面通用法向约束。

真实 state `895` 联合 CPU 门禁：

```text
gap before                           -0.453918 mm
gap after                            +1.499949 mm
inverted tetrahedra                  0
minimum tetra volume ratio           0.252849
contact inversion-safe scale         1.0
all smoke gates                      passed
```

GUI 会额外显示 `Kinematic no-crossing gap / last retraction`。该回退会让极端
穿透帧相对原 `depth_then_visual` 位姿产生毫米级偏移，这是满足“器械不能在
组织以下”的显式优先级，而不是位姿文件失效。

当前 v9 的 `79567` 个 rigid-body sphere 只作为回退路径；adaptive 模式不会
创建粒子碰撞球，也不会叠加旧 particle-shape contact。

第一版真实 A800 接触 smoke 使用 corrected pose、运行时 `-27 deg` roll 偏移和
轻微接触状态 `2557`：

```text
初始 minimum signed gap       = 0.252289 mm
初始 contact candidates       = 10
10 帧后 minimum signed gap    = 0.425402 mm
contact margin                = 0.400000 mm
inverted tetrahedra            = 0
minimum tetra volume ratio     = 0.601704
maximum speed                  = 0.080063 m/s
contact-disabled runtime       = 41.206 ms/frame
contact-enabled runtime        = 48.437 ms/frame
contact overhead               = 7.231 ms/frame
```

深穿透状态 `3922` 的 rest-skin gap 为 `-6.916 mm`，开关已按预期拒绝直接启用。
报告为
`soft_tissue_adaptive_v1/triangle_skin_contact_smoke_report.json`。这证明第一版
单边接触和防翻转链路可运行，但原阶段 C 的 `P95 <= 40 ms/frame`、完整时序
CCD crossing、单 jaw 滑动和双 jaw 闭合仍未全部验收；不能把本 smoke 当成
“已经完成夹持抬起”。

### 阶段 0：右目视觉力

1. 默认加载 `stereo_left,stereo_right`；
2. 左右目使用各自 native decoded timestamp；
3. 使用独立 packed mask provider；
4. 每目独立归一化后取平均；
5. 总视觉力 clamp 保持不变；
6. 补齐右目完整序列 mask；
7. 报告每目 loss、有效像素和实际 mask index。

### 阶段 A：弹性粒子资产

1. 从无平滑多视图 rest surface 生成粒子；
2. 冻结表面及向内 `1.5 mm` 的 `0.82 mm` 高密节点区；
3. 在 `1.5--8 mm` 建立几何连续渐变、`8 mm` 以下使用 `1.80 mm` 深层节点；
4. 建立 bond/tetra/material constraints；
5. 只保留必要边界 anchor；
6. 建立四粒子 Gaussian binding；
7. 导出 particle、tetra、skin 和 binding PLY。

第一版性能预算不是几何降质许可，而是设计约束：

```text
particles target / hard limit     = 25k--40k / 45k
tetrahedra target / hard limit    = 100k--180k / 200k
skin triangles target / hard limit = 15k--30k / 30k
active tool-skin contact candidates P95 <= 1000
```

如超限，调整顺序固定为：

1. 只稀疏 `d>5 mm` 深层；
2. 延长密度渐变带并改善 tetra 质量；
3. 简化器械的 collision-only proxy；
4. 优化/refit skin BVH 和 active contact set；
5. 绝不稀疏 contact skin；当前前 `1.5 mm` 高密区也保持冻结。

### 阶段 B：静置与材料门禁

- 1000 steps 全部 finite；
- inverted tetra = 0；
- anchor drift = 0；
- 无外力时原始峰谷顺序、rest curvature 和表面高度谱不丢失；
- 禁止出现为了通过门禁而新增的位置平滑。
- 原设计目标为 A800 单环境纯物理 P50 `<=20 ms/frame`、P95
  `<=25 ms/frame`；当前 `30.497 ms/frame` 稳定基线已由用户接受，不再通过
  降低材料迭代或稀疏表面追这个目标；
- 运行时显存和 contact buffer 必须记录峰值，禁止预分配
  `skin triangles x tool triangles` 的笛卡尔积。

### 阶段 C：collision skin 接触

- 全帧初始 signed-gap 扫描；
- 单 jaw 下压/滑动；
- 双 jaw 闭合；
- skin BVH 每 substep 只 refit，先做 tool-local broad phase，再做候选 CCD；
- active contact candidates P95 `<=1000`、maximum `<=5000`；
- CCD crossing = 0；
- maximum penetration 不超过门槛；
- 接触后无 tet 翻转和爆炸速度。
- 原设计目标为 A800 单环境“材料 + 接触”P50 `<=30 ms/frame`、P95
  `<=40 ms/frame`；第一版完整 runtime 的 10 帧均值为
  `48.437 ms/frame`，功能 smoke 通过但该性能目标尚未通过。

### 阶段 D：夹持抬起

- 完成“接近—闭合—保持—上提—横移—松开”；
- 报告 jaw contact count、normal force、friction、组织抬升高度和滑移；
- 松开后组织有限恢复，不产生永久数值漂移；
- 纯摩擦与可选 latch 分开验收。

### 阶段 E：双目视觉力联合门禁

- 左右目 loss 分别下降；
- 两目有效时权重严格等分；
- 总力预算不因相机数翻倍；
- 视觉力不能将 skin 拉穿器械；
- 视觉力关闭/左目/双目三组 A/B 对照；
- 最终再运行接触、夹持和双目视觉力联合长序列。

## 明确不采用的路径

- 不继续使用一个刚体上的数万 collision spheres 作为可变形组织；
- 不把全部微粒做成数万个 6-DoF rigid body；
- 不直接开启当前重叠粒子的全量自碰撞；
- 不让器械直接接触力学粒子，也不同时叠加 skin 接触与粒子球接触；
- 不以 Gaussian/median/Laplacian 位置平滑换取稳定；
- 不只靠 penalty contact 宣称“不可穿透”；
- 不通过无限降低材料刚度伪造“可以夹起”；
- 不让右目超过 mask 覆盖范围后复用最后一帧；
- 不让两台相机把视觉力总预算简单加倍。

## 当前不可穿透软接触实现（2026-07-25）

此前实现把两个不同问题混在了一起：一方面组织材料偏硬，另一方面器械穿过很薄
的上表面后，封闭 skin 的 inside/outside 查询会再次得到“在外部”，导致接触
消失。继续只降低 Young 模量并不能修复第二个问题；只增加 penalty stiffness
又会把器械位姿推偏。

当前实现明确分为两层：

1. **很软的体积材料**：`E=250 Pa`、`nu=0.40`、重力补偿为 `0`、速度阻尼
   `1/s`。粒子之间的约束已显著放松，允许局部压缩、拉伸和剪切。
2. **独立的不可穿透表面**：闭合三角 skin 继续处理侧面、底面和夹持接触；
   对 marker=`1` 的原始上表面额外建立定向单边屏障。屏障使用当前变形三角形
   法向计算有符号间隙，因此夹爪即使已经越过薄表面，也不会被误判为安全。

器械依然是运动学驱动，接触求解不改变 `depth_then_visual` 位姿。为防止一帧内
跳过薄表面，当前目标位姿在每个显示帧的 `12` 个物理 substep 内做刚体平移和
四元数 slerp 插值；每个 substep 都重新 refit skin BVH 并执行表面投影。旧的
世界 `+Z` 位姿回退已关闭：

```text
kinematic pose retraction          OFF
contact projection stride          1 substep
contact margin                     1.50 mm
maximum correction per substep     1.00 mm
```

单个接触三角的位移不再只压它的三个顶点。上表面每个三角预计算一个平滑局部
支撑柱：

```text
lateral support radius             4 mm
inward support depth               12 mm
top faces                          14,018
top nodes                          7,261
face-to-particle support entries   5,070,014
GPU id + weight storage            40.56 MB
```

支撑权重随横向距离和向内深度连续衰减。这样表面仍是由节点驱动的整体三角
skin，但按压不会只把第一层 tetra 压扁，也不需要恢复任何表面平滑。通用的
tet 邻接 contact spread 当前设为 `0` 层，避免和支撑柱重复扩散。

接触阶段的旧全局 `J` 缩放会因为一个危险 tetra 把所有接触一起冻结，因此当前
关闭该全局缩放（contact min `J=0`）；材料积分后的局部正体积屏障仍保留
`J>=0.001`，只修复真正将翻转的局部 tetra。它不是硬度参数，也不会把整个组织
锁死。

真实 corrected 轨迹 CPU 回归从 state `895` 接近表面，再运动到深压 state
`1065`，连续运行三个物理帧：

```text
未变形 skin 的初始深穿透             -2.945727 mm
求解后最小定向表面间隙               +0.714156 mm
top barrier active samples                  74
组织最大位移                          5.193470 mm
组织位移 P99                          4.067498 mm
器械目标位姿误差                      0
inverted tetrahedra                   0
minimum tetra volume ratio            0.001888
anchor drift                          0
all gates                             passed
```

视觉力在同一 `E=250 Pa` 材料下，对真实 `37,571` 节点资产运行 `12` 帧局部
目标测试，最大位移为 `6.115 mm`，翻转数为 `0`。这证明约束已经明显变软，但
尚不能把该短测试宣称为达到 `1--2 cm`；厘米级视觉形变仍需在实际双目序列中
增加持续施力时间或目标位移，并重新做稳定性门禁。

当前机器没有可用 CUDA，因此新的支撑柱接触只完成 CPU 正确性回归。下一步在
A800 上运行：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1
```

脚本已默认使用双目、`depth_then_visual`、`adaptive_soft` 和 triangle-skin
接触。GUI 中应确认 `Contact status: ON`，深压时
`Top barrier contacts` 大于零，并记录加入支撑柱后的实际 ms/frame。
