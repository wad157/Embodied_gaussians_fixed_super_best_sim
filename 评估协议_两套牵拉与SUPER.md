# 两套组织牵拉与 SUPER 评估协议

## 本次两套仿真数据

正面牵拉和侧面牵拉均直接从仿真器导出逐帧组织表面世界坐标，并使用双目标定投影得到 2D 真值。估计轨迹只通过首帧重建表面的一次性重心绑定获得，后续不做逐帧 GT 配准。报告 3D mean/RMSE/median/P95 与阈值成功率，以及双目 2D EPE、RMSE、PCK。该设置对应 Physically Embodied Gaussian Splatting 和 PhysTwin 中同时考察 2D/3D 跟踪的原则。

渲染指标只使用 RGB，报告组织层与全图的 PSNR、SSIM、LPIPS（AlexNet v0.1）。深度只属于仿真 GT/首帧全视角建模资产，不进入视觉残差或在线刚度优化。

重建与未来预测是两套独立实验，而不是一次运行的两个统计区间。重建参考 EH-SurGS 的 7:1：每连续 8 帧的前 7 帧可用于因果视觉残差/刚度估计，第 8 帧完全留出，只渲染并计算 2D/3D、PSNR、SSIM、LPIPS；测试帧不能进入随后刚度候选的验证。未来预测则用另一套运行：连续前 80% 帧估计状态和参数，在切分点冻结视觉残差、局部刚度和外观，后 20% 只保留已知 PSM 控制与五点夹持位移边界，直接开环预测。三种方法因此共运行 `2×3=6` 次，全部使用同一个预先固定的均匀初值，禁止材料真值、参数搜索和分支选择。

## SUPER 真实数据的真值制作

SUPER 序列不把算法自己的跟踪结果当真值。按照 Tracking Everything in Robotic-Assisted Surgery 的人工标注思路，在左右目关键帧分别标注同一批可辨识组织点，进行双人复核并记录遮挡/出视野状态；中间帧可以由跟踪器预传播，但最终仍须人工验收。

2D 指标直接对人工像素标注计算。3D 真值使用冻结的双目内外参和估计双目深度：在人工 2D 点处取经过左右一致性、前后向检查与局部中值过滤后的深度，再反投影到统一世界坐标。没有通过双目质量门禁的点只保留 2D 评估，不进入 3D 统计。SUPER 与仿真结果分表报告，不能把“仿真直接 GT”和“真实双目估计 GT”混成同一精度等级。

参考：

- https://arxiv.org/abs/2406.10788
- https://arxiv.org/abs/2503.17973
- https://arxiv.org/abs/2409.19821
- https://arxiv.org/abs/2501.01101
