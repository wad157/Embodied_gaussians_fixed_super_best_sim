# 器械位姿

> 建立日期：2026-07-25  
> 更新日期：2026-08-01  
> 当前结论：`raw_kinematics_v1` 是必须保留的“严格原始数据负基线”；
> P420006 和历代 paper LND 版本继续作为 CAD/视觉对照保留；当前 GUI 临时
> 默认切换为 `raw_paper_lnd_sam2_dense_contact_unbounded_xyz`：使用 22 对
> 双目锚点、强化可信人工尖端、XYZ 无硬边界，并逐位保留原始 q1–q7。

## 1. 版本定位

| 版本 | 状态 | 含义 |
|---|---|---|
| `raw_kinematics_v1` | 冻结、保留 | 只从原始 bag、相机标定、hand-eye、`LND.json` 重建运动学；GUI 接入时不读取历史 pose driver。它是重要负基线，不应覆盖或改名。 |
| `raw_lnd_cad420006_v1`（GUI 名 `raw_p420006`） | 已构建、可切换检查 | 保留 `raw_kinematics_v1` 的运动学和 GUI 坐标，只替换七个末端器械 CAD/固定适配变换；不覆盖第一版。 |
| `raw_lnd_cad420006_stereo_visual_v1`（GUI 名 `raw_p420006_stereo_visual`） | 已构建、已通过 GUI 运行时检查 | 从原始 bag 的 10 对严格双目关键帧、人工分割和尖端标注共同估计固定全局修正、小幅平滑时序残差及 jaw 单调标定；仍保留完整 5458 个原始机器人时刻。 |
| `raw_paper_lnd_sam2_online` | 已构建、可切换检查 | 新提取并重新人工标注严格双目第 0 对；左右目各用一个 SurgicalSAM2 predictor 因果传播完整 1631 对；优化与 GUI 都使用论文仓库同一套 paper LND 四部件 CAD/FK。 |
| `raw_paper_lnd_sam2_dense_contact_unbounded_xyz` | 当前临时默认、已通过 GUI 运行时检查 | 22 对双目锚点和增强人工尖端约束；完整 1631 对时变 XYZ/三维旋转矫正，XYZ 无硬边界，原始 q1–q7 位级不变。 |

当前临时默认版本的启动命令：

```bash
bash scripts/run_demo_thinlinc.sh --visual-force-iterations 1
```

等价的显式版本命令：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_paper_lnd_sam2_dense_contact_unbounded_xyz \
  --psm-roll-offset-deg 0 \
  --psm-camera-translation-mm 0 0 0
```

第二版 P420006 的运行命令：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_p420006 \
  --psm-roll-offset-deg 0
```

第三版双目视觉矫正 P420006 的运行命令：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_p420006_stereo_visual \
  --psm-roll-offset-deg 0
```

`run_demo_thinlinc.sh` 的临时默认值现为第 18 节无硬边界版；脚本默认附加
roll 和相机平移也同时归零，避免二次修正。旧版本均未覆盖，仍可用
`--psm-pose-driver` 显式切换。

## 2. 严格原始数据基线

运动学主干的唯一输入白名单是：

```text
data/grasp5/grasp5.bag
data/camera_calibration.yaml
data/handeye.yaml
data/LND.json
```

输出被冻结在：

```text
data/super/psm_raw_kinematics_v1（纯机器人学版本）/
```

GUI 接入另外只读取根目录的 `data/dvrk_model`、当前
`data/super/table_frame.json` 和当前相机 manifest；它不读取旧
`strict/registered/corrected/depth` 等 pose driver。

使用的坐标约定是：

```text
T_A_B @ p_B = p_A

T_GUIworld_CADlink(t)
  = X_GUIworld_rectifiedLeftCamera
  @ T_rectifiedLeftCamera_LNDlink(t)
  @ T_LNDlink_CADlink
```

已经通过的检查只说明“管线内部一致”：

- 原始 q7 共 `5458` 个状态，左右相机各 `1644` 条消息；
- 图像到最近 q7 的时间差小于约 `6 ms`；
- 所有 link 变换有限，旋转矩阵数值合法；
- GUI 相机与当前 table/world 坐标转换逐元素一致；
- 保存的 GUI pose driver 与运行时 FK 一致。

这些检查没有证明 CAD 轮廓、器械自旋和夹爪运动与真实视频一致。

## 3. 为什么整体位置和自旋仍然明显不对

### 3.1 当前 LND 到 CAD 的注册在器械轴向上不可观测

`scripts/build_super_raw_psm_gui_driver.py` 在 `q7=0` 时，只用六个 link 原点做
一次加权刚体拟合。两组点几乎都落在同一条长轴线上。

加权中心化点集的奇异值为：

```text
Classic URDF: [659.650527, 0.0027053, 0.0000383] mm
LND:          [659.650527, 0.0000610, ~0]        mm
```

第二、第三方向比主方向小约五到七个数量级，LND 点集的第三方向实际退化到零。
因此这组对应点可以确定器械长轴，却不能可靠确定绕长轴的自旋角。

当前拟合恰好给出了约 `133.015°` 的轴向/yaw 旋转。注册最大残差虽然只有
`0.794 µm`，但这个很小的残差只表示几乎共线的原点互相重合，不能证明
`133.015°` 的自旋是对的。

### 3.2 旧验证门槛只检查“投影在画面内”

raw 报告中的投影检查是：

```text
12/12 个 LND feature 有正深度，并落在图像范围内
```

它没有计算 feature 到真实器械像素、轮廓或关键点的距离。因此报告
`passed=true` 不能排除肉眼可见的平移、旋转和尺度偏差。今后不得再把
“在画面内”当成“与器械对齐”。

### 3.3 hand-eye、编码器零点和工具安装仍可能有系统残差

当前坐标链、相机 rectification 和时间同步是自洽的，所以它们不像是这次
大偏差的首要代码错误；但是这种自洽检查无法证明：

- hand-eye 在采集当日仍保持相同精度；
- PSM 编码器零点与实际机械零位完全一致；
- 实际安装的工具相对标称 LND 坐标没有固定装配偏差；
- 钢丝传动、回差和柔顺性可由理想 DH 完全表达。

这些项需要以原始双目视频的真实器械轮廓为外部观测继续验证。

## 4. 为什么两片夹爪不符合真实运动

### 4.1 当前 CAD 根本不是 Large Needle Driver

当前 raw GUI 构建器明确展开：

```text
data/dvrk_model/urdf/Classic/psm_tool_sca.urdf.xacro
```

这里的 `sca` 是 **Small Clip Applier**。官方
[jhu-dvrk/dvrk_cad 的 Classic/PSM](https://github.com/jhu-dvrk/dvrk_cad/tree/master/Classic/PSM)
原始零件名也直接包含
`Tool_Wristis_Small_Clip_Applier_*`。而本数据的运动学文件是
`data/LND.json`，即 **Large Needle Driver**。

因此当前结果是：

```text
Large Needle Driver 运动学
        +
Small Clip Applier 外形和夹爪结构
```

器械尖端长度、夹爪弯曲形状、闭合接触面和铰接关系都可能不匹配。这是当前
夹爪外观和运动错误最直接、证据最强的原因。

### 4.2 Classic SCA 用同一片网格近似镜像第二片夹爪

当前 `psm_tool_sca.urdf.xacro` 对两片夹爪都使用
`tool_wrist_sca_link_2.dae`。第二片只额外施加：

```xml
<origin rpy="0 3.1516 0" xyz="0 0 0"/>
```

这里的 `3.1516 rad` 比 π 多约 `0.010007 rad = 0.573°`。即使不考虑器械型号
错误，这个内置角度也会让侧视图中的两片夹爪出现肉眼可见的不平行趋势。

### 4.3 raw jaw 与当前 URDF 的零点定义冲突

根 Classic SCA URDF 的 jaw 限位是 `[0, 1.5707] rad`，但原始 bag 中的 jaw：

```text
min = -0.483825 rad
常见闭合簇约为 -0.10 rad
max = 1.001953 rad
负值状态占 42.6%
```

为了让 raw q7 能进入 GUI，当前构建器把 jaw 限位扩大到
`[-1.2, 1.6] rad`，随后直接执行：

```text
jaw_1 = +raw_jaw / 2
jaw_2 = -raw_jaw / 2
```

这虽然忠实复制了数字，却不是物理标定。负 jaw 会让夹爪越过 CAD 定义的闭合
零位；`LND.json` 的 jaw 模型本身也只有理想的 `±jaw/2`，没有 jaw 零点、
方向、比例、止挡、不对称、回差或钢丝传动模型。

因此“严格使用 raw jaw”与“严格符合真实夹爪”不是同一件事。

## 5. CAD 候选审计

### 优先级 A：仓库内已有的官方 Si `P420006`

本仓库已经存在：

```text
data/dvrk_model/meshes/instruments/420006/
├── tool_wrist_sca_ee_link_1.STL
├── tool_wrist_sca_ee_link_2.STL
├── tool_wrist_sca_link.STL
├── tool_wrist_sca_shaft_link.STL
└── ...
```

对应
`data/dvrk_model/urdf/Si/psm_tool.urdf.xacro` 中的 `P420006`。与当前 Classic
SCA 不同，它有两片独立夹爪 STL，两个夹爪的几何中心互为镜像，关节轴分别为
`+x` 和 `-x`。本地网格检查得到每片夹爪约
`3.157 × 5.000 × 11.500 mm`，各有 `31,690` 个三角形。

官方 [jhu-dvrk/dvrk_model](https://github.com/jhu-dvrk/dvrk_model) 同时包含
Classic 和 Si 模型，并指向原始 CAD 仓库。这个候选无需下载，最适合先做
离线 A/B 叠加。

复看 raw 左目 frame `0`、`822`、`1643` 的近景后，实物两片夹爪是明显的
渐缩楔形/持针面，而当前 Classic SCA 单片网格更接近等宽细条；
P420006 的渐缩轮廓和独立左右片在形态上更接近视频。这个观察提高了它的
候选优先级，但视频仍不足以单独确认工具代际和精确料号。

限制：

- 它属于 Si 工具描述，不能把整套 Si PSM 运动学直接覆盖到当前 raw 主干；
- 料号、实际视频器械代际和尖端形状仍需用原始帧照片核对；
- 只能先借末端 CAD/网格，再显式求 `LND link -> CAD link` 固定变换。

结论：**当前第一候选。保留 raw LND 运动学，只替换末端 CAD，不换整条机械臂。**

2026-07-26 已按这个边界构建：

```text
data/super/psm_raw_kinematics_v1（纯机器人学版本）/gui_p420006_v1/
├── psm_p420006.urdf
├── psm_p420006_mimic_map.json
├── psm_p420006_gui_pose_driver.npz
├── psm_p420006_surface_gaussians.npz
├── registration_report.json
└── gui_validation_report.json
```

第二版只改变七个 tool visual/contact mesh 和固定的
`T_LNDlink_P420006link`；原始 q7/时间戳、LND、标定、hand-eye、GUI
table/world、机械臂主干和 mimic 合同均不变。验证结果：

- pose driver 仍含原始 `5458 × 7` q7，逐元素与 raw 资产相同；
- 在实际 5458 个状态上，P420006 末端相对原生 P420006 URDF 的最大位置差
  为 `0.0314 mm`，最大转角差为 `0.000947°`；
- 两片夹爪在全部状态中共用同一铰点，保存为 float32 后最大分离为 `0 m`；
- 两片夹爪铰轴的最小绝对点积为 `0.99999999999999`，即严格平行；
- GUI 坐标链与运行时 FK 最大平移误差为 `1.62e-8 m`，最大转角误差为
  `3.72e-6°`；
- GUI 运行时已完成 CPU 装配检查，可加载七个 link、表面高斯和三条末端
  tissue 接触 shape，并可应用最后一个 q7 状态。

以上只证明“P420006 CAD 按它自己的铰接定义正确接入原始运动学”，仍不证明
整体位置、自旋、jaw 零点和外轮廓已经与视频实物一致。

### 优先级 A：`zixingjiang/dvrk_description` 的显式 8 mm LND

[zixingjiang/dvrk_description](https://github.com/zixingjiang/dvrk_description)
明确提供 `8mm Large Needle Driver - Si`，对应宏
[`tool_lnd_8mm_si`](https://github.com/zixingjiang/dvrk_description/blob/main/urdf/include/si/tool.macro.xacro)，
并提供了
[`12_psm_classic_lnd.urdf.xacro`](https://github.com/zixingjiang/dvrk_description/blob/main/urdf/examples/12_psm_classic_lnd.urdf.xacro)
示例入口。它还说明修复了官方描述中的错位网格和错误工具名。

优点：

- 工具名称明确是 Large Needle Driver；
- wrist、yaw、gripper 的分段和坐标定义清楚；
- 第二片夹爪使用精确的 π 镜像，不是当前 Classic SCA 的 `3.1516`。

限制：

- 仓库明确声明是非官方版本；
- README 明确警告其运动学和动力学参数有估算成分；
- 夹爪仍使用同一 LND 网格镜像生成，未必比官方 P420006 的两片独立网格更准确。

结论：**适合借显式 LND 网格、坐标定义和 Classic+LND 组装方式；不采用它的
PSM 运动学数值替换 raw `q7 + LND.json`。**

### 优先级 B：官方 `jhu-dvrk/dvrk_cad`

[jhu-dvrk/dvrk_cad](https://github.com/jhu-dvrk/dvrk_cad) 保存官方原始
SolidWorks CAD。其
[Si/end_effector_tool](https://github.com/jhu-dvrk/dvrk_cad/tree/master/Si/end_effector_tool)
包含 `End_effector.SLDASM`、tip holder、shaft 和 end teeth 等源零件。

优点：

- 是可继续测量、拆件和重新导出 STL 的源 CAD，而不是已经失去装配语义的单个
  render mesh；
- 可检查铰轴、闭合接触面、左右夹爪是否真正对称。

限制：

- 需要 SolidWorks/可兼容转换器导出；
- 仓库中的 `sca_*` 命名仍需结合装配图和视频确认实际器械类型；
- 不能仅凭文件名认定它一定与本次实物完全相同。

结论：**用于追溯 P420006 的装配和几何真值；若 STL 仍不匹配，再从这里重新
拆分、导出。**

### 优先级 C：`WPI-AIM/dvrk_env`

[WPI-AIM/dvrk_env](https://github.com/WPI-AIM/dvrk_env) 声明模型由 WPI
从头设计，提供 PSM 等设备的 CAD、URDF 和 SDF；它也是
`zixingjiang/dvrk_description` Classic 高精度网格的来源。

限制：

- 当前没有找到一个明确标注、且独立于上述 Si 资源的 Classic Large Needle
  Driver；
- Classic 工具很可能仍沿用 Small Clip Applier 几何谱系；
- 因此它更适合替换机械臂视觉网格，不是当前 LND 夹爪的第一替代源。

结论：**保留作外形对照，不作为首选 LND CAD。**

## 6. 采用新 CAD 时必须遵守的边界

1. 冻结 `raw_kinematics_v1`，新 CAD 必须产生新目录和新 pose-driver 名称。
2. 保留由原始 bag、标定、hand-eye、`LND.json` 得到的运动主干。
3. 第一轮只换末端 CAD；相机、table/world、组织、力学和 GUI 参数全部不动。
4. 不再用共线 link 原点确定自旋。注册至少需要：
   - 器械长轴；
   - 一个明确的 pitch/yaw 铰轴方向；
   - 左右夹爪或 CAD 表面上的非共线方向。
5. CAD 选择和固定注册必须在多帧、左右目同时验证，不能由单帧目测决定。
6. jaw 必须单独标定，不允许为了接受负数而只扩大 URDF 限位。

建议的 jaw 映射形式是：

```text
q_cad = clip(scale * sign * (q_raw - q_closed), 0, q_open_max)
```

其中 `q_closed`、`scale`、`sign`、`q_open_max` 必须由原始视频中的多组
闭合/张开帧估计，并在左右目共同验证。

## 7. 新版本验收门槛

新 CAD 版本只有同时满足以下条件，才能替代当前默认版本：

- **型号门槛**：用原始视频近景确认 jaw 外轮廓、长度、弯曲方向、齿面和腕部
  结构与候选 CAD 一致；
- **可观测性门槛**：注册点集至少有两个足够大的横向奇异值，禁止退化成近共线
  拟合；
- **轮廓门槛**：在首、中、末及 jaw 开/闭代表帧上计算真实器械 mask 到 CAD
  轮廓的双向 Chamfer/边缘距离，而不是只检查投影是否在画面内；
- **双目门槛**：同一个三维变换必须同时改善左、右目，不能只拟合一侧；
- **夹爪门槛**：闭合时两片夹爪不穿透，侧视接触面符合实物；张开量随 raw jaw
  单调且方向正确；
- **消融门槛**：A/B 对比时只改变 CAD 和其固定注册，其他 GUI 配置完全相同；
- **回归门槛**：原有 q7 时间戳、GUI 坐标链和运行时 FK 数值检查继续通过。

## 8. 1644 个原始相机时刻的结论

本次没有把 GUI 全链统一成 1644 帧，原因不是 P420006，而是当前图像资产链
已经固定在另一个时段：

| 数据 | 当前数量 | 时间范围/说明 |
|---|---:|---|
| raw 左目消息 | 1644 | `0.0291–54.9839 s` |
| raw 右目消息 | 1644 | `0.00185–54.9520 s` |
| 严格单调一一双目配对 | 1631 | `|dt| <= 20 ms`；左右各有 13 帧无法配对 |
| raw q7 | 5458 | `0–54.9896 s`；这是原生机器人时间轴，不应降采样成 1644 |
| 历史抽帧 PNG | 左 1441 / 右 1443 | 不是完整 raw 相机序列 |
| 当前 GUI MP4 + JSON | 左 1441 / 右 1441 | 左目末时刻 `48.1618 s`，右目末时刻 `48.1321 s` |
| 当前 visual-force tissue mask | 左 1441 / 右 1441 | 与当前 GUI 视频时间戳一一对应 |

当前左目 GUI 结束时只覆盖到 q7 索引 `4779`，后面还有 `678` 个原始 q7
状态；右目结束时后面还有 `681` 个。P420006 pose driver 已经保存全部 5458
个状态，运行时按视频时间戳取对应 q7，因此器械主干没有丢数据。

若只把 MP4/JSON 延长到 1644，最后 203 帧会超出现有左右 tissue mask 的时间
覆盖；目前代码会给这些帧零 visual-force 权重，而不是得到完整等价的 GUI。
如果要求“所有图像派生数据都完整”，还需要重新：

1. 从 bag 分别提取、校验并编码左右各 1644 帧；
2. 决定采用 1631 个严格双目对，还是允许重复/非严格匹配来凑 1644；
3. 传播最后 203 帧的左右 tissue mask；
4. 重建依赖当前 1441 时间轴的深度、器械 mask、视觉跟踪和相关验证资产。

因此这不是小改动，而且“1644 个严格完整双目时刻”在原始数据里实际不存在；
严格配对真值是 1631。按照“改动影响大的话就算了”的要求，本次保持现有
1441 帧 GUI 图像链不变，也不把 5458 个原始机器人状态错误压成 1644。

## 9. 原始双目视觉矫正版

### 9.1 原始输入和人工标注

视觉矫正根目录为：

```text
data/super/psm_visual_calibration/raw_p420006_stereo_v1/
```

10 对关键帧直接来自原始 bag 中 `1631` 对严格单调双目配对，关键帧槽位为：

```text
0, 91, 262, 341, 540, 736, 1239, 1359, 1479, 1630
```

左右图均用原始相机标定直接校正。每一对都人工标注器械主体 mask、两片夹爪
mask 和两个夹爪尖端。标注验证为 `10/10` 通过、`0` error、`0` warning。

组织遮挡不能被当作精确几何真值。根据人工标注说明和双目复查，第 4–6 组
（从 0 开始编号）使用降低的夹爪置信度：

| 关键帧 | 主体 | 夹爪 mask | 尖端 |
|---:|---:|---:|---:|
| 4 | 1.00 | 0.50 | 0.25 |
| 5 | 1.00 | 0.25 | 0.10 |
| 6 | 1.00 | 0.25 | 0.10 |

这意味着遮挡区域的人工估计只能提供弱提示，不能覆盖原始 q7 运动学主干。

### 9.2 双目共同优化

实现参考 `online_dvrk_tracking` 提交
`cb2a264167aaf05b5a9c20da885d48568f78311f` 的 NvDiffRast + CMA-ES。
左右目不是两个独立器械状态：

```text
T_rectRight_link = T_rectRight_rectLeft @ T_rectLeft_link
```

左右图使用各自真实时间戳最近的原始 q7，但同一关键帧对共享一个修正状态。
优化分两层：

1. 所有关键帧共享一个全局相机系修正；
2. 每个关键帧只允许较小、带原始先验和时间先验的局部残差。

原始投影平均损失 `6.27807`，全局修正后为 `2.90112`，最终为 `2.66726`；
10/10 关键帧都优于原始投影。全局修正最大边界占比 `0.750`，局部最大
`0.932`，均通过拒绝截断解的门槛。

最初投影出现的上下镜像是新 NvDiffRast 投影矩阵的 y 符号错误，只影响本次
新渲染器；已按上游 CtRNet 约定修正。原始标定、hand-eye 和 GUI 坐标没有
因此修改。

两个未接受的撞边界结果保留在 `diagnostics/` 中，只作负面诊断，没有接入
GUI。

### 9.3 提升到全部 5458 个原始时刻

固定全局修正在全部时刻保持不变。位置、旋转和腕部的小残差用端点钳制的
PCHIP 在 10 个关键帧之间平滑插值。

夹爪不直接做时间插值。否则遮挡段人工估出的张角会与随后变化的 raw jaw
叠加，在未标注帧产生伪张开。最终采用置信度加权、正斜率的物理映射：

```text
q_cad = 0.9216887510 * q_raw + 0.4927253406 rad
```

即零点补偿约 `28.2311°`，全部原始状态中的 CAD jaw 范围为
`2.6808°–81.1431°`。这是由当前 10 对标注得到的第一版标定，不代表已经
测得回差、左右不对称或钢丝迟滞。

最终 GUI 驱动为：

```text
data/super/psm_visual_calibration/raw_p420006_stereo_v1/gui_v1/
├── psm_p420006_stereo_visual_gui_pose_driver.npz
├── registration_report.json
├── gui_validation_report.json
├── projection_validation_report.json
└── runtime_smoke_report.json
```

验证结果：

- 原始 `5458` 个时间戳和完整 `q7_raw` 逐元素保留；
- 机械臂主干 `q0..q2` 逐元素不变；
- GUI 完整坐标链最大平移误差 `2.18e-8 m`，最大转角误差
  `4.98e-6°`；
- 两片夹爪共铰点最大误差 `1.48e-16 m`，铰轴最小绝对点积
  `0.9999999999999982`；
- 最终 5458 状态 GUI 驱动重新投影到全部 10 对原始双目关键帧后，平均视觉
  损失从 raw P420006 的 `6.27805` 降至 `2.65780`；10/10 均未退化，所有
  高置信帧均逐帧改善；
- A800 上真实 GUI 场景装配通过，P420006 URDF、384 个 tip-only 表面高斯、
  自适应组织均正常加载；首、中、末等 5 个状态回放误差均为 `0`。

### 9.4 可复现命令

```bash
python scripts/validate_super_p420006_stereo_annotations.py

/Media_HDD/jwshan/conda_envs/online_dvrk/bin/python \
  scripts/optimize_super_p420006_stereo_keyframes.py

python scripts/build_super_p420006_stereo_visual_gui_driver.py --overwrite
python scripts/validate_super_p420006_stereo_visual_gui_driver.py

/Media_HDD/jwshan/conda_envs/online_dvrk/bin/python \
  scripts/validate_super_p420006_stereo_visual_projection.py
```

## 10. 下一步人工检查

在 GUI 中依次用 `raw_kinematics`、`raw_p420006` 和
`raw_p420006_stereo_visual` 做 A/B/C，重点观察闭合、半开、全开、器械自旋
和第 4–6 组组织遮挡附近。当前数值检查只证明数据链、双目约束、平滑性和 GUI
接入正确；如果 CAD 外轮廓仍肉眼不符，应继续检查显式 8 mm LND CAD，不能用
更大的位姿自由度去吸收 CAD 型号错误。

## 11. 首帧 SurgicalSAM2 双目在线矫正版

这一版取代上节的“10 对人工 mask + 遮挡降权”作为当前正式视觉路线，但保留
旧版作对照。正式规则是：

1. 左右目只在严格双目对 `0` 使用人工多边形产生稀疏 SAM2 提示；
2. 后续 `1630` 对完全由 SurgicalSAM2 因果传播，不复制人工 mask；
3. 第 4–6 组不再人工降权；
4. 每个时刻的同一个物理残差同时在标定后的左右目中计算；
5. 首对人工标注的两个夹爪尖端只用于原文式初始化，后续尖端由
   ContourTipNet 自动给出；
6. 原始 bag q7、标定、hand-eye、LND 和 P420006 固定 CAD adapter 继续作为
   主干，视觉只估计有界残差。

对应 SurgicalSAM2 输入为：

```text
data/super/psm_visual_calibration/raw_p420006_stereo_v1/
  surgicalsam2_stereo_sequence_v1/
```

### 11.1 必须保留的失败基础版本

无原始运动学残差先验的第一次完整运行保留在：

```text
surgicalsam2_stereo_sequence_v1/online_stereo_cma_v1/
```

该版本 `passed=false`。首帧之后，遮挡区的 SAM2 会丢失黑色长杆，只留下末端；
优化器为了拟合不完整 mask，把局部自由度推向边界：

- `1620/1631` 对至少一个局部参数达到边界的 `95%`；
- 全局最大边界占比 `0.9848`；
- 局部最大边界占比 `1.0`。

它证明“直接照搬图像在线优化、但不给原始机器人主干残差先验”在这段数据上
不可接受，因此没有接入 GUI。

### 11.2 当前接入版本

当前版本在不改变原文 SAM2、ContourTipNet、CMA-ES、前态初始化和 Kalman
结构的前提下，只增加 raw-backbone 归一化残差先验：

```text
global prior = 0.08
local prior  = 0.50
```

完整结果位于：

```text
surgicalsam2_stereo_sequence_v1/online_stereo_cma_v2/
```

验收结果：

- 完整严格双目对：`1631/1631`；
- 双目 ContourTipNet 同时有效：`1580/1631`；
- 首帧全局目标：`0.502276 -> 0.119776`；
- 全局最大边界占比：`0.5062`；
- 局部最大边界占比：`0.6776`；
- `passed=true`。

1631 个 Kalman 后的残差按原始 ROS 时间戳线性采样到全部 `5458` 个 q7 状态，
再从 raw q7/LND FK 重新计算七个 P420006 link；不读取历史 tracked pose。
GUI 驱动为：

```text
surgicalsam2_stereo_sequence_v1/online_stereo_cma_v2/gui_v1/
  psm_p420006_sam2_online_gui_pose_driver.npz
```

驱动 SHA256：

```text
66d30115760deaab38957b8192bc0415f1e2d39890b27a5b94a24c7a872d0692
```

几何/运行时检查：

- q0–q2 与 raw q7 逐元素一致；
- 两片夹爪共铰点最大误差 `1.39e-16 m`；
- 两片夹爪铰轴最小绝对点积 `0.9999999999999984`；
- 当前 GUI 环境中 5 个代表状态的回放误差均为 `0`；
- P420006 URDF、384 个 tip-only 高斯和当前 adaptive tissue 正常装配。

### 11.3 当前 GUI 边界与运行命令

仍使用原来的：

```text
data/super/grasp5_offline_demo
```

左右目视频、组织、visual-force mask 都保持 `1441` 帧不变。新位姿驱动虽然有
全部 `5458` 状态，但当前 GUI 只播放到左目 `48.1618 s`，即约 q7 索引
`4779`；最后约 `6.83 s` 不会在这一 GUI 中显示。

默认命令现在直接使用本版位姿：

```bash
bash scripts/run_demo_thinlinc.sh --visual-force-iterations 1
```

显式选择本版：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_p420006_sam2_online
```

切换回纯机器人学/P420006 对照：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_kinematics

bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_p420006
```

## 12. paper LND 重新首帧分割 + 原文式双目矫正版

### 12.1 与 P420006 版本隔离

新根目录为：

```text
data/super/psm_visual_calibration/raw_paper_lnd_stereo_v1/
```

该目录从原始 bag 重新提取冻结配对中的严格双目第 `0` 对：

```text
left source index  = 0
right source index = 1
strict pair count  = 1631
```

没有读取或复制以下内容：

```text
raw_p420006_stereo_v1/annotations
raw_p420006_stereo_v1/surgicalsam2_stereo_sequence_v1
raw_p420006_stereo_v1/online_stereo_cma_v1
任何历史 corrected pose driver
```

本次重新人工标注保存在：

```text
raw_paper_lnd_stereo_v1/annotations/keyframe_00.json
```

左右目均重新标了 body、两片物理夹爪和两个可见尖端；源图 SHA256 与新
manifest 一致。该标注只用于在左右目首帧生成稀疏前景/背景提示，以及首帧
两个尖端初始化；人工 mask 像素不会复制到后续帧。

### 12.2 原文方法及双目扩展

固定上游仓库：

```text
https://github.com/hanyang-hu/online_dvrk_tracking
commit cb2a264167aaf05b5a9c20da885d48568f78311f
```

优化器与 GUI 使用同一套 paper LND 四部件定义：

```text
shaft   low_res_shaft_multi_cylinder.ply
logo    low_res_logo_low_res_1.ply
jaw R   low_res_jawright_lowres.ply
jaw L   low_res_jawleft_lowres.ply
FK      diffcali/eval_dvrk/LND_fk.py
```

新 Torch 批量 FK 与上游/NumPy paper FK 在随机姿态上的最大矩阵绝对误差为
`2.22e-16`。没有再用 paper 状态驱动 P420006 或旧 URDF CAD。

保留的原文结构：

1. SurgicalSAM2 endoscopic checkpoint；
2. 首帧稀疏点击初始化和后续因果传播；
3. NvDiffRast hard silhouette；
4. ContourTipNet 尖端；
5. 首帧全局 CMA-ES；
6. 后续每对 3 代、population 70、前态初始化；
7. 原文 Kalman。

双目扩展仅为：左右目分别传播 mask，但每个时刻只优化一个共享物理状态；
右目器械位姿固定为标定的 `T_rectifiedRight_rectifiedLeft` 乘左目位姿。不做
第 4–6 组降权。

### 12.3 完整运行结果

SurgicalSAM2：

```text
complete strict stereo pairs = 1631/1631
left empty masks             = 0
right empty masks            = 0
left median mask area        = 17969 px（960x540）
right median mask area       = 17210 px（960x540）
```

paper LND 双目 CMA-ES/Kalman：

```text
first-pair global loss       = 0.481168 -> 0.115735
mean initial loss            = 1.001866
mean measured loss           = 1.000840
mean filtered loss           = 1.000998
ContourTipNet stereo valid   = 1590/1631
max global bound fraction    = 0.4151
max local bound fraction     = 0.6619
passed                       = true
```

全局修正为：

```text
rotation-vector deg = [-4.4879, -4.9085, -5.1005]
translation mm      = [-3.3207, 2.5428, -0.7234]
q3..q6 deg          = [-3.3013, 3.7655, -5.9702, 9.5998]
```

全部 `1631` 对的 Kalman 残差按原始 ROS 时间戳线性采样到完整 `5458` 个 q7
状态；q0–q2 与冻结 raw q7 逐元素相同。当前旧 GUI 的视频/组织/mask 仍保持
`1441` 帧，按原时间轴读取这 5458 状态，所以不会引入新的双目或组织帧数
连锁改动；GUI 末尾之后约 6.8 秒的原始机器人状态仍保存但不会播放。

GUI 资产位于：

```text
raw_paper_lnd_stereo_v1/
  surgicalsam2_stereo_sequence_v1/
    online_stereo_cma_v1/gui_v1/
      psm_paper_lnd.urdf
      psm_paper_lnd_surface_gaussians.npz
      psm_paper_lnd_gui_pose_driver.npz
      registration_report.json
      runtime_smoke_report.json
```

driver SHA256：

```text
2253ac57cd3bf1bafacff6b461c8b16645283e9bf4378b034e1565acf345a5d0
```

GUI 运行时检查：

- 正确选择 `psm_paper_lnd.urdf`，没有选择 P420006 URDF；
- 5458 个状态、7 个 GUI 驱动链接、4 个真实可见 paper 部件正常装配；
- tip 模式加载 192 个腕/夹爪表面 Gaussian；
- 抽查状态 `0/1364/2728/4093/5457`，写入 Warp 的位姿误差均为 `0`；
- 当前 GUI table/world 矩阵门槛通过。

### 12.4 当前运行与版本切换

默认已改为本版，其他 GUI 参数未改：

```bash
bash scripts/run_demo_thinlinc.sh --visual-force-iterations 1
```

显式选择 paper LND 本版：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_paper_lnd_sam2_online
```

切换回旧 P420006 SurgicalSAM2 版：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_p420006_sam2_online
```

切换回严格纯机器人运动学负基线：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_kinematics
```

### 12.5 从新首帧开始的复现命令

```bash
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/prepare_super_paper_lnd_stereo_first_frame.py --overwrite

DISPLAY=:14 /Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/annotate_super_p420006_stereo_keyframes.py \
  --root data/super/psm_visual_calibration/raw_paper_lnd_stereo_v1

/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/track_super_p420006_stereo_surgicalsam2.py \
  --instrument paper_lnd \
  --visual-root data/super/psm_visual_calibration/raw_paper_lnd_stereo_v1 \
  --output-dir data/super/psm_visual_calibration/raw_paper_lnd_stereo_v1/\
surgicalsam2_stereo_sequence_v1 \
  --anchor-policy first --cuda-device 1

MPLCONFIGDIR=/tmp/paper_lnd_matplotlib \
  /Media_HDD/jwshan/conda_envs/online_dvrk/bin/python \
  scripts/optimize_super_p420006_stereo_sam2_online.py \
  --geometry paper_lnd \
  --visual-root data/super/psm_visual_calibration/raw_paper_lnd_stereo_v1 \
  --masks data/super/psm_visual_calibration/raw_paper_lnd_stereo_v1/\
surgicalsam2_stereo_sequence_v1/stereo_surgicalsam2_masks.npz \
  --sam2-report data/super/psm_visual_calibration/raw_paper_lnd_stereo_v1/\
surgicalsam2_stereo_sequence_v1/report.json \
  --first-annotation data/super/psm_visual_calibration/\
raw_paper_lnd_stereo_v1/annotations/keyframe_00.json \
  --mesh-dir /Media_HDD/jwshan/wad/online_dvrk_tracking/urdfs/dVRK/meshes \
  --output-dir data/super/psm_visual_calibration/raw_paper_lnd_stereo_v1/\
surgicalsam2_stereo_sequence_v1/online_stereo_cma_v1 \
  --cuda-device 1

/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/build_super_paper_lnd_sam2_online_gui.py
```

## 13. paper LND 多锚点双目 SAM2 与中段夹爪闭合版

版本名：

```text
raw_paper_lnd_sam2_multianchor_closedjaw
```

本版仍从冻结的原始 bag/q7、相机标定、hand-eye 和 LND 运动学主干开始，不读取
P420006、旧视觉位姿或旧 mask。CAD/FK 继续固定为原文仓库 commit
`cb2a264167aaf05b5a9c20da885d48568f78311f` 的 paper LND。

### 13.1 标注与 SAM2 路径

严格双目锚点为：

```text
0, 200, 400, 600, 800, 1000, 1200, 1400
```

每个锚点、每只眼睛均人工确认：

1. 两个物理夹爪尖端；
2. 银色腕部和夹爪区域；
3. 黑色长杆区域。

点击给出正负提示后由实时 SurgicalSAM2 直接产生区域，不是逐点绘制人工
mask。黑杆和银色末端使用两个独立对象传播，最终位姿观测为二者并集。每
200 对从新的人工确认锚点重新开始因果传播；左右目各自传播，但只优化同一个
共享三维器械状态。

标注和预览：

```text
raw_paper_lnd_stereo_multianchor_v2/
  annotations/live_sam2_multianchor_v4/
    annotation_state.json
    live_sam2_anchor_overview.png
```

完整传播结果：

```text
strict stereo pairs          = 1631/1631
empty/invalid union masks    = 0
part partition warnings      = 66
passed                       = true
```

66 个 warning 只出现在后段右目黑杆逐渐离开视野时的“shaft/distal 分区”检查，
对应的联合器械轮廓仍有效，因此不作为无效帧，也没有做置信度降权。源图
index 1441 的一次 bag 编码声明异常按实际 UYVY 载荷解码并记录在报告中。

```text
raw_paper_lnd_stereo_multianchor_v2/
  surgicalsam2_multianchor_parts_v4（paper_LND+视觉矫正）/
    stereo_multianchor_part_masks.npz
    report.json
    previews/representative_part_propagation.png
```

mask SHA256：

```text
d6378979a2e8cfc382094e113b15226d8ee92ee49dd3735ef9d613e920a784ff
```

### 13.2 paper LND 双目优化和夹爪约束

保留原文的 hard silhouette、ContourTipNet、首对 CMA-ES、逐对
`3 generations × population 70`、前态初始化和 Kalman；双目仅扩展为左右目
共同约束一个状态。

与上一版唯一关键约束差异是：

```text
global q7 jaw residual       = 0.0
all measured q7 residuals    = 0.0
all filtered q7 residuals    = 0.0
```

因此视觉轮廓不能把中段夹爪重新拉开。GUI 中的夹爪状态逐元素等于原始 q7：

```text
pair slot 0/200/400          = +0.999829 rad（张开）
pair slot 600/800/1000       = 约 -0.099 rad（闭合）
pair slot 1200               = -0.099803 rad（闭合）
pair slot 1400               = +0.999829 rad（重新张开）
```

完整优化结果：

```text
strict stereo pairs          = 1631
first-pair global loss       = 0.460443 -> 0.058720
mean initial loss            = 0.160618
mean measured loss           = 0.160261
mean filtered loss           = 0.160300
bilateral tip accepted       = 567/1631
max global bound fraction    = 0.709627
max local bound fraction     = 0.663927
passed                       = true
```

全局修正：

```text
rotation-vector deg = [3.52719, -0.78033, 1.97659]
translation mm      = [-2.99553, 1.99187, -2.83851]
distal q deg        = [-0.76012, 11.35213, -0.11278, 0.0]
```

优化结果和锚点叠加图：

```text
surgicalsam2_multianchor_parts_v4（paper_LND+视觉矫正）/
  online_stereo_cma_closedjaw_v1/
    online_stereo_corrections.npz
    report.json
    previews/anchor_online_overlay.png
```

correction SHA256：

```text
ad7af56fac47f6fb2fb8247de83250eb3529f87c6aaa6efcd7bc83573b84c5d9
```

### 13.3 接入旧 GUI

GUI 资产保留全部 `5458` 个原始机器人状态。旧 GUI 的左右视频、组织序列和
visual-force mask 仍为原来的 `1441` 帧，没有覆盖或扩帧。完整视觉矫正的
相对时间范围约为 `0.025–54.957 s`；旧左目为 `0.029–48.162 s`，旧右目首帧
比首个严格双目对早约 `23 ms`，该极短前段使用首个修正量的 clamped
interpolation，之后全部旧 GUI 帧均落在矫正范围内。末尾约 6.8 秒仍保存在
驱动中但旧 GUI 不播放。

资产目录：

```text
online_stereo_cma_closedjaw_v1/gui_v1/
  psm_paper_lnd.urdf
  psm_paper_lnd_mimic_map.json
  psm_paper_lnd_surface_gaussians.npz
  psm_paper_lnd_gui_pose_driver.npz
  registration_report.json
  runtime_smoke_report.json
```

driver SHA256：

```text
9345ac928f843149aec72aebd34a1c0662c559fc8c8bfc027eb2076e3548bb0a
```

GPU GUI 重放检查通过：

- 正确选择本版 paper LND URDF 和位姿源；
- 5458 个状态、7 个驱动刚体、4 个可见 paper 部件正常装配；
- 表面 Gaussian 正常加载；
- 抽查状态 `0/1364/2728/4093/5457`，Warp 位姿最大误差均为 `0.0`。

### 13.4 运行与切换命令

本节版本在构建时曾作为默认；自 2026-08-01 起当前临时默认已切换为第 18 节：

```bash
bash scripts/run_demo_thinlinc.sh --visual-force-iterations 1
```

显式选择本版：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_paper_lnd_sam2_multianchor_closedjaw
```

本版 GUI 额外显示 `PSM wrist rod left/right deg`。它对应 paper LND FK 的
`outer_wrist_pitch`（q7 索引 4），用于让银色杆、末端轴和两片夹爪作为同一
刚体段左右摆动。默认严格为 `0`，不会改变初始位姿；不再使用此前错误的
`outer_wrist_yaw` world-delta/URDF 重建路径。实现直接调用与生成 driver 相同
的 paper component FK，并以当前 `PSM1_tool_wrist_link` 为固定锚点。

切换回上一版首帧 paper LND：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_paper_lnd_sam2_online
```

切换回严格纯机器人运动学负基线：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_kinematics
```

### 13.5 完整优化与 GUI 重建命令

```bash
MPLCONFIGDIR=/tmp/paper_lnd_matplotlib \
  /Media_HDD/jwshan/conda_envs/online_dvrk/bin/python \
  scripts/optimize_super_p420006_stereo_sam2_online.py \
  --observation-mode multianchor_parts \
  --geometry paper_lnd \
  --visual-root data/super/psm_visual_calibration/\
raw_paper_lnd_stereo_multianchor_v2 \
  --masks data/super/psm_visual_calibration/\
raw_paper_lnd_stereo_multianchor_v2/\
surgicalsam2_multianchor_parts_v4（paper_LND+视觉矫正）/stereo_multianchor_part_masks.npz \
  --sam2-report data/super/psm_visual_calibration/\
raw_paper_lnd_stereo_multianchor_v2/\
surgicalsam2_multianchor_parts_v4（paper_LND+视觉矫正）/report.json \
  --anchor-state data/super/psm_visual_calibration/\
raw_paper_lnd_stereo_multianchor_v2/annotations/\
live_sam2_multianchor_v4/annotation_state.json \
  --paper-repo /Media_HDD/jwshan/wad/online_dvrk_tracking \
  --mesh-dir /Media_HDD/jwshan/wad/online_dvrk_tracking/urdfs/dVRK/meshes \
  --output-dir data/super/psm_visual_calibration/\
raw_paper_lnd_stereo_multianchor_v2/\
surgicalsam2_multianchor_parts_v4（paper_LND+视觉矫正）/online_stereo_cma_closedjaw_v1 \
  --freeze-jaw-correction \
  --cuda-device 1
```

GUI 重建时必须传仓库内的绝对输出路径，以满足表面采样器的路径检查：

```bash
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/build_super_paper_lnd_sam2_online_gui.py \
  --raw-root "$PWD/data/super/psm_raw_kinematics_v1（纯机器人学版本）" \
  --visual-root "$PWD/data/super/psm_visual_calibration/\
raw_paper_lnd_stereo_multianchor_v2" \
  --optimization-root "$PWD/data/super/psm_visual_calibration/\
raw_paper_lnd_stereo_multianchor_v2/surgicalsam2_multianchor_parts_v4（paper_LND+视觉矫正）/\
online_stereo_cma_closedjaw_v1" \
  --paper-repo /Media_HDD/jwshan/wad/online_dvrk_tracking \
  --output-dir "$PWD/data/super/psm_visual_calibration/\
raw_paper_lnd_stereo_multianchor_v2/surgicalsam2_multianchor_parts_v4（paper_LND+视觉矫正）/\
online_stereo_cma_closedjaw_v1/gui_v1" \
  --pose-version raw_paper_lnd_sam2_multianchor_closedjaw \
  --require-frozen-jaw-correction \
  --overwrite
```

## 14. paper LND 接触段加密提示双目版

版本名：

```text
raw_paper_lnd_sam2_dense_contact_closedjaw
```

本版保留第 13 节的原始 bag/q7、相机标定、hand-eye、paper LND CAD/FK、
双目共享残差和闭合夹爪约束；不读取旧视觉位姿。原有 8 组可靠锚点继续使用，
并在器械接触/牵拉组织的 500–1200 段增加 6 组可靠双目提示：

```text
0, 200, 400, 500, 540, 600, 700, 747, 800, 900, 1000, 1100, 1200, 1400
```

新增锚点为 `500、540、700、747、900、1100`。每个锚点、每只眼均确认两个
物理夹爪尖端、银色末端 SAM2 区域和黑色长杆 SAM2 区域。锚点之间继续采用
论文 SurgicalSAM2 因果传播；姿态优化使用左右目共享状态的 NvDiffRast
轮廓、ContourTipNet/CMA-ES 和 Kalman filter。没有对组织遮挡段降权，视觉
jaw 残差固定为零，原始 q7 的中段闭合状态完整保留。

### 14.1 输出与质量

主要目录：

```text
data/super/psm_visual_calibration/raw_paper_lnd_stereo_dense_contact_v3/
  annotations/live_sam2_dense_contact_v5/
  surgicalsam2_multianchor_parts_dense_contact_v5/
    stereo_multianchor_part_masks.npz
    report.json
    online_stereo_cma_closedjaw_dense_contact_v2/
      online_stereo_corrections.npz
      report.json
      gui_dense_contact_v2/
```

SAM2 传播结果：

- 完整严格双目对：`1631/1631`；
- 无效并集掩膜：`0`；
- 分区告警：`66`，仍全部集中在约 1530 帧以后右目长杆离开视野的末段；
- 500–1200 接触段不降权。

同第 13 节旧版比较：

- 全序列平均 filtered loss：`0.160300 -> 0.158793`；
- 500–1200 平均 filtered loss：`0.131324 -> 0.127935`；
- 500–799 平均 filtered loss：`0.170643 -> 0.162019`；
- 关键接触帧 747：`0.817073 -> 0.133101`；
- 双目同时有有效尖端约束的时刻：`567 -> 571`。

全局首锚点保持不变，所以全局配准仍为：

```text
0.460443 -> 0.058720
```

### 14.2 GUI 接入

GUI driver 保留全部 `5458` 个原始机器人状态，并继续由旧 GUI 的 1441 帧
时间线选取。资产目录：

```text
online_stereo_cma_closedjaw_dense_contact_v2/gui_dense_contact_v2/
```

driver SHA256：

```text
4a944883643a4e711b717693367ce638225e86a336078423e2ec427b5e1836a1
```

GPU GUI 烟雾检查通过：新版位姿源、paper LND URDF、7 个驱动刚体、表面
Gaussian 均正确加载；抽查状态 `0/1364/2728/4093/5457`，Warp 回放最大位姿
分量误差均为 `0.0`。

本节版本在构建时曾作为默认；自 2026-08-01 起当前临时默认已切换为第 18 节：

```bash
bash scripts/run_demo_thinlinc.sh --visual-force-iterations 1
```

显式选择本版：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_paper_lnd_sam2_dense_contact_closedjaw
```

切回第 13 节旧 8 锚点版：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_paper_lnd_sam2_multianchor_closedjaw
```

## 15. 首帧双目静态 SE(3) + q5 零位版

版本名：

```text
raw_paper_lnd_first_stereo_static_q5
```

本版是“视觉只标定首帧，后续纯机器人学”的对照版本。输入仍直接来自原始
bag、原始 q7、相机标定、hand-eye 和论文 paper LND CAD/FK，不读取此前任何
视觉矫正位姿。视觉观测严格限制为原始 `strict_pair_slot=0` 的左右目：使用
重新标注首帧生成的双目 SurgicalSAM2 整器械 mask，以及左右目各两个人工
夹爪尖端。

首帧共同优化 7 个参数：

```text
左目相机坐标系 rotvec(3) + translation(3) + paper q5/wrist_pitch 零位(1)
```

左右目不允许各自拟合位姿。右目始终为冻结的 `T_right_left @ T_left`；q5
通过论文 `LND_fk.py` 的 component FK 施加，使银色腕部和下游夹爪围绕真实
腕关节一起左右旋转。q4 自旋、q6、q7 夹爪开合全部冻结。

### 15.1 论文方法与静态扩展

首帧优化保留 `online_dvrk_tracking` commit
`cb2a264167aaf05b5a9c20da885d48568f78311f` 的初始化设置：

- NvDiffRast hard silhouette，不做 antialiasing；
- `mse_weight=6`；
- 初始化欧氏距离损失 `12e-7`；
- 面积损失 `6e-6`；
- 两尖端 permutation-invariant + centerline L1，初始化权重 `5e-3`；
- CMA-ES population `30`、iterations `100`。

双目扩展只增加“同一个物理状态在两只标定相机中共同求损失”。完成 pair 0
后不再读取 mask、尖端网络或视频，不运行逐帧 CMA-ES、Kalman 或插值残差。

固定变换施加规则为：

```text
T_left_frame4_new(t) = delta_T_left @ T_left_frame4_raw(t)
q5_new(t) = q5_raw(t) + delta_q5
```

所有 paper 部件随后由完整 FK 重算，不能直接在 GUI/world 坐标中旋转网格。

### 15.2 优化结果

最终采用非贴边搜索范围的结果：

```text
loss:                 0.7188717 -> 0.1421672
improvement:          80.22%
rotation rotvec deg:  [-6.414729, -8.710579, -7.307743]
translation mm:       [+9.502585, -3.566829, -12.291203]
q5 zero offset deg:   -13.125855
max boundary ratio:   0.791882
```

首帧优化前后左右目叠加图：

```text
data/super/psm_visual_calibration/raw_paper_lnd_first_stereo_static_v1/
  previews/first_pair_before_after.png
```

### 15.3 机器人学和 GUI 硬约束

GUI driver 保留全部 `5458` 个原始机器人状态，旧 GUI 仍按原来的 1441 帧
时间线选择状态。自动检查结果：

- q1–q4、q6 和 q7 与原始七关节序列逐元素完全相同，只有 q5 加固定零位；
- q5 全序列增量严格恒定为 `-0.2290893793 rad`；
- per-frame visual correction 数量严格为 `0`；
- 每一帧 `T_new_frame4 @ inv(T_raw_frame4)` 均为同一个 `delta_T_left`；
- frame4 相对运动与原始机器人学一致；
- 右目完全由固定左右目标定从左目导出；
- Warp GUI 抽查 `0/1364/2728/4093/5457`，位姿分量误差均为 `0.0`。

资产目录：

```text
data/super/psm_visual_calibration/raw_paper_lnd_first_stereo_static_v1/
  first_stereo_static_correction.npz
  report.json
  gui_v1/
    psm_paper_lnd.urdf
    psm_paper_lnd_mimic_map.json
    psm_paper_lnd_surface_gaussians.npz
    psm_paper_lnd_gui_pose_driver.npz
    registration_report.json
    runtime_smoke_report.json
```

driver SHA256：

```text
d1b21ab37b2c9ee117a2362bcb4592db26fe39c7bcede62625c451f82a8b42d6
```

### 15.4 运行、切换与重建

本版现保留为可选对照版本。自 2026-08-01 起当前临时默认为第 18 节，
因此运行默认命令不会选择本版：

```bash
bash scripts/run_demo_thinlinc.sh --visual-force-iterations 1
```

显式选择本版：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_paper_lnd_first_stereo_static_q5 \
  --psm-roll-offset-deg 0 \
  --psm-camera-translation-mm 0 0 0
```

显式选择旧接触段多锚点逐帧视觉版：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_paper_lnd_sam2_dense_contact_closedjaw
```

完整重建命令：

```bash
OVERWRITE=1 CUDA_DEVICE=1 \
  bash scripts/run_super_paper_lnd_first_stereo_static.sh
```

## 16. 首帧双目固定 SE(3)、q5 原始不变版

版本名：

```text
raw_paper_lnd_first_stereo_se3_fixed_q5
```

本版对应“优化 XYZ 和三维旋转、修正倾转并允许改变整体自旋、q5 不改动”。
视觉输入仍只使用第 15 节相同的原始 `strict_pair_slot=0`：左右目 SurgicalSAM2
整器械 mask 和左右目各两个尖端。它不读取任何旧的视觉矫正位姿。

首帧只有 6 个有效优化参数：

```text
左目相机坐标系 rotvec(3) + translation XYZ(3)
```

其中完整 `SO(3)` 旋转既可改变杆的深度倾转，也可改变器械整体自旋。右目没有
独立修正，始终使用标定给出的 `T_right_left @ T_left`。固定变换求得后应用于
全部原始机器人状态：

```text
T_left_frame4_new(t) = delta_T_left @ T_left_frame4_raw(t)
q_new(t) = q_raw(t)                  # 7 列全部严格不变
```

因此后续每帧的 q5 不是 0，也不是固定值；它始终是该原始时刻记录的
`q5_raw(t)`。为保持上游自定义 CMA-ES 的数值布局，优化器内部仍使用 7 维状态，
但第 7 维不进入渲染，产物中的 q5 offset 被硬性设为 `0`。

### 16.1 优化结果

```text
loss:                 0.7188717 -> 0.1499699
improvement:          79.14%
rotation rotvec deg:  [-8.655176, -7.468954, -2.699813]
translation mm:       [+7.572729, -8.542890, -10.471781]
q5 zero offset deg:   0.000000
max boundary ratio:   0.711907
```

首帧优化前后左右目叠加图：

```text
data/super/psm_visual_calibration/raw_paper_lnd_first_stereo_se3_fixed_q5_v1/
  previews/first_pair_before_after.png
```

### 16.2 全时序与 GUI 验证

资产目录：

```text
data/super/psm_visual_calibration/raw_paper_lnd_first_stereo_se3_fixed_q5_v1/
  first_stereo_se3_fixed_q5.npz
  report.json
  gui_v1/
    psm_paper_lnd.urdf
    psm_paper_lnd_mimic_map.json
    psm_paper_lnd_surface_gaussians.npz
    psm_paper_lnd_gui_pose_driver.npz
    registration_report.json
    runtime_smoke_report.json
```

硬性检查结果：

- 保留全部 `5458` 个原始机器人状态；
- driver 的 `q7` 与 `q7_raw` 位级完全相同，包括 q5；
- q5 offset 严格为 `0.0 rad`，逐帧视觉修正数量严格为 `0`；
- 固定左目变换最大误差 `4.72e-16`；
- frame4 相对机器人运动最大误差 `4.44e-16`；
- 右目变换链误差 `0.0`；
- CUDA GUI 抽查 `0/1364/2728/4093/5457`，7 个部件的回放位姿分量误差
  全部为 `0.0`。

driver SHA256：

```text
ade70540873f4f811d7e8ad7196e7fac8c7538463ab5dc2614ffaa628a2b4391
```

### 16.3 运行与重建

本版保留为可选对照；当前临时默认为第 18 节。显式查看本版时保持 roll 和
相机平移为 0，避免对已优化的 SE(3) 再做第二次修正：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_paper_lnd_first_stereo_se3_fixed_q5 \
  --psm-roll-offset-deg 0 \
  --psm-camera-translation-mm 0 0 0
```

完整重建：

```bash
OVERWRITE=1 CUDA_DEVICE=1 \
  bash scripts/run_super_paper_lnd_first_stereo_se3_fixed_q5.sh
```

## 17. 全 1631 对双目时变 SE(3)、全部关节原始不变版

版本名：

```text
raw_paper_lnd_sam2_dense_contact_se3_only
```

本版重新从纯机器人运动学主干出发：原始 bag q7、相机标定、hand-eye、LND
和 paper LND CAD/FK。禁止读取第 13–16 节生成的任何校正位姿。视觉输入沿用
第 14 节人工确认的 14 组双目关键帧以及由这些锚点传播得到的完整 1631 对
SurgicalSAM2 mask；接触段包含 500、540、600、700、747、800、900、1000、
1100、1200 等关键时刻。

### 17.1 状态定义

全局首锚点和每一对后续双目帧都只有 6 个有效视觉变量：

```text
[rx, ry, rz, tx, ty, tz]
```

内部保留上游 CMA-ES 的 10 维数值布局以避免改变论文优化器，但渲染前及保存
后均硬性设置：

```text
delta_q4(t) = delta_q5(t) = delta_q6(t) = delta_jaw(t) = 0
q_new(t) = q_raw(t)     # 七列逐位完全相同
```

总视觉修正由固定系统偏差和逐帧时变残差组成：

```text
R_total(t) = R_local(t) @ R_global
p_total(t) = p_global + p_local(t)
```

固定部分吸收相机标定/hand-eye 的系统误差；每帧局部部分使用双目 mask 和尖端
约束，并保留论文方法的上一状态初始化、每帧 3 代 population 70 CMA-ES 和
Kalman 滤波。左右目共同优化同一个物理 SE(3)，右目不拥有独立状态。

### 17.2 优化结果

```text
strict stereo pairs:       1631/1631
global loss:               0.4604433 -> 0.0673744
sequence mean loss:        0.1443423 -> 0.1439896 (filtered)
global rotvec deg:          [+1.672827, -3.058857, -1.179738]
global translation mm:     [-3.961545, +3.282558, -3.174999]
global q4/q5/q6/jaw:       [0, 0, 0, 0]
valid bilateral tip pairs: 571/1631
```

实际逐帧局部修正范围：

```text
rotation deg:
  rx [-0.420371, +0.420522]
  ry [-0.380676, +0.198462]
  rz [-0.363062, +0.615105]

translation mm:
  x  [-0.766209, +0.048755]
  y  [-0.480484, +0.204680]
  z  [-0.105826, +0.047426]
```

14 组人工关键帧的左右目 mask/CAD 叠加预览：

```text
data/super/psm_visual_calibration/raw_paper_lnd_stereo_dense_contact_v3/
  surgicalsam2_multianchor_parts_dense_contact_v5/
  online_stereo_cma_se3_only_dense_contact_v3/
  previews/anchor_online_overlay.png
```

### 17.3 GUI 与硬约束

1631 对时变 SE(3) 按时间线性插值到全部 5458 个原始机器人状态。自动检查：

- 严格双目 slot 为连续完整的 `0..1630`；
- 全局 q4–jaw 残差非零数为 `0`；
- 1631 帧局部 q4–jaw 残差非零数为 `0`；
- 插值到 5458 状态后的 q4–jaw 残差非零数为 `0`；
- GUI driver 的完整 `q7` 与 `q7_raw` 位级相同；
- CUDA GUI 正确加载新位姿源、paper LND URDF 和 7 个器械部件；
- 抽查 `0/1364/2728/4093/5457`，回放位姿最大分量误差全部为 `0.0`。

driver SHA256：

```text
ede21d43aeb2d58d82ad523a27961efd0d8b6c7a9a570d9401e8d73584a70350
```

### 17.4 运行与重建

本版保留为有硬边界的 A/B 对照。显式查看本版时应保持 roll 和相机平移为零：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_paper_lnd_sam2_dense_contact_se3_only \
  --psm-roll-offset-deg 0 \
  --psm-camera-translation-mm 0 0 0
```

完整重建：

```bash
OVERWRITE=1 CUDA_DEVICE=1 \
  bash scripts/run_super_paper_lnd_dense_contact_se3_only.sh
```

## 18. 22 对夹取锚点、XYZ 无硬边界版（当前临时默认）

版本名：

```text
raw_paper_lnd_sam2_dense_contact_unbounded_xyz
```

本版仍从原始 bag/q7、相机标定、hand-eye、LND 和 paper LND CAD/FK 主干
出发，不读取旧版矫正位姿。在第 17 节 14 对双目锚点基础上，于夹取段新增：

```text
570, 650, 725, 775, 850, 950, 1050, 1150
```

完整 22 对锚点为：

```text
0, 200, 400, 500, 540, 570, 600, 650, 700, 725, 747,
775, 800, 850, 900, 950, 1000, 1050, 1100, 1150, 1200, 1400
```

每个锚点的左右目人工夹爪尖端均作为高可信观测；考虑到夹爪被组织遮挡时
SAM2 只能分割可见区域，人工锚点尖端损失使用论文 `3e-3` 权重的 3 倍。轮廓
项仍保留，未按遮挡帧删除或降权。双目 SurgicalSAM2 因果传播完整覆盖
`1631/1631` 对，硬质量门为 `0` 个无效。

### 18.1 无硬边界参数化

有效视觉变量仍只有：

```text
[rx, ry, rz, tx, ty, tz]
```

XYZ 改为线性无截断参数，不再使用全局 `±8/±4 mm` 或局部
`±1/±0.75 mm` 的 tanh 硬边界。`5 mm` 仅为 CMA-ES 数值条件尺度，不是
最大修正量；平移只受连续软先验约束。三维旋转仍采用有界参数，全部 q1–q7
视觉残差严格固定为零。

结果：

```text
global translation mm: [-3.280980, +3.538967, -6.936125]
global rotvec deg:      [-0.799711, -10.598383, -3.346089]

local translation mm:
  x [-2.673075, +0.082430]
  y [-0.640842, +0.521502]
  z [-1.655259, +1.446335]

total translation mm:
  x [-5.954055, -3.198551]
  y [+2.898126, +4.060470]
  z [-8.591385, -5.489790]
```

与第 17 节旧硬边界版比较：

```text
all-frame mean filtered loss: 0.143990 -> 0.114466
500-1200 mean filtered loss:   0.104600 -> 0.092773
valid bilateral tip pairs:     571 -> 577
```

自动检查：

- 优化、GUI registration 和 CUDA GUI runtime smoke 均通过；
- driver 的 `q7` 与 `q7_raw` 位级完全相同；
- 全局及 1631 对局部 q4–jaw 残差非零数均为 `0`；
- 抽查状态 `0/1364/2728/4093/5457`，GUI 回放最大位姿分量误差均为 `0.0`；
- driver SHA256：
  `60b55055104719a88c38fbd76c73d4c01a62e16a51ab9b1fac111fce7eb30ae6`。

主要目录：

```text
data/super/psm_visual_calibration/raw_paper_lnd_stereo_dense_contact_v4/
  annotations/live_sam2_dense_contact_v6/
  surgicalsam2_multianchor_parts_dense_contact_v6/
    online_stereo_cma_se3_unbounded_dense_contact_v4/
      gui_unbounded_xyz_v1/
```

自 2026-08-01 起，本版为 `run_demo_thinlinc.sh` 的临时默认；脚本内置 roll
和相机平移已经归零。因此原运行命令即可进入本版：

```bash
bash scripts/run_demo_thinlinc.sh --visual-force-iterations 1
```

等价的显式运行命令：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_paper_lnd_sam2_dense_contact_unbounded_xyz \
  --psm-roll-offset-deg 0 \
  --psm-camera-translation-mm 0 0 0
```

切回此前 14 锚点、有硬 XYZ 边界的第 17 节版本：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1 \
  --psm-pose-driver raw_paper_lnd_sam2_dense_contact_se3_only \
  --psm-roll-offset-deg 0 \
  --psm-camera-translation-mm 0 0 0
```

完整重建：

```bash
OVERWRITE=1 CUDA_DEVICE=1 \
  bash scripts/run_super_paper_lnd_dense_contact_unbounded_xyz.sh
```
