# 修改后的方案

以下内容只包含需要新增或替换的部分。原有的组织资产、四面体网格、XPBD 三类约束、碰撞外皮、器械轨迹和前四章保持不变。

## 当前方案保留

*   四面体 XPBD 物理组织
*   三角碰撞外皮
*   双侧 $u_t$ 抓取
*   高分辨率视觉网格
*   每个视觉面一个组织 Gaussian
*   左右目 2D RGB residual

---

## 需要修改的部分

*   在线刚度更新
*   历史状态稳定性验证
*   下一步和多步 prediction gap
*   $u_t$ 与刚度更新的解耦
*   Gaussian 始终保持在组织表面
*   分离记录图像、物理、材料和预测指标

本方案参考当前框架、Liang 等人的 real-to-sim 方法以及 GauSTAR 的 Gaussian Surface 表示：`CURRENT_SUPER_TISSUE_FRAMEWORK(1).pdf`、`2309.11656v2.pdf`、`2501.10283v3 (1).pdf`。

---

## 1. $u_t$ 与刚度更新的关系

$u_t$ 产生的是人为指定的运动，不能直接用于判断材料软硬。因此，以下节点不应参与刚度信号计算：

*   4 个 direct anchors
*   support nodes
*   direct anchors 与 support nodes 的一环物理邻域

此外，以下时间段暂停刚度更新：

*   capture 前后若干帧
*   release 前后若干帧
*   $q_7$ 快速闭合或打开阶段
*   夹爪发生明显穿透或接触面切换的阶段

否则刚度更新器可能把 $u_t$ 造成的位移错误解释为组织材料太软或太硬。

---

## 2. 在线刚度更新的具体算法

当前刚度更新仍以原框架第 5.4 节为基础，但不再直接把结果写入 XPBD projector，而是先生成 $k_{candidate}$。

**保留：**

*   $k_{dist}[i]$：逐节点 distance 刚度
*   $k_{shape}[i]$：逐节点 shape 刚度
*   $k_{volume}$：全局固定标量 $10^{10}$

刚度范围继续为：

*   $k_{dist} \in [0.20, 1.60]$
*   $k_{shape} \in [0.004, 0.020]$

### 2.1 定义预测形变和视觉修正

在每次 residual 更新中保存三个状态：

*   $q_{pred}$：物理模拟完成、视觉 residual 修正之前的节点位置
*   $q_{acc}$：视觉 residual 被接受之后的节点位置
*   $r_i$：第 $i$ 个节点的 accepted residual，即：
    $$r_i = q_{acc}[i] - q_{pred}[i]$$

节点相对于 rest shape 的预测形变为：
$$d_i = q_{pred}[i] - q_{rest}[i]$$

> [!IMPORTANT]
> 这里必须使用 residual 修正之前的 $q_{pred}$，不能使用已经修正后的 $q_{acc}$，否则无法判断物理模型原来的预测方向。

### 2.2 刚度更新门控

只有满足以下条件，节点 $i$ 才生成原始刚度信号（这部分需要测试）：

*   节点不是 fixed node
*   $\|r_i\| \geq 0.02\text{ mm}$
*   $\|d_i\| \geq 0.10\text{ mm}$
*   residual 修正前所有 incident tetra 满足 $J \geq 0.03$
*   residual 修正后所有 incident tetra 满足 $J \geq 0.03$
*   节点具有有效双目图像监督
*   节点不处于器械遮挡区域
*   节点不属于 direct anchor、support node 或其一环邻域
*   当前没有发生 capture、release 或快速 $q_7$ 运动

其中：
$$J = \frac{V}{V_0}$$
表示四面体当前体积和静止体积的比例。

如果任意门控条件失败：
$$s_i = 0$$
并且如果是四面体质量门控失败，则：
$$\text{EMA}_i = 0$$

固定节点始终：
$$\text{EMA}_i = 0$$

### 2.3 计算原始方向信号

当前 5.4 的方向信号为：
$$s_i = \text{clip}\left( - \frac{d_i \cdot r_i}{0.20\text{ mm} \cdot \|r_i\| + \varepsilon} + 0.15, -1, 1 \right) \times \text{clip}\left(\frac{\|r_i\|}{0.20\text{ mm}}, 0, 1\right) \times \text{clip}\left(\frac{\|d_i\|}{0.75\text{ mm}}, 0, 1\right)$$

其中：

*   $\varepsilon$ 用于防止 $\|r_i\|$ 接近 0 时除零；
*   $s_i > 0$ 表示倾向硬化；
*   $s_i < 0$ 表示倾向软化；
*   $s_i = 0$ 表示本轮不产生更新。

**方向判断为：**

*   $d_i$ 与 $r_i$ 反向 $\to$ 当前模拟预测的变形过大 $\to$ 需要提高刚度 $\to$ $s_i > 0$
*   $d_i$ 与 $r_i$ 同向 $\to$ 当前模拟预测的变形不足 $\to$ 需要降低刚度 $\to$ $s_i < 0$

这里的 $+0.15$、$0.20\text{ mm}$ 和 $0.75\text{ mm}$ 是当前启发式算法的尺度和偏置，后续应通过验证集校准，而不能直接解释为材料物理常数。

### 2.4 邻域平滑

在由四面体边构成的物理节点图上进行一次邻域混合。设 $\mathcal{N}(i)$ 为节点 $i$ 的物理邻居，当前使用 $\text{blend} = 0.35$：

$$\bar{s}_i = (1 - \text{blend}) s_i + \text{blend} \times \frac{1}{|\mathcal{N}(i)|}\sum_{j \in \mathcal{N}(i)} s_j$$

即：
$$\bar{s}_i = 0.65 s_i + 0.35 \times \text{mean}(s_j)$$

这一步只在物理节点图上进行，不在高分辨率视觉网格上进行。原因是材料参数属于物理节点，而不是 Gaussian 或视觉面。

### 2.5 EMA 时间平滑

使用：
$$e_{i,t} = 0.20 \bar{s}_{i,t} + 0.80 e_{i,t-1}$$

其中当前邻域信号占 20%，历史 EMA 占 80%。EMA 的作用是阻止刚度逐帧跳变。

重置时：
$$e_i = 0$$

发生以下情况时，也应清零：

*   四面体质量门控失败
*   节点进入 capture/release 过渡区
*   接触表面发生明显切换
*   residual 被拒绝

### 2.6 log-space 更新

先计算 distance 刚度的 log step：
$$\ell_i = \text{clip}(0.18 e_{i,t}, -0.18, 0.18)$$

然后：
$$k_{dist\_candidate}[i] = \text{clip}\left( k_{dist\_verified}[i] \times \exp(\ell_i), 0.20, 1.60 \right)$$

shape 刚度使用更大的步长：
$$k_{shape\_candidate}[i] = \text{clip}\left( k_{shape\_verified}[i] \times \exp(1.5\ell_i), 0.004, 0.020 \right)$$

这里的 verified 表示上一次已经通过验证并提交的刚度。单次最大变化为：

*   **distance：** log step $\pm 0.18$
*   **shape：** log step $\pm 0.27$

生成的结果叫 $k_{candidate}$，而不是立即写入 $k_{verified}$。

---

## 3. 历史状态稳定性损失

维护一个历史快照缓冲区：
$$\mathcal{H} = \{S_h\}$$

每个快照至少包含：

*   particle_q
*   particle_qd
*   器械 $\text{SE}(3)+q_7$
*   direct anchors
*   support nodes
*   $u_t$ 局部坐标和目标
*   接触状态
*   已提交刚度
*   accepted residual

只保存准静态状态：

*   器械速度低
*   $q_7$ 变化小
*   $u_t$ 目标速度低
*   没有 capture/release
*   节点速度低
*   四面体质量正常
*   residual 已接受

**对于未抓取状态：**

*   固定器械姿态
*   不施加新的器械运动

**对于抓取状态：**

*   保留 direct anchors 和 support anchors
*   固定当前 $u_t$ 目标
*   不让锚点继续移动

然后对候选刚度执行短时间 XPBD 松弛：
$$q_h^{relaxed} = \text{Relax}\left( q_h^{accepted}, \text{固定接触/抓取状态}, k_{candidate} \right)$$

历史稳定性损失定义为：
$$L_{hist}(k_{candidate}) = \frac{1}{|\mathcal{H}|} \sum_h \|q_h^{accepted} - q_h^{relaxed}\|_W$$

$W$ 可以使用质量权重，也可以只统计非固定节点。如果 $L_{hist}(k_{candidate})$ 明显大于旧刚度下的历史损失，则说明候选刚度无法稳定保持过去已经对齐的状态，应拒绝。

---

## 4. 下一步 prediction gap 验证

在时刻 $t$ 生成 $k_{candidate}$ 后，保存完整快照 $S_t$，然后建立两个影子 rollout：

*   **baseline：** $S_t + k_{verified}$
*   **candidate：** $S_t + k_{candidate}$

两条 rollout 使用相同的：

*   $\text{SE}(3)+q_7$
*   接触参数
*   top barrier
*   $u_t$ 绑定
*   direct anchors
*   support nodes
*   初始 $q$、$qd$
*   相机参数
*   图像 mask

并关闭：

*   visual residual
*   visual force
*   在线刚度更新
*   新的抓取捕获判定

下一帧真实观测到达后，计算：
$$D_{old} = D_{vis}(\text{Render}(q_{old}), I_{t+1})$$
$$D_{candidate} = D_{vis}(\text{Render}(q_{candidate}), I_{t+1})$$

候选刚度只有在以下条件同时满足时才提交：

1.  $D_{candidate} < D_{old} - \text{margin}$
2.  $L_{hist}(\text{candidate})$ 不明显变差
3.  没有新增四面体翻转
4.  最小 $J$ 不明显下降
5.  接触穿透不恶化
6.  candidate 与 verified 的刚度变化未超过上限

*   **提交：** $k_{verified} \leftarrow k_{candidate}$
*   **拒绝：** 保持 $k_{verified}$，丢弃 $k_{candidate}$，EMA 保留或衰减，记录拒绝原因

> [!NOTE]
> 这里的“下一帧”只是验证候选刚度是否有预测价值。候选本身是在当前帧根据当前 residual 和历史状态生成的。

---

## 5. 多帧 prediction gap

除了下一帧验证，还应对已经提交的刚度进行开放环评估。从某一时刻的已接受状态开始：

*   固定 particle_q
*   固定 $k_{verified}$
*   读取后续器械轨迹
*   关闭 visual residual
*   关闭 visual force
*   关闭在线刚度更新

向前 rollout：$H = 1, 3, 5, 10$ 个视频帧。定义：
$$\text{Gap}(t, h) = D_{vis}(\text{Render}(\hat{q}_{t+h}), I_{t+h})$$

同时记录：

*   旧刚度的 Gap
*   新刚度的 Gap

未来预测阶段还要保留 $u_t$ 状态：

1.  **材料隔离评估：** 回放真实运行中记录的 direct anchors、support nodes 和 $u_t$ 目标；
2.  **端到端评估：** 让接触/抓取状态机自行运行。

材料更新的主要判断使用第一种，因为它可以避免把抓取状态机差异混入材料参数评价。

---

## 6. 四类指标分开记录

### 图像对齐指标

记录：

*   left loss before/after
*   right loss before/after
*   双目平均 loss
*   accepted/rejected
*   residual max
*   residual RMS
*   有效像素数量
*   mask 覆盖率

> **回答：** 当前帧图像是否对齐得更好？

### 物理可行性指标

记录：

*   最小 $J$
*   新翻转数量
*   接触穿透深度
*   距离约束能量
*   体积约束能量
*   形状匹配能量
*   line-search 回溯次数
*   局部冻结节点数
*   $u_t$ 锚点误差

> **回答：** 物理状态是否健康、约束是否满足？由于没有真实 3D 组织状态作为 ground truth，不能把它们直接称为“真实物理状态误差”。

### 材料参数指标

记录：

*   candidate 数量
*   committed 数量
*   rejected 数量
*   拒绝原因
*   $k_{dist}$ min/median/max
*   $k_{shape}$ min/median/max
*   active signal 节点数
*   正向 log step 节点数
*   负向 log step 节点数
*   EMA 有效节点数
*   $L_{hist}$
*   刚度空间平滑度

> **回答：** 刚度是否在稳定、有限和可解释地变化？

### 未来预测指标

记录：

*   $H=1$ prediction gap
*   $H=3$ prediction gap
*   $H=5$ prediction gap
*   $H=10$ prediction gap
*   old-k 与 candidate-k 的差值
*   开放环 residual RMS

> **回答：** 刚度变化是否真的改善未来预测？

所有指标还应按动作阶段分组：**压下**、**闭合**、**捕获**、**抬起**、**放下**、**松开**。

---

## 7. 修改后的完整运行时序

每个物理帧执行以下步骤：

1. 读取当前器械 $\text{SE}(3)+q_7$
2. 运行 12 个物理子步：
   * 普通 jaw 接触
   * oriented top barrier
   * 距离/体积/形状 XPBD
   * $u_t$ direct anchors
   * 一层有限体积传播
   * wrist-frame support
   * 局部材料松弛
   * $J$ 与翻转检查
3. 由物理节点更新视觉网格
4. 由视觉网格面中心更新 Gaussian mean
5. 每隔 3 个物理帧：
   * 渲染左右目 RGB
   * 计算当前视觉 loss
   * 优化节点 residual $r$
   * 检查 residual 是否接受
6. residual 被接受时：
   * 写入 particle_q
   * 计算 $d_i$ 和 $r_i$
   * 屏蔽 $u_t$ 直接控制区域
   * 计算 $s_i$
   * 邻域混合
   * EMA
   * 生成 $k_{candidate}$
   * 计算 $L_{hist}$
   * 保存完整快照
7. 下一帧观测到达：
   * 用 $k_{verified}$ 做 baseline shadow rollout
   * 用 $k_{candidate}$ 做 candidate shadow rollout
   * 比较 prediction gap
   * 检查物理健康和历史损失
8. **通过：** $k_{verified} \leftarrow k_{candidate}$
9. **拒绝：** 保持 $k_{verified}$，丢弃 $k_{candidate}$，记录拒绝原因
10. 继续下一物理帧

---

## 8. 2026-08-14 实现状态

本方案要求的功能代码现已完成：

> [!NOTE]
> 本文第 2 节保留的是恢复后的旧参数文字；当前运行代码继续采用后续调参确定的
> distance `0.10..2.00`、shape `0.003..0.020`、新证据/历史 `0.30/0.70`，以及归一化
> 夹角乘 residual/deformation 幅值的方向信号。实现状态按当前代码判断，不回滚这些更新。

* 第 1 节 `u_t` direct/support/一环节点同时禁止作为刚度信号源和更新目标；
* 第 2 节 candidate/verified、局部质量和视觉监督门、空间平滑、EMA、log-space 更新已接线；
* 第 3 节最多 4 个准静态快照的 baseline/candidate 历史松弛门已接线；
* 第 4 节下一可用新观测的双影子提交/拒绝门已接线；
* 第 5 节 `H=1/3/5/10` 已对每个 committed candidate 实现 old/new 多帧开放环；
* 第 5 节的材料隔离和端到端两种协议均已实现；
* 第 6 节图像、物理、材料、预测指标写入 append-only JSONL，并按压下、闭合、捕获、抬起、
  放下、松开生成 JSON 汇总；
* 第 7 节完整在线时序已接线，评测影子结束后恢复 live 状态。
* 抓取激活后的 visual residual 额外排除 direct/support 及严格一层 incident-tet 邻域；排除
  只扩张一次，不形成第二圈，用于避免 residual 与固定接触目标反复拉扯。

> [!NOTE]
> 2026-08-14 将向下 top-barrier 的直接物理节点核心收紧到约 `2.5 mm`；材料投影速度回灌
> 当前按用户调参设为 `0.14`。这是接触局部性/惯性设置；本方案的刚度信号、上下限、
> EMA、candidate/verified、历史门、下一帧门和多 horizon 验收均未修改。

评测功能默认关闭，避免普通 GUI 承担四个 horizon、两种协议和 old/new 共 16 条影子及额外
residual solve 的开销。使用：

```bash
bash scripts/run_super_stiffness_evaluation.sh
```

或给普通入口显式增加：

```text
--stiffness-evaluation-output <新目录>
--stiffness-evaluation-horizons 1,3,5,10
```

输出为 `metadata.json`、`events.jsonl` 和 `summary.json`。功能实现完成不等于实验结论已经
产生：仍需在有 CUDA/显示服务的机器上跑完真实轨迹，才能用输出数据判断 candidate 是否在
各动作阶段和各 horizon 上稳定优于 old stiffness。
