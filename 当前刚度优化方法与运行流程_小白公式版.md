# 当前刚度优化方法与运行流程：真正面向小白的公式版

本文解释的是当前仓库实际运行的
`paper_pbd + visual residual + online stiffness`，不是只把代码里的公式重新抄一遍。
每个重要公式都按下面四个问题来讲：

1. 每个符号代表什么；
2. 这条公式在解决什么实际问题；
3. 带入一组简单数字后怎么算；
4. 参数变大或变小，GUI 里会看到什么。

第一次阅读时不需要记住所有公式。先理解下面这句话：

> 物理模拟先猜组织会怎样动，双目图像再指出“猜错了多少”；系统利用这个错误判断局部是
> 太软还是太硬，但新刚度必须通过历史状态和下一张真实图像的双重考试，才能正式生效。

---

## 0. 先确定：刚度优化方案完成了吗

### 0.1 严格结论

以当前 402 行的 `刚度优化方案.md` 为验收基准，结论分两层：

> **方案要求的在线方法、多帧评测和阶段统计代码都已经完成；尚未在当前机器上跑出完整
> CUDA 真实轨迹报告。**

不能把这两层混成一个“完成/没完成”，因为它们回答的问题不同：

- 在线方法回答：“程序能不能安全地提出、验证、提交或拒绝一个刚度修改？”
- 离线评测回答：“大量完整轨迹的数据能不能证明新方法长期优于旧方法？”

### 0.2 逐节验收

| 方案章节 | 状态 | 实际代码做到的事情 |
|---|---|---|
| 第 1 节：`u_t` 与刚度解耦 | 已完成 | direct、support 及一环节点不产生材料信号，也不被更新 |
| 第 2 节：在线刚度候选 | 已完成，参数采用后续调参版本 | 质量/视觉门、邻域平滑、EMA、log-space、有界 candidate 都已运行 |
| 第 3 节：历史稳定性 | 已完成初版 | 最多 4 个准静态快照，各做 1 个物理帧的 baseline/candidate 松弛 |
| 第 4 节：多步 prediction gap | 已完成 | H=5 真实 residual/抓取闭环、四倍率影子和固定滞后状态接回 |
| 第 5 节：`H=1/3/5/10` 离线开放环 | 已实现 | 每个 committed candidate 精确跑四个 horizon，old/new 各有材料隔离与端到端协议 |
| 第 6 节：完整指标和动作阶段统计 | 已实现 | JSONL 逐事件记录，JSON 按六个动作阶段聚合 count/mean/min/max |
| 第 7 节：在线运行时序 | 已完成 | 物理、residual、candidate、历史门、影子门、commit/reject 已接线 |

因此：

- 如果你问“现在能不能打开 GUI 使用在线刚度优化”，答案是**可以**；
- 如果你问“方案正文要求的代码是否全部交付”，答案是**已经完成**；
- 如果你问“核心刚度优化算法是否已经写完”，答案是**已经写完并有回归测试**。
- 如果你问“是否已有完整 CUDA 数值证明”，答案是**尚未实际跑完评测轨迹**。

### 0.3 恢复后的方案有三处旧数值，不等于当前代码缺失

你恢复的方案保留了较早的参数。后续根据实际观察，代码已经更新：

| 项目 | 恢复后方案旧值 | 当前代码启动值 | 原因 |
|---|---:|---:|---|
| distance bounds | `0.20..1.60` | `0.10..2.00` | 允许不同区域形成更大的软硬差异 |
| shape bounds | `0.004..0.020` | `0.003..0.020` | Reset 值 `0.004` 以下仍留软化空间 |
| EMA 新证据/历史 | `0.20/0.80` | `0.30/0.70` | 让实时修正更快显现 |
| 方向信号 | 旧“投影长度”写法 | 归一化夹角 × 两个幅值因子 | 方向和位移大小分开，量纲更清楚 |

GUI 还能把 bounds 调到更宽的实验范围，但“滑条能调到”不等于“推荐这样设置”。

---

## 1. 先建立一幅脑内图：程序里有三个不同世界

### 1.1 物理世界：少量四面体节点

组织内部由物理粒子和四面体组成：

```text
       x2
       /\
      /  \
     /____\
   x0------x1
      \    /
       \  /
        x3
```

四个角点组成一个四面体。很多四面体共享角点，拼成整块组织。

物理世界负责：

- 质量和速度；
- 重力、夹爪接触；
- 拉伸、体积和形状约束；
- 抓取后随夹爪运动。

### 1.2 视觉世界：更密的表面和 Gaussian

物理网格比较粗，不适合直接渲染细致表面。因此还有：

```text
物理粒子 x  ──蒙皮──> 细密视觉三角面 ──绑定──> 组织 Gaussian ──渲染──> 左右目图像
```

Gaussian 只是“怎么把组织画出来”的表面表示，不是独立材料节点。材料刚度属于物理粒子，
不能直接给每个像素或每个 Gaussian 随便设一份刚度。

### 1.3 参数世界：每个物理节点的两种局部刚度

每个物理节点 \(i\) 有：

- \(k_{\mathrm{dist},i}\)：抵抗边长变化；
- \(k_{\mathrm{shape},i}\)：抵抗局部剪切和歪斜。

另外有一个全局固定值：

- \(k_{\mathrm{vol}}=10^{10}\)：强力保持四面体体积。

可以把它们想成：

```text
distance：四面体边上的橡皮筋
volume：四面体内部不能随便漏气的气囊
shape：让四面体尽量保持原来形状的软骨架
```

---

## 2. 阅读公式前必须知道的符号和单位

### 2.1 下标

| 符号 | 含义 |
|---|---|
| \(i,j\) | 第 \(i\)、第 \(j\) 个物理粒子 |
| \(e\) | 一条物理边 |
| \(T\) | 一个四面体 |
| \(t\) | 当前视觉更新时刻 |
| \(h\) | 一个历史快照或未来预测步长 |
| \(c\) | 一台相机，例如左目或右目 |
| \(p\) | 一个图像像素 |

### 2.2 位置

| 符号 | 含义 |
|---|---|
| \(x_i^0\) 或 \(q_{\mathrm{rest}}[i]\) | 没有受力时的静止位置 |
| \(x_i^{\mathrm{pred}}\) 或 \(q_{\mathrm{pred}}[i]\) | 纯物理算完、视觉修正之前的位置 |
| \(x_i^{\mathrm{acc}}\) 或 \(q_{\mathrm{acc}}[i]\) | 视觉 residual 验收通过后的位置 |
| \(r_i\) | 图像要求补上的小位移 |

所有物理位置在代码里使用米。文档为了直观经常换算成毫米：

\[
1\ {\rm mm}=10^{-3}\ {\rm m}.
\]

所以 `0.00020 m` 就是 `0.20 mm`。

### 2.3 向量的三个常用运算

三维向量写成：

\[
a=(a_x,a_y,a_z).
\]

长度是：

\[
\|a\|=\sqrt{a_x^2+a_y^2+a_z^2}.
\]

例如 \(a=(0.3,0.4,0)\ {\rm mm}\)：

\[
\|a\|=\sqrt{0.3^2+0.4^2}=0.5\ {\rm mm}.
\]

点积是：

\[
a\cdot b=a_xb_x+a_yb_y+a_zb_z.
\]

点积和夹角的关系是：

\[
\frac{a\cdot b}{\|a\|\|b\|}=\cos\theta.
\]

- \(\cos\theta=1\)：完全同向；
- \(\cos\theta=0\)：互相垂直；
- \(\cos\theta=-1\)：完全反向。

这会直接用于判断“太软还是太硬”。

---

## 3. XPBD 是什么：每个子步都把变形往合理状态拉回去

### 3.1 约束 \(C(x)=0\) 是什么

约束就是“理想状态下应该等于零的错误量”。

例如一条边原长 \(L^0=5.0\ {\rm mm}\)，现在长 \(5.5\ {\rm mm}\)，那么：

\[
C_{\mathrm{dist}}=5.5-5.0=0.5\ {\rm mm}.
\]

`C>0` 表示拉长，`C<0` 表示缩短，`C=0` 表示刚好。

### 3.2 XPBD 的核心公式

对任意一条约束，XPBD 计算：

\[
\Delta\lambda=
\frac{-C-\widetilde\alpha\lambda}
{\sum_i w_i\|\nabla_iC\|^2+\widetilde\alpha},
\]

\[
\widetilde\alpha=
\frac{s_{\mathrm{comp}}}{k\,\Delta t_{\mathrm{sub}}^2},
\]

\[
\Delta x_i=w_i\Delta\lambda\nabla_iC.
\]

逐个解释：

- \(C\)：当前违反约束多少；
- \(\lambda\)：这条约束在当前求解过程里累计了多少“纠正力”；
- \(w_i=1/m_i\)：逆质量；越轻的节点越容易移动；
- \(\nabla_iC\)：移动节点 \(i\) 最能减小错误的方向；
- \(k\)：刚度；
- \(\Delta t_{\mathrm{sub}}\)：一个物理子步的时间；
- \(s_{\mathrm{comp}}\)：全局柔顺缩放；
- \(\Delta x_i\)：这次实际修正节点多少。

### 3.3 为什么 \(k\) 越大越硬

关键只看：

\[
\widetilde\alpha\propto\frac1k.
\]

刚度扩大 10 倍：

\[
k\rightarrow10k
\quad\Rightarrow\quad
\widetilde\alpha\rightarrow\frac{\widetilde\alpha}{10}.
\]

\(\widetilde\alpha\) 越小，分母里的“让步项”越小，求解器越强烈地消除 \(C\)，所以材料更硬。

当前物理是 60 Hz、每帧 12 个子步：

\[
\Delta t_{\mathrm{sub}}
=\frac{1}{60\times12}
\approx0.001389\ {\rm s}.
\]

只比较相对量，若 \(s_{\mathrm{comp}}=1\)：

\[
\widetilde\alpha(k=0.2)
=\frac1{0.2\Delta t_{\mathrm{sub}}^2},
\]

\[
\widetilde\alpha(k=2.0)
=\frac1{2.0\Delta t_{\mathrm{sub}}^2}
=0.1\,\widetilde\alpha(k=0.2).
\]

所以 `distance=2.0` 比 `distance=0.2` 更强地恢复边长。这里的数值是 XPBD 数值刚度，
不是 Pa 单位的杨氏模量。

---

## 4. 三种物理约束到底各管什么

### 4.1 Distance：边长橡皮筋

物理边 \(e=(i,j)\) 的当前长度：

\[
L_{ij}=\|x_i-x_j\|.
\]

静止长度：

\[
L_{ij}^0=\|x_i^0-x_j^0\|.
\]

distance 约束：

\[
C_{ij}^{\mathrm{dist}}=L_{ij}-L_{ij}^0.
\]

一条边使用两个端点刚度的平均值：

\[
k_e=\frac{k_{\mathrm{dist},i}+k_{\mathrm{dist},j}}2.
\]

### 数字例子

假设：

```text
静止长度      5.00 mm
当前长度      5.50 mm
C_dist       +0.50 mm
节点 i 刚度   0.20
节点 j 刚度   0.60
边刚度        (0.20+0.60)/2 = 0.40
```

这条边会尝试缩回去。若把两个节点刚度都调高，恢复更强；调低则允许它被拉得更长。

### GUI 中的肉眼效果

- \(k_{\mathrm{dist}}\) 太大：夹爪附近的拉动容易沿边网络传到远处，整片组织一起被拖动；
- \(k_{\mathrm{dist}}\) 太小：夹爪附近容易被拉长，局部可能像松皮或面条；
- 你之前看到影响传播到 1 cm 以外，distance 太高或材料 passes 太多都可能是原因。

### 4.2 Volume：密封气囊

四面体有向体积：

\[
V=
\frac{
((x_1-x_0)\times(x_2-x_0))\cdot(x_3-x_0)
}{6}.
\]

静止体积为 \(V_0\)，volume 约束：

\[
C^{\mathrm{vol}}=V-V_0.
\]

代码也经常看体积比：

\[
J=\frac{V}{V_0}.
\]

### \(J\) 的直观意义

| \(J\) | 含义 |
|---:|---|
| \(1.0\) | 体积与静止状态一致 |
| \(0.8\) | 还剩 80% 体积 |
| \(0.03\) | 已被压到只剩 3%，非常危险 |
| \(0\) | 四面体完全压扁 |
| \(<0\) | 四面体翻转，几何方向反了 |
| \(1.2\) | 比静止体积膨胀 20% |

### 为什么 volume 固定为 \(10^{10}\)

当前不在线学习 volume：

\[
k_{\mathrm{vol}}=10^{10}.
\]

原因是“柔软”和“可以凭空压瘪”不是一回事。若通过降低 volume 刚度制造柔软，组织很可能：

- 被夹爪压成零厚度；
- 发生四面体翻转；
- 接触后在别处补偿性膨胀；
- Gaussian 表面出现异常鼓包。

因此当前只学习 distance 和 shape，体积保护固定。

### 4.3 Shape：四面体的软骨架

先构造当前形状矩阵：

\[
D_s=
\begin{bmatrix}
x_1-x_0 & x_2-x_0 & x_3-x_0
\end{bmatrix},
\]

静止形状矩阵：

\[
D_m=
\begin{bmatrix}
x_1^0-x_0^0 & x_2^0-x_0^0 & x_3^0-x_0^0
\end{bmatrix}.
\]

形变梯度：

\[
F=D_sD_m^{-1}.
\]

如果只是整体平移，\(F=I\)；如果只是刚体旋转，\(F\) 是旋转矩阵；拉伸、压缩和剪切都会改变
\(F\) 的形状。

代码通过 SVD：

\[
F=U\Sigma V^T
\]

提取最接近的纯旋转：

\[
R=UV^T.
\]

然后计算每个角点偏离“只旋转、不变形”目标的距离：

\[
g_a=c+Rq_a,
\]

\[
C^{\mathrm{shape}}
=\sqrt{\sum_{a=0}^{3}\|x_a-g_a\|^2}.
\]

其中：

- \(c\)：当前四面体中心；
- \(q_a\)：静止时角点相对中心的位置；
- \(g_a\)：如果它只发生刚体旋转，角点应该在哪里。

### 形象理解

拿一个海绵骰子：

- 整个骰子拿起来平移：shape 不反对；
- 整个骰子转 30°：shape 不反对；
- 把骰子剪成平行四边形：shape 会把它拉回；
- 把一个角扭歪：shape 会把角拉回。

### GUI 中的肉眼效果

- \(k_{\mathrm{shape}}\) 太大：局部剪切困难，夹爪位移容易带动更大区域，弯曲显得僵；
- \(k_{\mathrm{shape}}\) 太小：局部容易歪斜，粒子可能相对整体偏离，表面出现尖角或跳点；
- shape 不是单纯“硬度总开关”，它更像“局部形状秩序”。

---

## 5. 双目图像怎样告诉物理“你猜错了”

### 5.1 每个像素先计算颜色误差

对像素颜色差 \(z=I^{\mathrm{sim}}-I^{\mathrm{obs}}\)，使用 Smooth-L1：

\[
\rho_\beta(z)=
\begin{cases}
\dfrac{z^2}{2\beta}, & |z|<\beta,\\[6pt]
|z|-\dfrac{\beta}{2}, & |z|\ge\beta.
\end{cases}
\]

当前 \(\beta=0.05\)，颜色通常已归一化到 0–1。

为什么不用普通平方误差？

- 小误差仍然细致优化；
- 大误差近似线性增长；
- 少数反光、遮挡或标注错误像素不容易统治整个优化。

### 数字例子

当 \(\beta=0.05\)：

\[
z=0.02<0.05
\Rightarrow
\rho=\frac{0.02^2}{2\times0.05}=0.004.
\]

\[
z=0.20>0.05
\Rightarrow
\rho=0.20-0.025=0.175.
\]

若使用纯平方，极端误差的梯度会增长得更快；Smooth-L1 更稳。

### 5.2 mask 权重 \(W_c(p)\)

并不是每个像素都同样可信。每台相机的损失：

\[
D_c=
\frac{
\sum_pW_c(p)\,
\rho_\beta(I_c^{\mathrm{sim}}(p)-I_c^{\mathrm{obs}}(p))
}{
\sum_pW_c(p)
}.
\]

\(W_c(p)\) 越接近 0，这个像素越被忽略。当前会降低：

- 组织 mask 外；
- 器械遮挡附近；
- 图像边缘；
- 组织轮廓不可靠区域；
- 离夹爪过远、当前不需要强监督的区域。

### 5.3 左右目等权，而不是“大 mask 的相机说了算”

总视觉损失：

\[
D_{\mathrm{vis}}=
\frac1{|\mathcal C|}
\sum_{c\in\mathcal C}D_c.
\]

若：

```text
左目 loss = 0.02，有效像素 10000
右目 loss = 0.06，有效像素  2000
```

则：

\[
D_{\mathrm{vis}}=\frac{0.02+0.06}{2}=0.04.
\]

不会因为左目像素更多就把右目淹没。双目都必须基本安全。

---

## 6. Visual residual：先修正“当前状态”，不是直接改刚度

### 6.1 residual 的定义

物理先给出：

\[
x_i^{\mathrm{pred}}.
\]

图像优化给出小位移：

\[
r_i.
\]

候选位置：

\[
x_i^{\mathrm{cand}}=x_i^{\mathrm{pred}}+r_i.
\]

验收通过后：

\[
x_i^{\mathrm{acc}}=x_i^{\mathrm{pred}}+r_i.
\]

它像相机对物理状态说：

> “你预测的位置差一点，请向这个方向补 0.1 mm。”

它并没有直接说材料刚度是多少。

### 6.2 抓住以后，为什么要给 residual 划一个很小的禁区

夹爪激活后，direct anchors 和 support nodes 已经由夹爪目标 (u_t) 控制。如果视觉 residual
还去移动同一批节点，就会出现“图像拉走一次、夹爪下一子步又拉回来”的反复争抢。现在先定义
抓取控制节点集合：

\[
\mathcal C_0=\{i\mid i\text{ 是当前 direct anchor 或实际 support node}\}.
\]

再找出所有直接包含这些节点的四面体：

\[
\mathcal T_1=\{T\mid T\cap\mathcal C_0\ne\varnothing\}.
\]

最终的小范围排除集合为：

\[
\mathcal C_1=\mathcal C_0\cup\bigcup_{T\in\mathcal T_1}T.
\]

对其中每个节点同时规定：

\[
r_i=0,\qquad \frac{\partial D_{\mathrm{vis}}}{\partial r_i}=0,
\qquad i\in\mathcal C_1.
\]

通俗地说，就是四个直接抓取点、当前真正被启用的 support 点，以及与它们直接共用一个
四面体的邻居不接受图像位置修正。这里**只从原始控制节点扩张一次**；新加入的邻居不会继续
作为种子，所以绝不会扩成第二圈或传遍整块组织。抓取释放后，动态禁区自动清空。

这样做没有关闭外围组织的视觉修正：禁区外的粒子仍照常从双目图像获得 residual。GUI 的
`Visual residual diagnostics` 会显示 `grip excluded` 数量，方便确认范围是否过大。

### 6.3 Gaussian 如何跟着物理节点动

一个表面 Gaussian 由附近物理节点加权绑定。近似平移写成：

\[
\mu_g^{\mathrm{cand}}
=
\mu_g^{\mathrm{pred}}
+
\sum_{i\in\mathcal S_g}w_{gi}r_i,
\]

\[
\sum_iw_{gi}=1.
\]

例如三个支持节点权重分别为 \(0.2,0.3,0.5\)，residual 的 x 分量分别为
\(0.1,0.2,0.0\ {\rm mm}\)：

\[
\Delta\mu_{g,x}
=0.2\times0.1+0.3\times0.2+0.5\times0
=0.08\ {\rm mm}.
\]

优化内部用这个近似快速求梯度；最终验收会调用正式蒙皮，重新计算 Gaussian 的 mean、rotation
和 scale。

### 6.4 residual 总损失

当前优化目标：

\[
\begin{aligned}
L={}&D_{\mathrm{vis}}
+0.4L_{\mathrm{dist}}
+1.0L_{\mathrm{vol}}
+0.008L_{\mathrm{shape}}\\
&+0.05L_{\mathrm{spatial}}
+0.10L_{\mathrm{temporal}}
+0.01L_{\mathrm{mag}}.
\end{aligned}
\]

这不是七个互不相关的神秘数字。它们分别阻止不同的作弊方式。

### \(L_{\mathrm{dist}}\)：别为了图像随便拉长边

\[
L_{\mathrm{dist}}
=\operatorname{mean}_e
\left(
\frac{L_e-L_e^0}{L_e^0}
\right)^2.
\]

例：原长 5 mm，变成 5.5 mm：

\[
\left(\frac{5.5-5.0}{5.0}\right)^2
=0.1^2=0.01.
\]

### \(L_{\mathrm{vol}}\)：别压瘪或吹大四面体

\[
L_{\mathrm{vol}}
=\operatorname{mean}_T(J_T-1)^2.
\]

例：

- \(J=0.9\)：损失 \((0.9-1)^2=0.01\)；
- \(J=0.5\)：损失 \(0.25\)；
- 离 1 越远，惩罚越大。

### \(L_{\mathrm{shape}}\)：别用剪切歪斜骗图像

视觉优化里的 shape 指标使用：

\[
C=F^TF,
\]

\[
L_{\mathrm{shape}}
=\operatorname{mean}(C-I)^2.
\]

\(F^TF\) 会消掉纯旋转，所以刚体转动几乎不受罚；拉伸和剪切会让它偏离单位矩阵 \(I\)。

### \(L_{\mathrm{spatial}}\)：相邻 residual 不要一个上、一个下

\[
L_{\mathrm{spatial}}
=
\frac{
\operatorname{mean}_{(i,j)\in E}\|r_i-r_j\|^2
}{r_{\max}^2}.
\]

若相邻两个节点分别修正 \(+0.5\) mm 和 \(-0.5\) mm，它们相差 1 mm，惩罚很大。这一项专门
抑制“椒盐跳点”。

### \(L_{\mathrm{temporal}}\)：这一帧别和上一帧突然反着跑

\[
L_{\mathrm{temporal}}
=
\frac{
\operatorname{mean}_i\|r_{i,t}-r_{i,t-1}\|^2
}{r_{\max}^2}.
\]

注意：每次优化仍从 \(r=0\) 开始，上一轮 residual 只作为正则参考，不会被再次叠加。

### \(L_{\mathrm{mag}}\)：能少动就少动

\[
L_{\mathrm{mag}}
=
\frac{\operatorname{mean}_i\|r_i\|^2}{r_{\max}^2}.
\]

如果两种位移都能让图像变好，系统偏向位移较小的那种。

### 6.5 当前优化步数和上限

默认：

```text
Adam iterations        8
learning rate          0.040 mm
maximum residual       0.60 mm / node / solve
image scale            0.25
minimum volume ratio   0.30（对原本健康的 tetra）
```

最大 residual 是安全帽，不是每次都会移动 0.60 mm。

### 6.6 residual 的三层安全检查

1. 优化中每步限制 \(\|r_i\|\le r_{\max}\)，fixed node 始终为 0；
2. 对危险四面体先冻结相关节点，再最多 10 次把 candidate 整体乘 0.5；
3. 正式 Gaussian 重蒙皮后重新渲染验收。

最终至少要求：

\[
D_{\mathrm{vis}}^{\mathrm{exact,new}}
<
D_{\mathrm{vis}}^{\mathrm{old}},
\]

任一相机不能退化超过：

\[
\max(10^{-6},0.005D_c^{\mathrm{old}}),
\]

并且不能新增翻转四面体。

通过后只写位置 `particle_q`，不写速度 `particle_qd`。因此这是状态校正，不是给粒子一拳。

---

## 7. 最关键部分：怎样从 residual 判断太软还是太硬

### 7.1 两支箭头

第一支箭头是物理预测相对静止位置的形变：

\[
d_i=x_i^{\mathrm{pred}}-x_i^0.
\]

第二支箭头是图像要求的补偿：

\[
r_i=x_i^{\mathrm{acc}}-x_i^{\mathrm{pred}}.
\]

画成一条线：

### 情况 A：物理变形过头

```text
rest x0  -------------------->  x_pred
          d：物理向右变形
                         <----  r：图像要求往回拉
```

\(d\) 和 \(r\) 反向。图像认为物理走太远，倾向说明局部太软，需要硬化。

### 情况 B：物理变形不够

```text
rest x0  ------------>  x_pred  ----> 图像认为还应继续
          d：向右                  r：也向右
```

\(d\) 和 \(r\) 同向。图像认为物理还没走够，倾向说明局部太硬，需要软化。

### 7.2 哪些节点根本不允许参与判断

只有同时满足下面条件，节点才提供材料证据：

```text
不是 fixed
|r| >= 0.02 mm
|d| >= 0.10 mm
residual 前后的 incident tetra 都有 J >= 0.03
左右目 mask 和渲染梯度真的监督到该节点
不属于 direct/support u_t
不属于 direct/support 的物理一环
当前没有快速 q7、capture/release、过深穿透或接触片突变
```

为什么要门控？因为 0.001 mm 的噪声也有方向，但它不代表材料。

### 7.3 当前代码实际使用的方向项

先计算夹角余弦：

\[
c_i=
\frac{d_i\cdot r_i}
{\max(\|d_i\|\|r_i\|,\varepsilon)}.
\]

再计算方向分数：

\[
g_i=
\operatorname{clip}(-c_i+b,-1,1),
\qquad b=0.15.
\]

三个典型方向：

| 关系 | \(c_i=\cos\theta\) | \(g_i=\operatorname{clip}(-c_i+0.15)\) | 倾向 |
|---|---:|---:|---|
| 完全反向 | \(-1\) | \(1\)（1.15 被截到 1） | 硬化 |
| 垂直 | \(0\) | \(0.15\) | 轻微硬化偏置 |
| 完全同向 | \(1\) | \(-0.85\) | 软化 |

\(b=0.15\) 不是材料常数，只是在方向含糊时略偏向防止过软。

### 7.4 位移太小时，方向再漂亮也不应更新

residual 强度：

\[
a_{r,i}
=
\operatorname{clip}
\left(
\frac{\|r_i\|}{R_{\mathrm{full}}},0,1
\right),
\qquad
R_{\mathrm{full}}=0.20\ {\rm mm}.
\]

形变强度：

\[
a_{d,i}
=
\operatorname{clip}
\left(
\frac{\|d_i\|}{D_{\mathrm{full}}},0,1
\right),
\qquad
D_{\mathrm{full}}=0.75\ {\rm mm}.
\]

最终原始信号：

\[
s_i=g_i\,a_{r,i}\,a_{d,i}.
\]

### 完整数字例子 1：反向，应该硬化

假设：

```text
|d| = 0.30 mm
|r| = 0.10 mm
d 与 r 完全反向
```

方向项：

\[
g=1.
\]

residual 强度：

\[
a_r=\frac{0.10}{0.20}=0.5.
\]

形变强度：

\[
a_d=\frac{0.30}{0.75}=0.4.
\]

所以：

\[
s=1\times0.5\times0.4=0.20.
\]

这是一个中等偏小的硬化证据，而不是直接把刚度增加 20%。

### 完整数字例子 2：同向，应该软化

保持位移大小不变，但 \(d,r\) 完全同向：

\[
g=-1+0.15=-0.85.
\]

\[
s=-0.85\times0.5\times0.4=-0.17.
\]

负号表示软化。

### 完整数字例子 3：residual 太小

若 \(\|r\|=0.01\ {\rm mm}\)，低于最小门 \(0.02\ {\rm mm}\)，直接：

\[
s=0.
\]

即使方向完全反向，也不会用噪声硬化。

### 7.5 为什么恢复后方案中的旧公式不是当前实际公式

恢复后方案写的是：

\[
-\frac{d_i\cdot r_i}
{0.20{\rm mm}\,\|r_i\|+\varepsilon}.
\]

忽略 \(\varepsilon\) 后，它等于：

\[
-\frac{\|d_i\|\cos\theta}{0.20{\rm mm}}.
\]

这会把“方向”和“形变大小”提前混在一起，后面又乘一次形变强度。当前代码改为：

\[
-\cos\theta
\quad\times\quad
\text{residual 强度}
\quad\times\quad
\text{形变强度}.
\]

这样每一项都容易解释、没有混乱量纲。本文后续全部按当前代码讲。

---

## 8. 为什么一个节点的证据不能直接改刚度

### 8.1 空间平滑：问一下物理邻居

在物理边图上：

\[
\bar s_i=
(1-\beta)s_i
+
\beta
\frac1{|\mathcal N(i)|}
\sum_{j\in\mathcal N(i)}s_j,
\qquad\beta=0.35.
\]

也就是：

\[
\bar s_i=0.65s_i+0.35\,\operatorname{mean}(s_j).
\]

### 数字例子

当前节点：

\[
s_i=0.60.
\]

三个邻居：

\[
s_j=(0.20,0.40,0.00),
\]

邻居均值：

\[
\operatorname{mean}(s_j)=0.20.
\]

平滑后：

\[
\bar s_i=0.65\times0.60+0.35\times0.20=0.46.
\]

孤立的 0.60 被降到 0.46，减少单粒子突变；如果邻居也都接近 0.60，结果仍接近 0.60。

### 参数变大变小

- `Neighbor smoothing blend` 变大：材料区域更连续，但局部差异会被抹平、影响传播更远；
- 变小：更局部，但更容易出现一个节点特别软或特别硬；
- `Spatial smoothing passes` 增多：相当于消息在图上传更多圈，范围更广。

`u_t` 排除节点在平滑前后都会再次置零，不能由邻居把信号漏回来。

### 8.2 时间 EMA：这一帧只占 30% 发言权

当前代码：

\[
e_{i,t}=0.30\bar s_{i,t}+0.70e_{i,t-1}.
\]

### 从零开始的数字例子

假设连续三次都有 \(\bar s=0.60\)，初始 \(e_0=0\)：

\[
e_1=0.3\times0.6+0.7\times0=0.18,
\]

\[
e_2=0.3\times0.6+0.7\times0.18=0.306,
\]

\[
e_3=0.3\times0.6+0.7\times0.306=0.3942.
\]

它逐渐接近 0.60，不会第一帧就跳到 0.60。

### GUI 参数含义

GUI 显示 `EMA new-evidence weight`，也就是公式中的 0.30：

- 调大：响应更快，但更跟随图像噪声；
- 调小：更稳，但可能很多帧都看不到变化；
- 恢复后方案的 0.20 已被当前 0.30 替代。

候选拒绝时，当前 verified EMA 不采用 candidate EMA，只衰减：

\[
e_i\leftarrow0.50e_i.
\]

质量失败、fixed、`u_t` 排除和关键接触切换会清零相应历史。

---

## 9. 从证据变成刚度：为什么使用指数

### 9.1 先算 log step

\[
\ell_i=
\operatorname{clip}
\left(
\eta e_{i,t},-\ell_{\max},\ell_{\max}
\right).
\]

当前启动值：

\[
\eta=0.18,\qquad\ell_{\max}=0.18.
\]

- \(\eta\)：学习率，证据转成变化的速度；
- \(\ell_{\max}\)：无论证据多大，单次提交都不能越过的安全帽。

### 9.2 Distance 候选

\[
k_{\mathrm{dist},i}^{\mathrm{cand}}
=
\operatorname{clip}
\left(
k_{\mathrm{dist},i}^{\mathrm{ver}}e^{\ell_i},
k_{\mathrm{dist}}^{\min},
k_{\mathrm{dist}}^{\max}
\right).
\]

当前启动 bounds：

\[
k_{\mathrm{dist}}\in[0.10,2.00].
\]

### 9.3 Shape 候选

\[
k_{\mathrm{shape},i}^{\mathrm{cand}}
=
\operatorname{clip}
\left(
k_{\mathrm{shape},i}^{\mathrm{ver}}
e^{1.5\ell_i},
k_{\mathrm{shape}}^{\min},
k_{\mathrm{shape}}^{\max}
\right).
\]

当前启动 bounds：

\[
k_{\mathrm{shape}}\in[0.003,0.020].
\]

shape 的 log step 是 distance 的 1.5 倍，所以对同一证据响应更明显。

### 9.4 为什么不用 \(k_{\mathrm{new}}=k+\Delta k\)

指数更新有一个重要优点：按比例变化。

如果：

\[
\ell=0.10,
\]

则：

\[
e^{0.10}\approx1.105.
\]

无论原刚度是 0.2 还是 1.0，都增加约 10.5%：

```text
0.2 -> 0.221
1.0 -> 1.105
```

这比所有区域都加固定 0.1 更合理。固定加法会让软区域瞬间变化 50%，硬区域只变化 10%。

### 9.5 完整数字例子：从 \(s\) 到 candidate

接着前面的平滑结果，假设：

\[
e=0.46.
\]

log step：

\[
\ell=0.18\times0.46=0.0828.
\]

若 verified distance 为 0.20：

\[
k_{\mathrm{dist}}^{\mathrm{cand}}
=0.20e^{0.0828}
\approx0.2173.
\]

约硬化 8.7%。

若 verified shape 为 0.004：

\[
k_{\mathrm{shape}}^{\mathrm{cand}}
=0.004e^{1.5\times0.0828}
\approx0.004529.
\]

约硬化 13.2%。

注意：这时只是 candidate，正式物理仍在使用旧的 verified。

### 9.6 单次绝对上限

如果 \(\ell=+0.18\)：

\[
e^{0.18}\approx1.197,
\]

distance 单次最多约增加 19.7%。

如果 \(\ell=-0.18\)：

\[
e^{-0.18}\approx0.835,
\]

distance 单次最多约降低 16.5%。

shape 使用 \(1.5\ell\)：

\[
e^{0.27}\approx1.310,\qquad
e^{-0.27}\approx0.763.
\]

### 9.7 bounds 和学习率不是一回事

- bounds 决定“最终允许走多远”；
- learning rate 决定“每次走多快”；
- maximum log step 决定“单次最多跨多大一步”。

例如把 upper bound 从 2 提到 10，并不会立刻变硬；只有连续可信证据、并且每次影子验收通过，
刚度才逐步接近更高值。

---

## 10. 为什么抓取节点 \(u_t\) 不能用于学习材料

### 10.1 \(u_t\) 是人为指定的运动

捕获后，direct anchor 的目标来自夹爪局部坐标：

\[
u_t=X_{\mathrm{jaw}}(t)\,u_{\mathrm{local}}.
\]

它的含义是：“这个点已经被夹住，应当跟随夹爪”。这是控制命令，不是材料自然产生的位移。

如果把它拿去计算 \(d\) 和 \(r\)，系统可能得出：

> “这个节点移动特别大，所以材料特别软。”

实际上它只是被夹爪强制带走。

### 10.2 当前排除范围

材料信号和更新目标同时排除：

- 4 个 direct anchors；
- support nodes；
- 上述节点的一环物理邻居。

“一环”指通过一条物理边直接相连的节点：

```text
邻居 o ----- [direct anchor X] ----- o 邻居
                         \
                          o 邻居
```

这使人工抓取位移和自然材料响应之间留出缓冲带。

### 10.3 全局暂停门

当前包括：

```text
|q7 速度| > 0.50 rad/s
最大穿透 > 1.0 mm
grip active 状态改变
接触数量发生明显倍数级切换
```

触发后冷却 3 次更新。capture/release、穿透或接触切换还会清理 EMA，避免旧证据在新接触条件下
继续生效。

---

## 11. 第一场考试：历史状态稳定门

### 11.1 它要防什么

新刚度可能让当前图像更好，却让过去已经对齐的状态自己漂走。例如：

```text
旧刚度：过去的组织放着不动
新刚度：过去的组织即使工具不动，也慢慢回弹、滑动或鼓起
```

所以系统保存最多 4 个准静态历史快照。

### 11.2 什么状态才有资格进入历史

当前初版要求：

```text
不在接触过渡门中
q7 速度 <= 0.10 rad/s
最大粒子速度 <= 0.02 m/s
当前 visual residual 已接受
```

完整 rollout 快照还包含位置、速度、器械命令、Gaussian、接触/抓取状态、插值器、时间和材料。

### 11.3 公平比较

对同一个历史状态，分别使用：

- baseline：旧 verified 刚度；
- candidate：新 candidate 刚度。

都把粒子速度清零、冻结抓取状态机和工具目标，各运行 1 个物理帧。

### 11.4 质量加权 RMS

单个历史快照的漂移：

\[
E_h(k)=
\sqrt{
\frac{
\sum_{i\in\mathrm{dynamic}}
m_i
\|x_{i,h}^{\mathrm{relax}}(k)-x_{i,h}^{\mathrm{acc}}\|^2
}{
\sum_{i\in\mathrm{dynamic}}m_i
}
}.
\]

逐项解释：

- \(x^{\mathrm{acc}}\)：历史上已经和图像对齐的位置；
- \(x^{\mathrm{relax}}(k)\)：换成刚度 \(k\) 后自己松弛出来的位置；
- 两者距离越大，说明新材料越不能保持该状态；
- \(m_i\) 让质量大的节点贡献相应权重；
- 开平方后重新回到米或毫米单位。

### 数字例子

三个节点质量为 \(1,2,1\)，漂移分别为 \(0.01,0.02,0.01\) mm：

\[
E=
\sqrt{
\frac{
1\times0.01^2+
2\times0.02^2+
1\times0.01^2
}{1+2+1}
}.
\]

\[
E=\sqrt{0.00025}\approx0.0158\ {\rm mm}.
\]

对最多 4 个历史快照取平均：

\[
L_{\mathrm{hist}}(k)
=
\frac1{|\mathcal H|}
\sum_{h\in\mathcal H}E_h(k).
\]

### 11.5 当前通过阈值

\[
L_{\mathrm{hist}}^{\mathrm{cand}}
\le
1.10L_{\mathrm{hist}}^{\mathrm{base}}
+0.01\ {\rm mm}.
\]

例：baseline 漂移 \(0.020\) mm：

\[
\text{允许上限}
=1.10\times0.020+0.010
=0.032\ {\rm mm}.
\]

candidate 为 0.030 mm 可以通过；0.040 mm 会拒绝。

这只是“1 个物理帧、最多 4 个快照”的初版稳定门，不等于长期稳定性证明。

---

## 12. 第二场考试：H=5 真实闭环影子验收

### 12.1 为什么叫影子

生成 candidate 后，正式 live 仿真仍然使用 verified。程序保存完整快照 \(S_t\)，并记录之后
5 个视频帧真实执行的器械命令：

\[
U_{t:t+5}.
\]

第 5 张新图像到达后，从同一个快照复制出 baseline 和四个 candidate 步长倍率的临时轨迹：

\[
\hat q_{t+1:t+5}^{\mathrm{base}}
=
\operatorname{Rollout}
(S_t,k_{\mathrm{ver}},U_{t:t+5},R_{t+1:t+4}),
\]

\[
\hat q_{t+1:t+5}^{\mathrm{cand},a}
=
\operatorname{Rollout}
(S_t,k_{\mathrm{cand}}(a),U_{t:t+5},R_{t+1:t+4}),
\]

其中 \(a\in\{0.5,1.0,1.5,2.0\}\) 是 log-step 倍率，\(R\) 表示中间帧按 live 相同算法
求解并写回的视觉 residual。候选创建帧的 residual 已经包含在 \(S_t\) 中，所以不重复写回。

它们是临时“影子”，跑完后真实 live 状态会恢复。

### 12.2 公平条件

两条影子完全相同的部分：

- 初始 \(q,qd\)；
- 器械 SE(3) 和 q7 命令；
- 接触参数和 top barrier；
- 已有 direct/support 绑定；
- 相机、真实图像和 mask；
- Gaussian 外观。

影子中关闭新的在线刚度提案，但**不关闭**真实 capture/release 状态机；end-to-end admission
还会逐帧重放 visual residual。baseline 与 candidate 的命令、图像、mask、residual 算法和
接触状态机完全相同，主要差别仍是材料场。

### 12.3 总图像损失必须真正改善

主报告比较的是每个新图像到来时、当前 residual 尚未写回前的 prediction loss。因此不能只
看第 5 帧终点；定义每一帧：

\[
D_{\mathrm{base}}^{(h)}
=D_{\mathrm{vis}}
(\operatorname{Render}(\hat q_{t+h}^{\mathrm{base}}),I_{t+h}),
\]

\[
D_{\mathrm{cand}}^{(h)}
=D_{\mathrm{vis}}
(\operatorname{Render}(\hat q_{t+h}^{\mathrm{cand}}),I_{t+h}).
\]

五帧累计平均和改善量：

\[
\bar D_{\mathrm{base}}=\frac15\sum_{h=1}^{5}D_{\mathrm{base}}^{(h)},
\qquad
\bar D_{\mathrm{cand}}=\frac15\sum_{h=1}^{5}D_{\mathrm{cand}}^{(h)},
\]

\[
\Delta\bar D=\bar D_{\mathrm{base}}-\bar D_{\mathrm{cand}}.
\]

当前必须满足：

\[
\Delta\bar D\ge10^{-5}.
\]

### 数字例子

若五帧 baseline loss 是：

\[
(0.0200,0.0210,0.0220,0.0215,0.0205),
\]

则平均为：

\[
\bar D_{\mathrm{base}}=0.0210.
\]

candidate 五帧平均至少要达到：

\[
\bar D_{\mathrm{cand}}\le0.02099.
\]

只让终点下降、但前四帧平均变差不会通过；这正是 H=1 旧版会长期漂移的原因。

### 12.4 任一相机不能明显牺牲

每台相机：

\[
D_{\mathrm{cand},c}
\le
D_{\mathrm{base},c}
+
\max(10^{-6},0.005D_{\mathrm{base},c}).
\]

若左目 baseline 是 0.030：

\[
\text{允许退化}
=\max(10^{-6},0.00015)
=0.00015.
\]

即使双目平均变好，左目也不能超过 0.03015。

### 12.5 体积质量门

candidate 不能增加翻转四面体数，并且：

\[
J_{\min}^{\mathrm{cand}}
\ge
\max
\left(
J_{\min}^{\mathrm{base}}-0.01,
0.98J_{\min}^{\mathrm{base}}
\right).
\]

若 baseline 最小 \(J=0.80\)：

\[
0.80-0.01=0.79,
\]

\[
0.98\times0.80=0.784,
\]

取更严格的 0.79，所以 candidate 不能低于 0.79。

### 12.6 接触和抓取门

最大穿透：

\[
p_{\mathrm{cand}}
\le
p_{\mathrm{base}}+0.10\ {\rm mm}.
\]

锚点 RMS 和最大误差都要求：

\[
E_{\mathrm{anchor}}^{\mathrm{cand}}
\le
E_{\mathrm{anchor}}^{\mathrm{base}}
+0.05\ {\rm mm}.
\]

历史门也必须继续满足。不能用“图像更像”换取更深穿透或更差抓取。

### 12.7 等待过久会过期

候选最多等待：

```text
120 个物理步
或 10 个视频帧
```

暂停播放、关键接触门触发或 grip active 改变都会拒绝，防止拿不同条件硬比较。

### 12.8 Commit 与 reject

全部通过：

\[
k_{\mathrm{ver}}\leftarrow k_{\mathrm{cand}},
\qquad
e_{\mathrm{ver}}\leftarrow e_{\mathrm{cand}}.
\]

同时尝试接回已经验证的固定滞后状态：

\[
q_{live}\leftarrow \hat q_{t+5}^{\mathrm{cand}}
\quad\text{仅当}\quad
\operatorname{RMS}(q_{live},\hat q_{t+5}^{\mathrm{cand}})\le0.25\ \mathrm{mm}
\]

且任一粒子变化不超过 `1.0 mm`。超限时材料仍可提交，但状态不接回，避免肉眼可见的跳点。

任何一项失败：

\[
k_{\mathrm{ver}}\ \text{保持不变},
\qquad
e\leftarrow0.5e.
\]

`reject` 是安全机制的正常工作，不是程序崩溃。频繁 reject 说明证据、参数步长或图像监督
不足以证明候选更好。

---

## 13. 从按下 Play 到一次刚度提交：完整运行流程

### 13.1 启动条件

推荐命令：

```bash
bash scripts/run_demo_browser_12.sh \
  --tissue-mode paper_pbd \
  --visual-feedback-mode residual \
  --online-stiffness-update \
  --visual-feedback-update-interval 3
```

只有同时满足：

```text
tissue mode   = paper_pbd
feedback      = residual
online update = enabled
```

才创建在线刚度更新器。日志应看到：

```text
online paper stiffness: ON
```

### 13.2 两个并行时钟

程序有视频协程和物理协程：

```text
视频请求：约 30 FPS
物理配置：60 Hz
每物理帧：12 substeps
视觉请求：每 3 个物理帧一次
```

但只有 `current_frame_index` 真的进入新图像，材料学习才向前推进。配置 30/60 Hz 不代表墙钟
一定实时，因为渲染、Adam 和双影子都是同步计算。

### 13.3 一个普通物理帧

1. 读取时间戳对应的器械 SE(3) 和 q7；
2. 将器械运动插值到 12 个子步；
3. 计算 jaw triangle-skin 接触和 oriented top barrier；
4. 求解 distance、volume、shape XPBD；
5. 若已捕获，应用 direct anchors、有限传播和 wrist support；
6. 检查局部 \(J\)，缩小或撤销危险修正；
7. 更新粒子速度并施加阻尼；
8. 从物理节点重蒙皮视觉表面和 Gaussian；
9. 恢复离线记录器械的严格运动学姿态。

### 13.4 到达一张新的视觉反馈图像

当前没有 pending candidate 时：

1. 计算快速 q7、穿透、capture/release 和接触切换门；
2. 确认图像索引比上一轮新；
3. 渲染左右目并计算 \(D_{\mathrm{vis}}\)；
4. 用 8 次 Adam 优化节点 residual；
5. 做局部体积保护和 backtracking；
6. 用正式 Gaussian 重蒙皮重新渲染；
7. residual 通过后写入 `particle_q`，速度不变；
8. 保存 \(x^{\mathrm{pred}}\)、\(x^{\mathrm{acc}}\)、\(r\)；
9. 计算质量、视觉监督和 `u_t` 排除 mask；
10. 计算原始 \(s_i\)；
11. 做物理邻域平滑和 EMA；
12. 用指数生成 candidate；
13. 跑历史稳定门；
14. 历史门通过则保存完整快照，进入 pending。

若 residual 自己被拒绝：

```text
位置不改
刚度不改
旧 residual 不叠加
材料证据历史清理
```

### 13.5 Pending 期间

```text
live XPBD：继续使用 k_verified
器械命令：继续执行并逐步记录
新 residual：继续正常求解和写回
新 stiffness candidate：暂停
```

等待期间 live 不能为了材料验收而停掉视觉校正。公平性由影子中对 baseline/candidate 重放
完全相同的中间 residual 保证。

### 13.6 第 5 张新图像到达

1. 从保存快照安装 verified；
2. 回放 5 帧完全相同的器械命令与中间 residual；
3. 记录 baseline 的五帧 pre-residual loss；
4. 对 `0.5/1.0/1.5/2.0` 四个 candidate 倍率重复同一闭环；
5. 恢复真正的 live 状态和当前图像；
6. 选累计双目 loss 最低的安全倍率；
7. 检查总图像、逐相机、\(J\)、翻转、穿透、锚点和历史门；
8. 全部通过则 commit，否则 reject；
9. commit 后在 `0.25/1.0 mm` 状态界内接回候选末状态；
10. 当前第 5 帧再正常求解一次 live residual，并允许生成下一轮 candidate。

完整状态机：

```text
waiting for visual evidence
          |
          v
 residual accepted?
    | no         | yes
    v            v
 wait       build candidate
                 |
                 v
          history gate
          | fail      | pass
          v           v
        reject      pending
                       |
                 collect H=5 images
                       |
          baseline + four closed-loop shadows
                | fail       | pass
                v            v
              reject       commit + bounded state adoption
                \            /
                 \          /
                  v        v
                    wait
```

---

## 14. GUI 参数怎么理解：调大调小分别发生什么

### 14.1 最常用材料参数

| GUI 参数 | 调大 | 调小 | 风险 |
|---|---|---|---|
| Distance lower bound | 最软区域也更紧 | 允许局部更能拉伸 | 太低会松散 |
| Distance upper bound | 可形成更硬区域 | 限制远程拉动 | 太高会长程传播 |
| Shape lower bound | 最软区域仍保持形状 | 允许更强剪切弯曲 | 太低会歪斜跳点 |
| Shape upper bound | 局部更抗剪切 | 更容易弯曲 | 太高会整片僵硬 |
| Learning rate | 每次候选变化快 | 变化慢 | 太高会频繁 reject |
| EMA new weight | 更跟当前图像 | 更依赖历史 | 太高追噪声，太低无明显效果 |
| Hardening bias | 含糊证据偏硬化 | 更容易软化 | 太高可能越学越硬 |
| Shape update gain | shape 比 distance 变化更快 | shape 更保守 | 太高容易把局部冲击传远 |

### 14.2 Advanced evidence and smoothing

| GUI 参数 | 实际公式位置 | 调大效果 |
|---|---|---|
| Maximum log step | \(|\ell|\le\ell_{\max}\) | 单次允许更大百分比变化 |
| Minimum residual | \(\|r\|\) 门 | 忽略更多小视觉修正 |
| Residual full scale | \(a_r=\|r\|/R_{\rm full}\) | 同样 residual 产生更弱信号 |
| Minimum deformation | \(\|d\|\) 门 | rest 附近更少学习 |
| Deformation full scale | \(a_d=\|d\|/D_{\rm full}\) | 同样形变产生更弱信号 |
| Spatial smoothing passes | 邻域混合次数 | 信号传播更多物理圈 |
| Neighbor smoothing blend | \(\beta\) | 更相信邻居、更平滑 |
| Rejected EMA keep ratio | reject 后的 EMA 比例 | 被拒证据保留更多 |

一个容易弄反的例子：

> `Residual full scale` 调大，不是允许 residual 更大；它是把同样 residual 看得更弱。

最大 residual 上限属于视觉 mapper，不是这个材料证据尺度。

### 14.3 当前 GUI 宽实验范围

```text
Distance lower bound    0.01 ..  1.00
Distance upper bound    0.10 .. 10.00
Shape lower bound       0.0001 .. 0.030
Shape upper bound       0.001  .. 0.100
```

启动值仍是：

```text
distance bounds  0.10 .. 2.00
shape bounds     0.003 .. 0.020
Reset field      distance=0.20, shape=0.004
```

超出启动范围属于压力测试。GUI 允许调，不代表已经验证安全。

### 14.4 Apply 为什么必须暂停

按 `Apply stiffness settings` 会：

1. 校验上下限和尺度是否合法；
2. 保留 verified 刚度，但裁剪到新 bounds；
3. 清空旧 EMA；
4. 清空历史快照；
5. 清空旧 validation 摘要；
6. 冷却 3 次更新。

如果播放中或仍有 pending candidate，会拒绝 Apply，防止一场考试中途改考卷。

---

## 15. 与接触/抓取相关、当前暴露的三个参数

这些参数不是刚度公式本身，但会改变“夹爪如何给组织施加运动”，因此会间接改变材料证据。

当前还有一项不放进常用 GUI 的安全默认值：向下 top-barrier 只给实际命中点附近约
`2.5 mm` 的物理面节点直接位移。设命中点为 (p)，物理面节点为 (x_i)，在局部表面切平面
上的距离为

\[
d_i^{\mathrm{tan}}
=\left\|(x_i-p)-((x_i-p)\cdot n)n\right\|.
\]

只有

\[
d_i^{\mathrm{tan}}\le2.5\ \mathrm{mm}
\]

才接收直接向下 contact delta；粗三角形无人落入时只选最近一个节点兜底。`2.5 mm` 以外仍可
通过真实四面体材料产生较弱响应，但不再被接触代码直接压下。材料投影修正进入下一子步速度
的比例当前为 `0.14`；几何接触速度比例 `0.35` 保持。`0.14` 会比此前 `0.05` 保留更多材料
跟随速度，但也更容易保留振荡和远端惯性长尾。这里没有改任何在线刚度公式或 bounds。

### 15.1 Tip entry allowance

允许夹爪尖端进入 top-sheet 屏障下方的距离。

- 变大：尖端可以更深，较容易从组织下方进入；
- 太大：可能出现穿透、捕获错误和体积质量门暂停；
- 它受 distal barrier 几何动态限制，不能无限超过有效夹爪段。

### 15.2 Capture max penetration

捕获瞬间允许的最大接触穿透：

\[
p_{\mathrm{capture}}\le p_{\max}^{\mathrm{capture}}.
\]

- 变大：深处接触也可能建立 persistent grip；
- 太大：夹爪已经穿过组织才绑定，随后会拉出异常尖峰；
- 变小：更保守，但可能一直无法 capture。

### 15.3 Grip support radius

direct anchor 周围允许成为 support 的空间半径。

- 变大：夹取更稳、更多节点共同跟随；
- 太大：人为运动影响更远，局部夹取变成整片拖动；
- 变小：更局部，但 direct anchor 周围可能承受过大相对变形。

这些 direct/support 节点及其一环都从刚度学习中排除，所以调大 support radius 还会减少可学习
节点数量。

---

## 16. 看到异常时，应该先看哪一个公式

### 16.1 少数粒子跳动、远离整体

先区分：

- residual 跳点：看 \(L_{\mathrm{spatial}}\)、\(L_{\mathrm{temporal}}\)、\(r_{\max}\)；
- 材料孤岛：看 neighbor smoothing、EMA 和 shape lower bound；
- 接触冲击：看 penetration、contact correction、velocity damping；
- 抓取控制：看 support radius、anchor error。

建议顺序：

1. 看 `Visual residual accepted / RMS / max`；
2. 看跳点是否发生在 residual 更新瞬间；
3. 若是，降低 residual learning rate 或 max residual，增强 spatial/temporal；
4. 若不是，适当提高 shape lower bound；
5. 若发生在夹爪接触瞬间，优先调接触而不是刚度学习率。

### 16.2 后侧超过 1 cm 的整体位移

常见原因：

- distance/shape 太高；
- material passes 太多；
- smoothing passes 或 blend 太高；
- grip support radius 太大；
- 人工 support 位移范围过宽。

这不是单纯 volume displacement，因为 volume 守恒会要求局部补偿，但不应天然只在 1 cm 外
产生一个明确控制范围。

当前 direct downward core 已限制为约 `2.5 mm`，因此若仍看到明显的 1 cm 运动，优先用关闭
visual residual 的 A/B 区分视觉状态修正与 XPBD 自然响应；不要把 `query distance=10 mm`
误当成 10 mm 位移半径。

### 16.3 后方局部鼓包

重点看：

\[
J=\frac{V}{V_0}.
\]

- 若鼓包处 \(J>1\)：确实有局部体积膨胀；
- 若 \(J\approx1\) 但表面鼓：更可能是四面体剪切、表面蒙皮或邻区位移重分布；
- 若 residual 更新后才出现：看 exact reskin 前后和局部冻结数；
- 若接触时出现：看 top support/pressure shoulder；当前显式 pressure shoulder 已关闭。

### 16.4 刚度完全看不出变化

按顺序检查：

1. 是否启动了 `paper_pbd + residual + online update`；
2. residual 是否 accepted；
3. `Evidence active` 是否大于 0；
4. 是否一直触发 q7/contact/penetration gate；
5. \(\|r\|\) 是否超过 0.02 mm；
6. \(\|d\|\) 是否超过 0.10 mm；
7. candidate 是否频繁被 prediction gap 拒绝；
8. bounds 是否已经把刚度夹在边界。

不要一看到没变化就只扩大 bounds。没有材料证据时，范围再大也不会更新。

---

## 17. GUI 实时数据应该怎么看

默认摘要中最有用的是：

```text
Stiffness status
commit / reject count
distance min / median / max
shape min / median / max
evidence active / harden / soften
latest rejection reason
residual accepted / loss reduction / max
contact penetration / grip state / anchor error
```

状态解释：

| 状态 | 含义 |
|---|---|
| waiting | 还没有合格的新图像证据 |
| candidate | 已产生候选，但未提交 |
| pending | 正等待下一张新图像考试 |
| committed | 候选通过，正式刚度已更新 |
| rejected | 候选失败，verified 保持不变 |
| expired/cancelled | 等太久、暂停或接触条件改变 |

最重要的不是只看 min/max，而是同时看：

```text
刚度变化
    +
prediction improvement
    +
minimum J / penetration / anchor error
```

图像变好但物理质量变坏，不算成功。

---

## 18. 第 5、6 节现在怎样运行和保存

### 18.1 多帧开放环

方案要求从一个已接受状态出发，向未来预测：

\[
H\in\{1,3,5,10\}.
\]

未来误差：

\[
\operatorname{Gap}(t,h)
=
D_{\mathrm{vis}}
\left(
\operatorname{Render}(\hat q_{t+h}),
I_{t+h}
\right).
\]

现在每个通过下一图像考试并正式 commit 的 candidate，都会保留提交前的完整快照、旧刚度、
新刚度和随后真实执行的器械命令。在精确的目标视频帧 \(t+H\)，分别运行：

```text
材料隔离：
  old stiffness + 冻结原有 direct/support/grip 状态机
  new stiffness + 冻结原有 direct/support/grip 状态机

端到端：
  old stiffness + 接触/抓取状态机自行演化 + 中间帧 residual 重放
  new stiffness + 接触/抓取状态机自行演化 + 中间帧 residual 重放
```

如果 GUI 的视觉求解跳过了某个目标帧，评测器会临时读取该目标帧的真实双目图像，评测完成后
恢复当前 live 图像和完整仿真状态。因此 \(H=3\) 记录对应的就是起点后三个视频帧，不会把
“下一次恰好可用的图像”冒充成 \(H=3\)。

除了 Gap，还会对 old/new 的开放环状态各求一次“不写回”的视觉 residual：

\[
R_{\mathrm{open}}(t,H)
=
\sqrt{
\frac1N\sum_i\|r_i^{\mathrm{needed}}\|^2
}.
\]

它回答：“纯物理向前预测 \(H\) 帧以后，还需要多大的节点修正才能重新对齐图像？”值越小，
说明开放环预测越接近观测。求这个 residual 只用于测量，不会修改影子或 live 粒子。

### 18.2 完整指标持久化和阶段分组

方案要求把图像、物理、材料、未来预测指标按：

```text
压下 / 闭合 / 捕获 / 抬起 / 放下 / 松开
```

分组保存。现在使用世界 \(+Z\) 作为抬起方向，并结合 q7 motion、接触数和 grip active 转换
自动分类：

```text
press    未抓取、向下接近或已有表面接触
close    q7 正在闭合或捕获计数正在累积
capture  grip 从 false 变成 true
lift     已抓取且工具向 +Z 运动
place    已抓取且工具向 -Z 运动
release  grip 解除或 q7 打开
idle     暂时不能可靠归入以上六类
```

capture/release 会保持几个采样，避免很短的状态跳变没有落到任何视觉记录中。

### 18.3 怎样启动

最简单的评测入口：

```bash
bash scripts/run_super_stiffness_evaluation.sh
```

脚本自动创建带时间戳的目录。也可以直接指定：

```bash
bash scripts/run_demo_browser_12.sh \
  --tissue-mode paper_pbd \
  --visual-feedback-mode residual \
  --online-stiffness-update \
  --stiffness-evaluation-horizons 1,3,5,10 \
  --stiffness-evaluation-output outputs/my_stiffness_run
```

输出目录中：

```text
metadata.json   数据集、horizons、协议和 schema
events.jsonl    每一行一个可独立读取的事件
summary.json    按阶段聚合数值 count/mean/min/max 和状态/拒绝原因计数
```

目录若已经有上述结果文件，程序会拒绝覆盖，防止误删上一轮实验。

### 18.4 每条 prediction 记录有什么

主要字段：

```text
baseline_gap / candidate_gap / gap_improvement
left/right baseline and candidate camera gaps
baseline/candidate open-loop residual RMS
baseline/candidate displacement RMS
baseline-candidate particle RMS
minimum J / inverted tetrahedra
distance/volume/shape loss
penetration / anchor RMS / anchor maximum
old/new distance min/median/max/mean
old/new shape min/median/max/mean
phase / protocol / start frame / target frame / H
```

功能实现和 headless 回归已经完成；仍需在有 CUDA 和显示服务的机器上跑完整真实轨迹，才能
得到可用于论文结论的数值报告。代码“会产出报告”和“报告已经证明新方法更好”仍是两件事。

---

## 19. 一次推荐的最小观察实验

为了看懂参数，不要一次改五个值。建议：

1. 使用 Reset：distance 0.20，shape 0.004；
2. 暂停后只改一个参数；
3. 从同一视频起点重新运行；
4. 记录同一动作阶段的 commit/reject、刚度分布和最小 \(J\)；
5. 不只看当前图像，还看后续组织是否漂移；
6. 至少比较三组：

```text
A：online stiffness off
B：online stiffness on，默认参数
C：online stiffness on，只改变一个参数
```

例如研究 neighbor blend：

```text
0.10：更局部
0.35：当前默认
0.60：更平滑但传播更远
```

这样才能知道效果来自哪个参数。

---

## 20. 最后用四句话记住整套方法

1. **XPBD 刚度**决定物理约束把组织拉回合理形状的强弱。
2. **Visual residual**只回答当前粒子位置应该补多少，不直接等于材料刚度。
3. **\(d\) 与 \(r\) 的夹角**给出软硬方向，位移幅值、门控、平滑和 EMA 决定证据可信度。
4. **Candidate 必须通过历史门和下一图像双影子门**，才能变成 verified。

最短流程：

```text
物理预测
 -> 双目发现误差
 -> 小幅修正位置
 -> 判断局部太软/太硬
 -> 生成有界刚度候选
 -> 历史考试
 -> 下一图像考试
 -> commit 或 reject
```

---

## 21. 关键代码位置

| 功能 | 文件 |
|---|---|
| 在线运行时序、GUI、门控、历史和影子验收 | `examples/example_embodied_super_offline.py` |
| 刚度信号、平滑、EMA、candidate/verified | `src/embodied_gaussians/physics_simulator/online_tissue_stiffness.py` |
| 多 horizon、动作阶段和 JSONL/summary | `src/embodied_gaussians/physics_simulator/stiffness_evaluation.py` |
| distance/volume/shape XPBD | `src/embodied_gaussians/physics_simulator/integrator.py` |
| 双目 residual 损失和物理正则 | `src/embodied_gaussians/physics_simulator/visual_tissue_residual_mapping.py` |
| 正式 Gaussian 重蒙皮与 residual 验收 | `src/embodied_gaussians/embodied_simulator/simulator.py` |
| 接触和 persistent grip | `src/embodied_gaussians/physics_simulator/triangle_skin_contact.py` |
| 场景默认参数 | `examples/embodied_environments/super_embodied/super_embodied.py` |
| 原目标方案 | `刚度优化方案.md` |
| 当前框架边界 | `CURRENT_SUPER_TISSUE_FRAMEWORK.md` |
| 一键评测入口 | `scripts/run_super_stiffness_evaluation.sh` |
