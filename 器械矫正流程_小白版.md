# SUPER PSM 器械矫正流程（小白版）

> 更新时间：2026-07-25  
> 当前默认方案：`depth_then_visual`  
> 本文只讲器械位姿，不讲组织重建和视觉力求解。

## 1. 先记住最终答案

目前 GUI 默认读取的器械位姿文件是：

```text
data/super/psm_tracking/psm_depth_then_visual_pose_driver_candidate.npz
```

虽然文件名还带有 `candidate`，它现在已经是本项目默认启用、通过现有门禁的
`depth_then_visual` driver。这里的 `candidate` 只是生成时留下的历史文件名，
不是说程序仍默认使用旧的 `corrected`。

日常运行只需要：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1
```

这个脚本已经默认设置：

```text
左右目                         stereo_left,stereo_right
器械位姿                       depth_then_visual
器械显示                       tip
运行时器械自旋修正             -27°
组织                           adaptive_soft
组织—器械接触                  开启
```

整个矫正思路可以用一句话概括：

> 机器人编码器负责让器械“稳定地运动”，双目深度负责判断“离相机多远”，
> 图像轮廓负责判断“画面里对没对齐”，URDF 负责保证“夹爪结构不能散架”，
> 最后把结果烘焙成一张逐时刻位姿表交给 GUI 播放。

## 2. 为什么原始机器人数据还需要矫正

PSM 会记录自己的关节角，例如插入了多少、手腕转了多少、夹爪张开了多少。
这些记录很稳定，但它们不等于“视频里绝对准确的位置”。

可能产生偏差的原因包括：

- 相机和机器人之间的 hand-eye 外参存在小误差；
- 关节零点、钢丝传动和回差不完全理想；
- 数字 CAD 与真实器械型号、安装位置存在小差别；
- 相机图像和机器人状态的采样频率不同；
- 二维图像看起来对齐时，三维深度仍可能偏几毫米；
- 左右夹爪如果分别自由拟合，很容易出现铰点分离。

因此不能只相信编码器，也不能只相信图像。当前方案给不同信息分配了不同职责。

## 3. 小白需要先认识的几个词

### 3.1 q7

`q7` 是 PSM 自己报告的 7 个主要关节状态。可以把它理解为机器人内部的七个
“旋钮读数”：

```text
yaw / pitch / insertion / roll / wrist pitch / wrist yaw / jaw
```

### 3.2 前向运动学

前向运动学简称 FK。它根据 q7、每段器械的长度以及关节父子关系，计算每个零件
在三维空间中的位置和朝向。

### 3.3 位姿

位姿同时描述：

- 在哪里：`x, y, z`；
- 朝向哪里：四元数 `qx, qy, qz, qw`。

所以一个零件的位姿一共是 7 个数。

### 3.4 link

器械不是一整块模型，而是由多个刚性零件组成。当前 driver 保存 7 个可见 link：

| link | 通俗含义 |
|---|---|
| `tool_main_link` | 长器械杆主体 |
| `tool_wrist_link` | 末端腕部锚点 |
| `tool_wrist_shaft_link` | 腕部固定连接段 |
| `tool_wrist_sca_link` | wrist-pitch 后的零件 |
| `tool_wrist_sca_shaft_link` | 两片夹爪的共同父节点 |
| `tool_wrist_sca_ee_link_1` | 第一片夹爪 |
| `tool_wrist_sca_ee_link_2` | 第二片夹爪 |

### 3.5 pose driver

pose driver 是提前计算好的“器械动画表”。当前文件包含：

```text
5458 个机器人时间点
× 7 个器械 link
× 7 个位姿数值
```

GUI 运行时不再重新跑深度网络和图像优化，只读取这张表。

### 3.6 mask

mask 是图像中的区域标签。当前把器械分成：

```text
body
jaw_left
jaw_right
```

这样可以避免一片夹爪漏分时，把整件器械错误地拉向另一片夹爪。

## 4. 当前完整流程鸟瞰

```text
原始双目视频 + q7 + 相机标定 + hand-eye + LND
                         │
                         ▼
             机器人运动学稳定主干
                 strict / registered LND
                         │
                         ▼
       body、jaw_left、jaw_right 三部分图像矫正
                         │
                         ▼
              corrected 基准位姿
                         │
         ┌───────────────┴────────────────┐
         │ 9 个关键帧重新从 registered LND 出发 │
         ▼                                │
  FoundationStereo 小步深度修正             │
         │                                │
         ▼                                │
  可微 CAD 图像轮廓修正                     │
         │                                │
         ▼                                │
  与旧 corrected 逐帧 A/B                  │
         └────只保留确实更好的 5 帧─────────┘
                         │
                         ▼
       相对 corrected 的小残差按真实时间戳传播
                         │
                         ▼
           URDF 重建腕部和对称夹爪结构
                         │
                         ▼
       1441 个视频时刻重采样到 5458 个机器人时刻
                         │
                         ▼
              depth_then_visual driver
                         │
                         ▼
                    GUI / 物理仿真
```

下面逐步解释。

## 5. 第一层：机器人运动学主干

原始视频约有 `1441` 帧，约为 30 Hz；机器人状态有 `5458` 个，约为 99 Hz。
两者不能按数组序号硬配对，必须使用真实时间戳。

项目先用：

```text
q7
+ LND Modified-DH 运动学
+ hand-eye 外参
+ 双目 rectification
```

得到每个时刻的稳定器械运动。`LND` 可以理解为一套描述器械长度、关节轴和运动
方式的几何说明书。

这一阶段产生两种常见对照：

- `strict`：直接使用 LND/编码器得到的稳定运动学；
- `registered_lnd`：在 strict 基础上，加一次固定的 CAD 坐标注册。

它们运动稳定，但还没有逐帧贴合真实图像。

## 6. 第二层：生成旧的 corrected 基准

`corrected` 是当前深度矫正之前的可靠基准。它不是纯图像跟踪，而是：

1. 以 registered LND 为运动主干；
2. 把视频中的器械分成 body、左夹爪和右夹爪；
3. 用图像只估计一个有上限的小残差；
4. 对残差做时间平滑；
5. 用 URDF 重新构建腕部和两片夹爪。

每个视频帧只允许优化 9 个小修正：

```text
整件器械旋转       3
整件器械平移       3
wrist 残差         2
共同 jaw 残差      1
合计               9
```

旧 corrected 阶段的主要边界是：

| 项目 | 上限 |
|---|---:|
| 整体平移 | `3 mm` |
| 整体旋转 | `8°` |
| wrist | `12°` |
| 共同 jaw | `25°` |

这些边界像护栏：图像可靠时允许模型靠近视频轮廓；mask 出错时，不允许器械
突然飞走。

旧 corrected 文件仍保留为：

```text
data/super/psm_tracking/psm_part_corrected_pose_driver.npz
```

它现在主要用于 A/B 对照和作为新流程的安全基准，不是默认入口。

## 7. 第三层：为什么还要“先深度、后视觉”

二维图像只能直接看到上下左右的位置。一个模型离相机远一点并放大，或者离相机
近一点并缩小，有时会产生相似的二维轮廓。因此只看单目轮廓，三维深度可能仍有
毫米级误差。

当前 `depth_then_visual` 在 9 个诊断关键帧上采用：

```text
registered LND
  -> 双目深度微调
  -> 图像轮廓微调
  -> 与旧 corrected 比较
```

注意：关键帧候选从 registered LND 出发，而不是在 corrected 上无限叠加优化。
最后只有通过 A/B 门禁的候选，才会作为相对 corrected 的小修正写入新 driver。

## 8. 第四层：双目深度怎样修正

深度阶段使用两套方法：

- FoundationStereo：提供主要深度观测；
- RAFT-Stereo：只做独立一致性检查。

可以把它们理解为“一把主尺子”和“一把复核尺子”。如果两把尺子明显冲突，
该处深度就不值得信任。

项目把真实 CAD 三角面渲染成深度图，与双目观测的器械主体深度比较。每轮只沿
rectified 左相机的 `z` 方向移动一点：

```text
每轮最多                  0.3 mm
最多                      4 轮
累计最多                  1.2 mm
```

每移动一次都会重新渲染，而不是一次套用一个固定的全序列平移。如果出现以下
情况，就停止或拒绝：

- Foundation 深度没有改善；
- RAFT 复核变差；
- body 轮廓 Dice 明显下降；
- 可靠深度像素不足。

这也是为什么此前被拒绝的固定 `-5.8 mm` 或 `stereo_3d` 实验不能作为当前
正式流程：它虽然让某些深度数字变小，却让真实图像中的器械投影明显变差。

## 9. 第五层：深度之后再做图像轮廓修正

深度只能帮助确定前后距离，不能精确解决：

- 器械在图像中的左右和上下偏差；
- 小角度倾斜；
- wrist 方向；
- 夹爪尖端位置。

因此深度状态会作为可微 CAD 渲染器的初始化。优化器把数字 CAD 投影到图像，
与 body、jaw-left、jaw-right mask 和夹爪尖端比较。

这一阶段仍然有严格边界：

| 项目 | 上限 |
|---|---:|
| 相机 x/y 平移 | 每轴 `±3 mm` |
| 相机 z 平移 | `±0.4 mm` |
| 整体旋转 | `8°` |
| wrist | `12°` |
| 共同 jaw | `25°` |

左右夹爪不能分别自由平移；它们只能围绕共同铰点对称开合。

## 10. 第六层：新结果必须打败旧 corrected

不是优化器给出的结果都会采用。每个关键帧都会把：

```text
新 depth_then_visual 候选
vs
旧 corrected
```

放在一起检查。检查内容包括：

- body Dice；
- body 轮廓距离；
- 左右夹爪 Dice；
- 夹爪尖端像素误差；
- Foundation 深度误差；
- 当前 mask 的置信度。

9 个关键帧中，最终接受 5 帧：

```text
141, 160, 480, 1120, 1280
```

拒绝并严格回退旧 corrected 的 4 帧：

```text
0, 320, 800, 1360
```

序列首尾也是零残差锚点。也就是说，拒绝新结果不会把旧的可靠位姿破坏掉。

5 个接受帧的中位数结果为：

| 指标 | 旧 corrected | 新结果 |
|---|---:|---:|
| body Dice | `0.757` | `0.791` |
| 左右夹爪尖端平均误差 | `1.59 px` | `1.03 px` |
| Foundation 深度绝对中值误差 | `3.30 mm` | `2.45 mm` |
| RAFT 深度绝对中值误差 | `3.29 mm` | `2.42 mm` |

对比图位于：

```text
data/super/psm_tracking/psm_depth_then_visual_v1/contact_sheet.png
```

![depth_then_visual 关键帧对比](data/super/psm_tracking/psm_depth_then_visual_v1/contact_sheet.png)

## 11. 第七层：怎样从 5 帧变成完整视频

不能只在 5 个时刻突然改变器械，否则画面会跳动。项目先计算：

```text
被接受候选 - 同帧旧 corrected = 小残差
```

然后在真实视频时间戳上使用 PCHIP 插值传播这些小残差。PCHIP 的作用是让曲线
平滑，同时尽量不在关键帧之间产生过冲。

被拒绝关键帧和序列首尾的残差严格为 0，所以插值在这些位置一定回到旧
corrected。

完整时序还必须通过运动门禁：

| 新增修正的最大运动量 | 实测 |
|---|---:|
| 最大线速度 | `0.662 mm/s` |
| 最大角速度 | `2.689°/s` |
| 最大线加速度 | `2.26 mm/s²` |
| 最大角加速度 | `15.43°/s²` |

这些数值都低于设置的硬上限，说明新增修正不会产生明显抖动或瞬移。

## 12. 第八层：URDF 保证夹爪结构正确

图像优化完成后，不会把七个 link 当成七个互不相关的物体。项目使用 URDF
重新构建远端父子链：

```text
tool_wrist_link
  └─ wrist shaft
      └─ wrist pitch
          └─ wrist yaw
              ├─ jaw 1: +jaw/2
              └─ jaw 2: -jaw/2
```

这样两片夹爪始终满足：

- 同一个父节点；
- 同一个铰点；
- 同一条铰轴；
- `+jaw/2` 和 `-jaw/2` 对称开合。

完整 1441 帧的门禁结果：

```text
两片夹爪共同铰点最大 gap       0 m
铰轴最大角误差                 5.79e-6°
```

因此图像噪声不会把左右夹爪拉成两个分离物体。

## 13. 第九层：生成 GUI 使用的 driver

图像流程得到 `1441` 个视频时刻，机器人/GUI 时间轴有 `5458` 个时刻。最后按
真实时间戳重采样：

- 平移使用线性插值；
- 旋转使用四元数 SLERP。

最终 driver 格式为：

```text
timestamps                         (5458,)
link_names                         (7,)
poses_rect_camera_xyz_xyzw         (5458, 7, 7)
```

当前文件 SHA256 为：

```text
72bf8aaade1e935445c6908e0519a8c63c9d202f31c21710e598cef91938b07f
```

完整生成报告：

```text
data/super/psm_tracking/psm_depth_then_visual_full_v1/report.json
```

## 14. GUI 运行时到底做什么

GUI 运行时不会再次矫正器械。它只做下面几件事：

1. 从 driver 读取 rectified 左相机坐标系中的 7 个 link 位姿；
2. 用统一的 `X_table_camera` 转换到当前 table/world 坐标系；
3. 按当前播放时间选择 5458 个机器人状态中的一个；
4. 把位姿写入 Warp 的两个物理 state；
5. 每次 physics step 后重新写回精确目标，避免 articulation FK 覆盖 driver；
6. 在一帧的 12 个物理 substep 内插值器械位姿，避免瞬间跨过组织 skin；
7. 让绑定到 link 的 Gaussian 外观和碰撞 mesh 一起运动。

当前组织接触不会修改器械目标位姿：

```text
kinematic pose retraction = OFF
```

接触时是组织让开并变形，而不是把器械从 corrected 轨迹推走。

还要区分两种完全不同的“视觉”：

- 离线器械矫正：生成 `depth_then_visual` driver；
- 运行时 visual force：只拉动组织，PSM 不参与 visual force。

## 15. 默认的 -27° 是什么

当前命令还会在 driver 上叠加：

```text
PSM self-spin offset = -27°
```

它是沿器械长杆轴的运行时自旋修正，用来改善当前序列中的外观对齐。它：

- 不写入 `depth_then_visual` 文件；
- 不修改 `robots.json`；
- 不通过 strict FK 覆盖 driver；
- 会让远端腕部和夹爪共同绕真实 URDF 轴旋转。

GUI 中手动增加 jaw opening 或相机方向平移，也都是临时叠加，不会改写离线
driver。Reset 后会恢复命令行默认值。

## 16. 各种 driver 到底有什么区别

| 名称 | 含义 | 当前用途 |
|---|---|---|
| `strict` | 原始 q7 + LND 稳定运动学 | 运动学对照 |
| `registered_lnd` | strict 加固定 CAD 注册 | 图像矫正先验 |
| `paper` | 尽量忠实复现论文视频跟踪 | 论文基线 |
| `paper_robust` | 历史增强论文版本 | 历史对照 |
| `hybrid` | 历史 LND/论文混合实验 | 消融对照 |
| `corrected` | LND + 三部分图像小残差 | 深度前基准、A/B |
| `depth_then_visual` | 深度先行、视觉复核、相对 corrected 安全传播 | **当前默认** |

不要使用已撤销的 `stereo_3d` 实验。它曾让数值深度变好，但真实图像投影变差，
因此没有运行选项，也没有覆盖任何正式 driver。

## 17. 当前有哪些安全门

为了避免一次错误分割或深度估计毁掉全序列，当前有以下保护：

1. table frame、camera 和旧 corrected 文件有 SHA256 冻结检查；
2. 深度每轮只允许小步移动，且每步重新渲染；
3. Foundation 是主深度，RAFT 负责独立复核；
4. 图像优化的平移、旋转、wrist 和 jaw 都有上限；
5. 每个关键帧必须和旧 corrected 做 A/B；
6. 被拒绝关键帧使用严格零残差；
7. 插值使用真实时间戳，而不是简单帧号；
8. 完整序列检查新增修正的速度和加速度；
9. URDF 强制夹爪共享铰点并对称开合；
10. driver 独立保存，不覆盖旧 corrected。

## 18. 怎样确认自己运行的是正确版本

运行：

```bash
bash scripts/run_demo_thinlinc.sh \
  --visual-force-iterations 1
```

启动日志必须出现类似：

```text
psm_pose_driver=depth_then_visual:
.../psm_depth_then_visual_pose_driver_candidate.npz

enabled_cameras=['stereo_left', 'stereo_right']
```

如果要只比较器械位姿，建议暂时关闭组织接触和视觉力：

```bash
# 当前默认
bash scripts/run_demo_thinlinc.sh \
  --psm-pose-driver depth_then_visual \
  --no-psm-tissue-contact \
  --visual-force-iterations 0

# 旧 corrected 对照
bash scripts/run_demo_thinlinc.sh \
  --psm-pose-driver corrected \
  --no-psm-tissue-contact \
  --visual-force-iterations 0
```

两次运行都保留相同的相机、CAD、URDF 和 `-27°` 自旋，看到的差异才主要来自
depth-then-visual 矫正本身。

## 19. 只有需要重建 driver 时才运行的命令

仓库中已经有正式产物。日常回放不要重新运行下面这些耗时步骤。

先重新生成 9 个关键帧的深度后视觉候选：

```bash
/Media_HDD/jwshan/conda_envs/online_dvrk/bin/python \
  scripts/refine_super_psm_depth_then_visual_keyframes.py \
  --output-dir data/super/psm_tracking/psm_depth_then_visual_v1
```

再把通过 A/B 的关键帧残差传播到完整序列并生成 runtime driver：

```bash
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/build_super_psm_depth_then_visual_driver.py
```

两个脚本都会检查冻结输入；坐标或旧 driver 被意外修改时会直接拒绝继续。

## 20. 当前仍需知道的边界

- 真正新增的深度后视觉证据来自 5 个接受关键帧，其余时刻是相对旧 corrected
  的平滑残差传播，不是每帧重新做一次双目优化。
- 默认 `-27°` 来自当前视频序列的目视修正，还不是用独立标定工具测出的绝对
  roll 零点。
- 自动门禁和 A800 100 步加载测试已经通过，但完整视频仍值得持续人工观看，
  尤其是快速自转、严重遮挡、夹爪重新张开和真实组织接触区间。
- `depth_then_visual` 修正的是器械位姿；组织视觉力和组织接触是另外两条运行时
  链路，不能用组织形变反推器械已经正确。

## 21. 最后再用一句话记忆

```text
q7/LND 决定“怎么稳定地动”
双目深度决定“前后距离”
三部分 mask 决定“轮廓和尖端是否对齐”
旧 corrected 负责“候选不行就回退”
URDF 负责“夹爪必须像真正的关节”
pose driver 负责“把最终答案交给 GUI”
```
