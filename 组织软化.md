# 组织软化

> 建立日期：2026-07-30  
> 当前目标：参考论文 arXiv:2309.11656，从现有夹取视频离线标定出与视频柔性表现接近的等效软组织；将标定后的固定物理模型放回 GUI，使用视频器械位姿完成接触回放，最后用视觉力修正剩余的局部状态误差。
>
> 当前进度：阶段 A、B、C、D 已完成；下一步实施阶段 E（固定标定材料后加入论文式可见表面残差映射）。

## 1. 当前人工器械位姿矫正基线

以下数值是人工观察并调整后的当前基线。材料标定首先使用这组固定修正，不能让组织刚度优化器暗中修改器械位姿来降低损失。

```text
PSM self-spin offset deg                 -9.184 deg
Manual image X mm (+right)               +0.332 mm
Manual image Y mm                        0.000 mm
Manual camera Z mm (+far)                -6.735 mm
PSM jaw opening offset deg (+open)        0.000 deg
其他未提到的人工位姿修正                  0
```

符号解释：

- `Manual image X > 0` 表示在图像中向右；
- `Manual camera Z > 0` 表示远离相机，因此当前 `-6.735 mm` 表示向相机靠近；
- 自旋、平移和夹爪 offset 都叠加在所选视频 pose driver 上；
- 这组值是当前序列的人工对齐基线，不代表器械 CAD、hand-eye 或运动学已经得到唯一的真实矫正。

在开始材料优化前，需要把所使用的 pose driver 名称、视频帧范围、相机标定版本和上述数值一同冻结，保证离线标定与 GUI 回放采用完全相同的器械轨迹。

## 2. 总体思路

完整流程分成三个互相隔离的阶段：

```text
夹取视频、双目点云、器械位姿
                ↓
论文式残差映射和离线材料标定
                ↓
冻结并导出等效软组织参数
                ↓
GUI 中按视频绝对位姿进行器械—组织接触回放
                ↓
Gaussian 跟随四面体组织运动
                ↓
视觉力只修正剩余局部误差
```

核心原则：

1. 材料标定时关闭视觉力，避免视觉力掩盖错误的组织刚度。
2. GUI 回放时冻结材料参数，不实时优化刚度。
3. 不把离线逐帧残差位移烘焙成固定动画。
4. 保留现有四面体物理粒子、Gaussian 粒子及其绑定关系。
5. 器械始终按照视频的绝对位姿回放，不允许物理求解器为了防穿透而回退或修改器械位姿。
6. 理论上只让两片夹爪参与组织接触；轴杆不参与组织碰撞。
7. 接触模型在材料标定期间保持固定，避免接触参数和材料刚度同时变化而互相补偿。

## 3. 对论文方法的适配

参考论文：

```text
Real-to-Sim Deformable Object Manipulation:
Optimizing Physics Models with Residual Mappings for Robotic Surgery
https://arxiv.org/abs/2309.11656
```

论文方法包含两部分：

1. **残差映射**：利用观测表面点云与模拟表面的距离，估计当前状态的非刚性残差；数据项作用于可见表面，物理约束把影响传播到组织内部。
2. **刚度优化**：利用残差大小、历史静止一致性和空间平滑约束，更新 PBD 中的局部刚度。

论文优化的是 PBD 距离约束和形状匹配刚度。当前项目使用四面体 XPBD 和以杨氏模量、泊松比表达的材料模型，因此不能直接复制论文中的刚度数值。这里复用的是它的“观测残差驱动物理参数标定”思路，实际优化当前求解器自己的材料参数。

单段夹取视频只能获得这条操作轨迹下的**等效柔性模型**，不能证明标定值就是真实生物组织的材料参数。没有被夹取轨迹激励到的区域也无法可靠识别局部刚度。

## 4. 位姿误差与材料优化的隔离

器械位姿误差会被错误吸收到材料参数中：

- 器械估计得偏高，优化器可能把组织调得过软；
- 器械估计得偏低，优化器可能把组织调得过硬；
- 水平或自旋误差可能被解释为局部刚度或摩擦差异；
- 时间同步误差可能被解释为阻尼差异。

因此第一版采用以下规则：

1. 使用第 1 节的人工修正作为固定轨迹基线；
2. 优先通过器械轮廓、器械关键点和 CAD 投影评估位姿误差；
3. 组织表面误差不得反向自由修改逐帧器械位姿；
4. 如果后续需要继续校正，只允许优化整段共享的低维刚体偏移、时间偏移和幅度受限的平滑轨迹残差；
5. 每次修改位姿版本后，旧的材料标定结果自动失效，必须重新标定或重新验证。

## 5. 分阶段实施计划

### 阶段 A：冻结输入和建立数据清单

记录并固定：

- 原始左右目视频及时间戳；
- 相机内参、外参和双目基线；
- 当前使用的 PSM pose driver；
- 第 1 节人工位姿修正；
- 器械 CAD 版本和夹爪开合数据；
- 组织初始四面体、表面三角形和 Gaussian 绑定资产；
- 首次接触、夹取、牵拉、释放和回弹帧。

输出一份可复现的标定 manifest，后续所有离线运行和 GUI 回放均读取同一份记录。

#### 阶段 A 实施结果（2026-07-30）

已新增 `scripts/freeze_super_tissue_calibration_inputs.py`，输出：

```text
data/super/tissue_calibration_v1/stage_a_frozen_manifest.json
```

当前冻结结果：

- PSM pose driver 固定为
  `raw_paper_lnd_sam2_dense_contact_closedjaw`；
- 人工修正固定为第 1 节的
  `-9.184 deg / +0.332 mm / 0 mm / -6.735 mm / 0 deg`；
- 左右视频各 `1441` 帧，完整 GUI 播放范围为左帧 `0..1440`；
- 双目观测采用最近时间戳配对并固定 `20 ms` 门槛；左帧
  `86, 158, 240, 716, 738, 1108, 1213, 1440` 因超限而不进入双目材料标定，
  其余 `1433` 帧可用；
- 关键时刻冻结为：首次接触候选帧 `270`、夹爪闭合帧 `548`、牵拉参考帧
  `700`、最大回撤帧 `906`、释放完成帧 `1276`、回弹参考帧 `1300`；
- 器械 CAD/URDF、两片夹爪、原始 jaw q7、相机标定、table frame、四面体组织、
  Gaussian 四粒子绑定、左右组织 mask 和器械 mask 均记录了路径、大小和
  SHA256；
- 原始 `28 GB` bag 复用既有完整 SHA256，并重新核对文件大小；其他冻结输入
  由脚本重新计算 SHA256；
- 14 项输入一致性门禁全部通过。阶段 A 没有运行材料优化，也没有修改 GUI
  runtime。

复验命令：

```bash
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/freeze_super_tissue_calibration_inputs.py --verify-only
```

### 阶段 B：生成组织表面观测

优先使用同步双目数据生成每帧组织表面点云：

1. 分割组织并剔除器械遮挡；
2. 使用双目深度恢复可见表面；
3. 统一到 GUI/物理世界坐标；
4. 对点云降采样并移除离群点；
5. 在夹取点附近补充少量可跟踪表面关键点。

如果只有单目观测，则改用轮廓、关键点和光流损失；此时只能标定视觉表现相似的相对柔性，不能可靠恢复绝对三维刚度。

#### 阶段 B 实施结果（2026-07-30）

本阶段已生成完整双目组织表面观测，没有启用视觉力、残差映射或材料优化。
新增：

```text
scripts/build_super_tissue_surface_observations.py
scripts/postfilter_super_tissue_surface_observations.py
scripts/build_super_tissue_keypoint_tracks.py
scripts/validate_super_tissue_surface_observations.py
```

正式输出：

```text
data/super/tissue_calibration_v1/stage_b_surface_observations/
data/super/tissue_calibration_v1/stage_b_dense_baseline/
data/super/tissue_calibration_v1/stage_b_lr_stratified_audit/
data/super/tissue_calibration_v1/stage_b_validation_report.json
```

完整首版采用以下分工：

1. 对阶段 A 的全部 `1433` 个合格左帧运行 FoundationStereo，生成左参考系
   三维表面；
2. 使用左组织 mask 的 `2 px` 腐蚀和器械 mask 的 `30 px` 膨胀移除边缘及
   器械遮挡；
3. 根据标定视差把每个三维点重投影到右目，只保留位于右目组织且不在右目
   器械 mask 内的点；
4. 统一变换到 table/world 坐标，以 `1 mm` voxel 降采样并做统计离群点剔除；
5. 对覆盖全时段的 `64` 个等距样本加全部操作关键时刻，共 `69` 帧，额外运行
   FoundationStereo 反向推理和 `1.5 px` 左右一致性门禁；
6. 在 `270..1300` 帧夹取窗口内进行前后向 LK 跟踪，并把二维轨迹关联到正式
   三维表面。

这里没有把昂贵的反向网络推理重复到每一帧：论文方法需要完整逐帧可见表面，
左右一致性属于观测质量审计而不是材料标定变量。正式表面仍覆盖全部 `1433`
帧，分层审计覆盖完整时间范围及所有关键操作时刻。

结果：

- 正式表面 `1433/1433` 帧完成，无缺失、无失败；每帧 `4749..5597` 点；
- 右目语义重投影保留率为 `90.03%..94.07%`；
- `69/69` 帧左右一致性审计通过；每帧 `4751..5487` 点，有效可见比例
  `92.73%..96.48%`，保存点的左右误差均不超过 `1.5 px`；
- 选出 `32` 条夹取区长 tracklet，长度 `731 / 896 / 1031`
  （min/median/max）帧；`28428` 个观测中 `27521` 个关联到三维表面，
  三维关联率 `96.81%`；
- 全部 `1433` 帧正式表面相对稠密基线的双向 Chamfer p95 最大值为
  `0.554 mm`；关键时刻正式表面相对左右审计的 Chamfer p95 最大值为
  `0.240 mm`；
- 最大回撤相对初始表面抬升 `9.298 mm`，释放下降 `9.523 mm`，回弹参考帧
  与初始最大高度相差 `0.429 mm`；
- 初始观测到冻结 rest top surface 的距离 p95 为 `0.743 mm`；
- 统一验收 `17/17` 门禁通过，逐帧异常列表为空。

复验：

```bash
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/postfilter_super_tissue_surface_observations.py
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/build_super_tissue_keypoint_tracks.py
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/validate_super_tissue_surface_observations.py
```

### 阶段 C：建立无视觉力的确定性离线回放

回放条件：

```text
器械位姿                              使用冻结后的视频绝对位姿
夹爪接触                              开启
轴杆接触                              关闭
Gaussian—四面体绑定                   保留
视觉力                                关闭
逐帧深度残差位移                      关闭
在线刚度优化                          关闭
夹爪捕获/随动约束                     关闭
```

同一输入重复运行必须得到一致的组织粒子轨迹、接触统计和渲染结果。

#### 阶段 C 实施结果（2026-07-30）

本阶段已完成冻结全序列的真实物理回放，不是只跑关键帧 smoke。新增：

```text
scripts/replay_super_tissue_calibration_stage_c.py
scripts/validate_super_tissue_calibration_stage_c.py
```

正式输出：

```text
data/super/tissue_calibration_v1/stage_c_offline_replay/
├── particle_positions.npy
├── gaussian_means.npy
├── gaussian_quats.npy
├── trajectory_metrics.npz
├── landmark_renders/
├── stage_c_replay_report.json
└── stage_c_artifact_validation.json
```

回放严格读取阶段 A manifest：

- 覆盖左视频 `0..1440` 全部 `1441` 帧；
- 使用固定 `60 Hz` 物理时钟和 `12` 个子步，共推进 `2888` 个物理步；
- 每个物理目标时刻对 `5458` 状态的冻结 pose driver 做
  `searchsorted(..., side="right") - 1` ZOH；
- 使用冻结人工修正
  `-9.184 deg / +0.332 mm / 0 mm / -6.735 mm / 0 deg`；
- 只启用两片夹爪的 triangle-skin 接触；轴杆、粒子—shape 接触、粒子—粒子
  接触和持久夹取/随动约束均关闭；
- `compute_visual_forces=False`，`frames=None`，视觉迭代为 `0`；
- 深度 residual、在线 stiffness update 均关闭；
- 保留 `2863` 个组织 Gaussian 的四粒子 tetra 绑定，并在每个物理步后更新。

原 CUDA 求解器的材料 Jacobi 和夹爪接触使用浮点原子加法。无接触段可逐字节
重复，但接触后原子执行顺序会令两次轨迹逐渐分叉。阶段 C 没有用宽松容差掩盖
这一问题，也没有采用会改变物理状态的运动量化；现改为：

1. 每个四面体先写四个独立角点修正，再按预计算的
   `particle -> sorted tet corner` 顺序归约；
2. 每个 jaw surface sample 先写三个独立重心修正，再按唯一的
   `particle id + contribution id` key 排序并固定顺序归约。

材料方程、接触修正、摩擦、边界条件和器械位姿均不变。正式序列连续运行两次，
结果：

- `37570` 个组织粒子的全部 `1441` 帧位置逐元素完全相同；
- 粒子逐帧 SHA256 不匹配 `0`，最大绝对误差 `0 m`；
- 全部接触整数统计不匹配 `0`，浮点统计最大误差 `0`；
- `8318` 个场景 Gaussian 的 mean/quaternion 逐帧不匹配 `0`，最大误差 `0`；
- 7 个冻结关键时刻的真实 Gaussian float32 与 uint8 渲染哈希均完全相同；
- 全序列粒子有限、翻转四面体最大数 `0`、固定边界最大漂移 `0 m`；
- 最大动态位移 `6.762 mm`，最大速度 `0.190 m/s`；
- 接触活跃 `435` 帧，首次活跃帧 `184`，末次活跃帧 `1284`；
- 运行报告 `23/23` 门禁通过；独立离线产物校验全部通过。

完整轨迹约 `940 MiB`，其中粒子位置 `620 MiB`、Gaussian means `138 MiB`、
Gaussian quaternions `183 MiB`。复验保存产物无需 GPU：

```bash
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/validate_super_tissue_calibration_stage_c.py
```

重新生成完整双次回放需要 CUDA：

```bash
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/replay_super_tissue_calibration_stage_c.py
```

当前 `E=50 Pa, damping=30/s` 只是进入阶段 D 前的固定初值，不是标定结果。
它在夹爪闭合后达到约 `6.762 mm` 形变，随后形变基本保留，且牵拉参考帧已经
没有接触；这与阶段 B 观测到的继续牵拉、释放下降和回弹不一致。该偏差已被
确定性地暴露出来，下一阶段应只搜索全局杨氏模量和阻尼，不能重新打开视觉力
或修改器械位姿来吸收误差。

### 阶段 D：标定低维全局材料参数

第一轮只优化：

- 全局杨氏模量或等价 XPBD compliance；
- 速度阻尼。

第一轮固定：

- 泊松比或体积约束；
- 夹爪摩擦；
- 接触间隙和接触求解参数；
- 组织固定边界；
- 器械位姿。

先进行对数尺度的粗搜索，再在较优区间局部搜索。禁止第一轮直接优化每粒子刚度。

建议损失：

```text
L_total =
    w_surface  * 可见表面点云/深度误差
  + w_keypoint * 组织关键点轨迹误差
  + w_temporal * 速度、回弹和时间连续性误差
  + w_physics  * 翻转、异常体积和异常位移惩罚
```

视频必须按连续时间拆分为标定段、验证段和测试段，不能随机拆帧。验证和测试时关闭残差映射与视觉力，检查材料模型是否真正具有未来预测能力。

#### 阶段 D 实施结果（2026-07-30）

本阶段已完成第一版全局材料标定，不是单候选或短时 smoke。新增：

```text
scripts/calibrate_super_tissue_material_stage_d.py
scripts/validate_super_tissue_material_stage_d.py
```

正式输出：

```text
data/super/tissue_calibration_v1/stage_d_material_calibration/
├── calibrated_material.json
├── stage_d_calibration_report.json
├── stage_d_artifact_validation.json
└── candidate_reports/                 # 26 次独立回放报告
```

整个搜索只覆盖全局杨氏模量与速度阻尼。泊松比 `0`、重力 `0`、夹爪摩擦、
contact margin/query/solver、固定边界、PSM 轨迹和人工修正全部保持阶段 A
冻结值；视觉迭代、逐帧 depth residual 和在线 stiffness optimization 始终为
`0/OFF/OFF`。

连续时间划分为：

```text
标定段       270..906
验证段       907..1276
测试段       1277..1439
```

没有随机拆帧。候选排序只读取标定段；验证段和测试段只在标定排名前三已经选定
后做未来预测报告，不能反向参与选参。损失使用：

```text
0.70 * 12 条 first-contact 长轨迹的鲁棒相对位移 RMSE
+ 0.30 * 对运动区域加权的可见表面单向鲁棒距离
+ 非有限、翻转或边界漂移的硬物理惩罚
```

两张 A800 并行完成 `13` 个对数粗搜候选和围绕粗搜最优的 `8` 个局部候选；
然后对标定排名前三做完整未来段回放，并对最终参数再做两次 `0..1439`
逐帧稳定性/哈希复验，共 `26` 次正式回放。结果：

```text
冻结初值                              E=50 Pa, damping=30/s
标定结果                              E=100 Pa, damping=5/s
标定目标                              1.872780 mm
未来验证目标                          2.667120 mm
未来测试目标                          1.287354 mm
初值标定目标                          1.931738 mm
相对初值改进                          3.052 %
逐帧稳定性检查                        1440 / 1440，两次
最大翻转四面体                        0
最小四面体体积比                      9.8693e-5，保持为正
最大固定边界漂移                      0 m
最大动态位移                          5.904 mm
最大动态速度                          0.320 m/s
两次标定/验证/测试指标差异             0
关键时刻与末帧粒子状态哈希差异          0
独立产物门禁                          16 / 16 PASS
```

`E=100 Pa, damping=5/s` 是严格按标定段选出的第一版固定材料结果；不能因为另一
候选在测试段偶然更低而改选，否则会泄漏未来数据。另一方面，最优相对初值只改善
约 `3.05%`，且多个相距较远的候选仍在最优的 `10%` 范围内，说明仅靠一个全局
杨氏模量和一个阻尼不能唯一解释所有局部夹持、牵拉和释放形态。阶段 D 因此给出
了可复现的全局物理基线，但没有把低置信度伪装成唯一真实材料值；下一阶段应固定
这两个数并进入可见表面 residual mapping，不能继续靠修改器械位姿或接触参数
追逐剩余误差。

复验已有产物无需 CUDA：

```bash
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/validate_super_tissue_material_stage_d.py
```

### 阶段 E：加入论文式残差映射

在全局材料参数已有合理初值后：

1. 计算模拟表面与视频点云的残差；
2. 数据项只作用于可见组织表面；
3. 使用四面体材料能量保持内部连续性；
4. 加入时间历史一致性和空间平滑；
5. 将残差用于评估和优化材料参数，而不是永久写入每帧组织位置。

需要分别保存：

- 物理预测状态；
- 残差修正状态；
- 残差大小和空间分布。

如果残差集中跟随器械移动，优先检查器械位姿和接触；如果接触位置正确但整体变形幅度、传播范围或回弹速度不对，再判断为材料参数问题。

#### 阶段 E2：Gaussian 视觉残差首版（2026-08-08）

论文体积组织使用可见表面粒子与双目点云的对称 Chamfer 作为数据项，并把 PBD
约束能量加入同一个 \(\Delta x\) 求解。本项目首版保持这一结构，只把数据项换成左右目
masked Gaussian RGB Smooth-L1；优化物理节点残差，再通过视觉面绑定重建 Gaussian，
不再把独立 Gaussian target 直接解释为残差状态。

新增核心、合成门禁和真实帧门禁：

```text
src/embodied_gaussians/physics_simulator/visual_tissue_residual_mapping.py
scripts/test_super_visual_tissue_residual_mapping.py
scripts/smoke_super_visual_tissue_residual_real_frame.py
EmbodiedGaussiansSimulator.solve_visual_tissue_residual()
```

当前选中的强度档：

```text
image scale / iterations                0.25 / 4
particle-residual learning rate         2.5e-5 m
per-particle hard cap                    0.25 mm
frame 420 max / RMS residual             0.170 / 0.032 mm
visual loss                              0.02412754 -> 0.02261484 (-6.27%)
minimum tet volume ratio                 0.458
inverted tetrahedra / backtracks         0 / 0
organization-only cold / warm solve      627 / 68.7 ms
```

该结果比首轮 `1 iteration, 2e-5 m` 的 `0.0346 mm` 修正明显更强。5 次迭代也可达到
`0.170 mm`，但 warm 求解约 `73.5 ms`，因此拒绝作为当前实时候选。

当前已同步接入 GUI：XPBD 后求物理节点增量，loss 下降且没有新增翻转才只回写
`particle_q`，速度不变。每 4 个物理帧更新一次。当前强档为 6 次迭代、`3e-5 m`
学习率和 `0.40 mm` 单次上限；frame 420 最大/RMS 为 `0.304/0.048 mm`、loss 下降
`9.18%`，warm 约 `92 ms`。体积门禁对健康 tet 保持 `0.30`，对进入修正前已低于
阈值的 tet 只允许最多 `2%` 相对恶化；违规时先局部清零相关节点残差，最多 4 轮后才
全局回溯，避免一个预压缩 tet 让全局修正归零。残差反向渲染只用组织的 `26,754` 个
Gaussian，GUI 正向显示仍包含完整器械。online stiffness update 仍关闭。

### 阶段 F：可选的局部刚度优化

只有全局模型在未参与标定的帧上仍存在稳定、重复的空间误差时才启用。

第一版最多划分少量区域：

- 夹取主要区域；
- 周围过渡区域；
- 远端或固定边界区域。

优化 2–4 个区域参数，并加入强空间平滑。暂不使用数万个独立粒子刚度，避免单段视频过拟合。

### 阶段 G：导出标定组织

建议输出独立的标定配置：

```text
tissue_calibrated_profile
├── 输入数据和代码版本
├── 器械 pose driver 与人工修正
├── 全局材料参数
├── 阻尼
├── 固定的接触参数
├── 边界条件
├── 可选区域刚度
├── 四面体/Gaussian 资产哈希
└── 标定、验证和测试指标
```

GUI 只加载最终冻结的参数，不加载优化器状态或离线逐帧残差。

### 阶段 H：在 GUI 中重新加入视觉力

GUI 每帧顺序：

```text
视频器械位姿驱动夹爪
        ↓
夹爪—组织表面接触
        ↓
标定后的物理模型产生主要形变
        ↓
Gaussian 随四面体运动并渲染
        ↓
计算组织视觉残差
        ↓
有上限的局部视觉力散射回物理粒子
```

视觉力只修正剩余局部状态，不参与材料参数更新。必须保留以下三种可切换模式：

1. 仅物理；
2. 标定物理但无视觉力；
3. 标定物理并开启视觉力。

## 6. 验收标准

离线标定至少检查：

- 未参与优化的连续视频段上，组织表面和关键点仍能合理预测；
- 关闭视觉力后，组织已经呈现主要的压入、牵拉和回弹；
- 器械位姿不被物理求解器修改；
- 没有持续增加的翻转四面体；
- 接触过程中没有因查询范围而突然失去接触；
- 局部夹取不会引起不合理的大范围刚体式联动；
- 释放后组织能以合理速度恢复，而不是继续漂移或振荡。

GUI 集成后检查：

- Gaussian 始终保留并与物理组织一致运动；
- 视觉力能降低局部表面误差；
- 视觉力关闭后模型仍然基本成立；
- 视觉力不会把组织或 Gaussian 拉飞；
- 器械只通过两片夹爪与组织接触；
- 同一标定配置能够被离线回放和 GUI 无歧义地重复加载。

## 7. 第一版完整实施范围

第一版完成以下完整闭环，不以少量帧或最小可行性 smoke 替代全序列产物：

1. 冻结当前人工器械位姿修正；
2. 从夹取视频生成组织表面观测；
3. 关闭视觉力，建立确定性回放；
4. 离线标定一个全局柔软度和一个阻尼参数；
5. 在视频后半段验证；
6. 导出固定组织配置并放回 GUI；
7. 最后开启有上限的视觉力。

完成这一版后，再根据验证残差决定是否值得增加论文式局部刚度场。第一版不直接进行逐粒子刚度优化。

阶段 E/F 已完成：双向可见表面 residual mapping 与时间历史在 1433 个观测帧上
将平均表面 RMSE 从 `0.804240` 降至 `0.559477 mm`（改善 `30.43%`），P95
改善 `35.59%`；两次回放 byte-exact、无翻转单元。阶段 F 由三段持续残差生成
平滑三分区并回放 5 个候选，选中 primary=`1.5`、transition=`0.75`、far=`1.0`，
四面体模量约 `95.99..149.99 Pa`。产物位于 `data/super/tissue_calibration_v1/`
下的 `stage_e_residual_mapping/` 与 `stage_f_local_stiffness/`。

误差和冻结参数已汇总导出到 `stage_g_calibrated_profile/`，下一步是把这份配置
加载到 GUI，分别测试视觉力关闭与开启的在线 A/B 结果。

GUI 接入已完成：`examples/example_embodied_super_offline.py` 默认加载
`stage_g_calibrated_profile/tissue_calibrated_profile.json`，应用全局材料、阻尼
和三分区四面体刚度；残差修正与在线视觉力默认关闭，保持离线验证时的物理状态。
可用 `--calibrated-profile` 指定其他冻结配置。

## 8. 受重力观测形状与材料参考形状必须分离（2026-08-02）

当前双目重建组织是放置在平面、固定一侧并已经受重力后的观测平衡形状，记作
`X_obs`。不能再把 `X_obs` 同时作为零应力材料 rest shape 并直接施加地球重力；
这种重复加载在当前 `E=50 Pa, nu=0` 的可压缩基线中会迅速把组织压扁。实测
20 个完整物理步后自由节点平均向下约 `4.14 mm`，最低四面体体积比接近
`1e-4`。

最终应同时保存三套相互独立但拓扑一致的数据：

- `initial_positions_table = X_obs`：GUI、reset 和碰撞表面的初始位置；
- `material_rest_positions_table = X_rest`：逆静力求出的无载荷材料参考形状；
- `skinning_reference_positions = X_obs`：Gaussian 初始外观和蒙皮参考。

GUI 从 `X_obs` 启动，四面体材料形变梯度使用 `X_rest` 的 inverse rest matrix。
初始时 `F != I`，由此产生的材料预应力与 `M g`、地面反力和固定边界反力平衡；
组织被夹起后地面反力消失，但正常向下重力仍然存在。不能用持久保存 XPBD
拉格朗日乘子代替该材料预应变，因为当前求解器每个 substep 会清空材料乘子。

离线求解采用“逆重力种子 + 正向平衡迭代”：先从 `X_obs` 在反向重力下静置
得到 `X_rest` 初值，再以 `X_obs` 为初始状态、`X_rest` 为材料参考，在正常重力
下正向静置得到 `X_eq`，按 `X_obs-X_eq` 迭代修正 `X_rest`。每次更新固定锚点、
做四面体邻域平滑，并用正体积屏障和线搜索防止 rest tetra 翻转。目标门禁是
正常重力、无器械控制下表面 P95 漂移小于 `0.1 mm`、最大漂移小于 `0.3 mm`、
固定点漂移为零且没有翻转单元。

单个静态观测不能同时唯一确定 `X_rest` 与材料刚度；预应变依赖 `E`、泊松比或
体积约束、质量、固定边界和地面接触。当前先保持 `paper_soft gravity=0`，作为
已受重力观测形状的补偿基准。可以用冻结的临时材料参数生成一版预应力资产，
但完成全局/局部刚度标定后必须重新逆求最终 `X_rest`。组织的三角面、四面体
拓扑和 Gaussian 绑定不需要从头生成。
