# 全局可观测 MHE 刚度更新原型（2026-09-01）

## 目的

当前 `differentiable_global_relative` 在最近 3 帧上用 Adam 同时更新全局 paper-PBD distance 刚度与速度阻尼。短窗口内两者对轨迹的影响容易同向，导致参数不可辨识：梯度方向即使稳定，也不表示刚度与阻尼能被分别确定。

本原型新增 `differentiable_global_mhe`。旧模式和既有 A/B/C 结果均不改动。

## 参数与固定项

只估计两个无量纲 log 修正量：

\[
\theta=
\begin{bmatrix}
\theta_k\\
\theta_d
\end{bmatrix},\qquad
k_d=k_{d,0}e^{\theta_k},\qquad
d=d_0e^{\theta_d}.
\]

- `paper_distance_stiffness` 与速度阻尼参与更新；
- `paper_volume_stiffness`、`paper_shape_stiffness` 固定；
- 已知夹持区域轨迹作为三组方法统一的运动边界，耦合系数严格固定为 1；
- 这里的 `distance stiffness` 是项目原生 paper-PBD 参数，不报告成 Young 模量；
- 不估计局部刚度图，不使用仿真器刚度真值进行更新或选参。

## 因果历史窗口

保留最近 20 个已经发生的转移。7:1 重建协议中的留出帧会把历史分成若干连续段，每段从当时在线可获得的真实状态开始，段内开放环前向：

\[
\hat q_{t+h}=F_\theta^{(h)}(q_t,\dot q_t,u_{t:t+h}).
\]

这是一种多重射击（multi-shooting）移动窗口。留出造成的不连续不会清空全部材料证据，但不同连续段也不会错误串接。窗口只包含当前帧及过去帧，不读取未来 20% 数据。

## 残差

对 CoTracker/FoundationStereo 观测，使用绝对轨迹残差和邻接轨迹相对残差：

\[
r_i^{abs}=\frac{\hat x_i-x_i^{obs}}{s_x},\qquad
r_{ij}^{rel}=\frac{(\hat x_i-\hat x_j)-(x_i^{obs}-x_j^{obs})}{\sqrt 2s_x}.
\]

同时保留不读取 GT 深度的边应变信号：

\[
r_e^{strain}=
\frac{\|\hat q_a-\hat q_b\|-\|q_a^{obs}-q_b^{obs}\|}
{\ell_e^0s_\varepsilon}.
\]

三类残差沿用现有权重：轨迹 0.8、边应变 0.2；轨迹中绝对项 0.35、相对项 0.65。每个块按有效残差数量归一化，较长的段内预测间隔权重从 1.5 逐渐增加并在 3.0 截止。

## Warp 雅可比与可观测性门

直接重放实际 Warp paper-PBD，不用另一个 Torch 近似求解器代替前向模型。中心有限差分为：

\[
J_j\approx\frac{r(\theta+\epsilon e_j)-r(\theta-\epsilon e_j)}{2\epsilon},
\qquad \epsilon=0.005.
\]

另外用 `2ε` 再计算一套雅可比。两套梯度方向余弦必须不低于 0.95，避免数值差分不稳定。

对 Cauchy 权重后的数据雅可比做 SVD：

\[
J_w=U\Sigma V^T,qquad
\rho=\frac{\sigma_{min}}{\sigma_{max}}.
\]

只有同时满足下列条件才更新：

- 至少积累 8 个转移；
- \(\|J_w\|_F\ge 10^{-4}\)；
- \(\rho\ge0.02\)，即刚度与阻尼方向不是近乎共线；
- `ε` 与 `2ε` 梯度余弦不低于 0.95；
- 每 5 帧至多尝试一次。

不满足时保持原参数继续运行，不暂停视频，也不选择另一条仿真分支。

## 鲁棒 LM 更新

当前残差处固定 Cauchy IRLS 权重：

\[
w_i=\frac{1}{\sqrt{1+(r_i/c)^2}},\qquad c=2.5.
\]

令 \(\tilde r=Wr,\tilde J=WJ\)，单次更新为：

\[
\Delta\theta=-\left(
\tilde J^T\tilde J+\lambda_pI+
\lambda_{LM}\operatorname{diag}(\tilde J^T\tilde J+\lambda_pI)
\right)^{-1}
\left(\tilde J^T\tilde r+\lambda_p\theta\right).
\]

当前 `λLM=0.05`、`λp=0.05`，每次两个 log 参数的变化均限制为 `±0.02`。这是单一连续更新规则，不做候选分支选择，也不根据评估 GT 调参。

## 已完成验证

CPU 数值门测试：

```bash
PYTHONPATH=src /Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/test_sim_observable_global_mhe.py
```

结果：

- 可区分的合成雅可比奇异值比为 `0.9134`，允许更新；
- 刚度列与阻尼列近乎共线时正确拒绝；
- `ε` 与 `2ε` 方向相反时正确拒绝；
- shape、volume、夹持耦合没有被更新；
- 原有全局 Adam 与低维更新回归测试通过。

## 正式轨迹探针

```bash
bash scripts/run_sim_foundation_observable_mhe_probe.sh
```

该脚本不覆盖已有输出，结果写入：

`outputs/sim_foundation_observable_mhe_probe_v1/`

它会运行重建 7:1 和未来 80/20 两组 C-MHE，并复用已冻结的 A/B 轨迹结果。先比较 3D Tracking Error (mm) 与 2D Tracking Error (px)，只有 C-MHE 确认优于 B 后才值得启动包含 PSNR/SSIM/LPIPS 的完整渲染测评。
