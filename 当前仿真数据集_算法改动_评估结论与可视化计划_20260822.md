# 当前仿真数据集、算法改动、评估结论与可视化计划

更新时间：2026-08-22

## 1. 当前状态和核心结论

两套专属组织牵拉数据集、GUI/重建资产以及三组消融评估均已完成。两套任务都没有人为硬固定区域，使用相同组织几何、纹理、相机、十区域材料分布和统一初始仿真参数，只改变抓取位置与牵拉方向。

当前最可靠的结论如下。

1. `PBD + RGB 视觉残差` 相比纯 PBD 明显降低了 2D/3D 轨迹误差，尤其是后 20% 不读取未来图像的开环预测。
2. `PBD + RGB 视觉残差 + 在线刚度修正` 在两套任务的 2D/3D 轨迹指标上均继续优于第二组，说明刚度确实发生了更新，并改善了未来形变预测。
3. 刚度修正相对视觉残差的轨迹提升目前约为 1%–5%，是稳定但不算“远远领先”的提升。
4. 渲染指标没有在所有任务上保持第三组严格最优：侧向未来预测略有改善，正面未来预测略有退化。因此当前不能声称在线刚度对 PSNR/SSIM/LPIPS 有一致显著提升。
5. 所有组都使用预先固定的统一初值 `distance=0.20、shape=0.004`，没有按数据集挑选初值，没有分支选择，也没有利用未来帧选择参数。

最终总表位于：

- `outputs/two_free_support_ablation_v2/两个任务总表.md`
- `outputs/two_free_support_ablation_v2/front_pull/comparison.md`
- `outputs/two_free_support_ablation_v2/side_pull/comparison.md`

## 2. 已完成的工作

所有改动都位于复制项目 `embodied_gaussians_fixed_super_best_sim`，没有修改原始 `Embodied_gaussians_fixed_super_best`。

### 2.1 仿真和数据集

- 使用 `eg_sim` 环境运行 Isaac/Orbit-Surgical 场景生成；使用 `eg_codex` 环境运行当前项目、GUI 资产构建与评估。
- 使用官方 PSM 视觉资产和组织原生纹理，构造近景、高分辨率、双目组织牵拉场景。
- 建立正面牵拉和侧向牵拉两个 300 帧任务。
- 去除右上角或边缘的硬固定：`support_mode=free`，`hard_anchor_nodes=0`，GUI 中 `fixed_mask` 也为全零。
- 设置十个平滑过渡材料区域；杨氏模量为固定随机种子生成的约 `786–1012 Pa`，泊松比为 `0.45`。这些数值只用于产生仿真真值，重建算法不会读取。
- 保存准确双目 RGB、深度、语义 mask、相机参数、PSM 位姿、组织节点状态以及 2D/3D 真值轨迹。
- 保存 50 个视角的首帧全视角扫描，用于无死角首帧组织建模。
- 将夹持边界限制为红色区域附近的 5 个重建粒子；夹持区域附近 9 个组织真值点从轨迹评估中排除，避免已知边界直接降低误差。
- 修正侧向任务 PSM pitch 符号。修正后闭合尖端相对红色抓取中心的偏差约为 `(-1.72, -1.31, 7.34) mm`，通过数据集门禁。

### 2.2 GUI 和重建资产

每套数据集均已生成：

- 2,658 个四面体重建粒子；
- 7,602 个四面体；
- 5,312 个表面三角形；
- 36,300 个绑定到四面体/表面的 Gaussian；
- 5 点红色夹持边界轨迹；
- 官方 PSM 末端网格和逐帧器械 mask。

Gaussian 使用重心坐标绑定在四面体/表面上，物理粒子运动后会带动 Gaussian。除 5 点夹持边界外，其余组织形变由 PBD、RGB 视觉残差和在线刚度更新共同决定。

### 2.3 RGB 视觉残差和在线刚度

三组方法为：

| 组别 | 当前项目中的含义 | 对应 Liang 等人的命名 |
|---|---|---|
| PBD | 统一初始参数的原始 PBD | PBD |
| PBD + 视觉残差 | PBD 状态加 RGB 残差修正 | PBD-RM |
| PBD + 视觉残差 + 刚度修正 | RGB 残差修正并在线更新局部刚度 | PBD-RM-ON |

视觉残差设置：

- 输入只有双目 RGB、相机参数和已知五点夹持边界，不使用深度、轨迹真值或材料真值；
- 每帧 8 次 Adam 迭代；
- 学习率 `4e-5 m`；
- 单次允许残差上限 `0.6 mm`；
- RGB 对齐诊断分辨率比例 `0.25`；
- 到第 240 帧时完全冻结视觉残差，后 60 帧只开环预测。

刚度更新设置：

- 在原视觉残差信号上增加局部边应变信号，权重 `0.80`；
- 对局部更新做时间 EMA，衰减 `0.60`；
- 空间平滑 2 次，每次混合系数 `0.30`；
- log-domain 学习率 `0.30`，每次 log 步长上限 `0.10`；
- distance 范围 `[0.1, 2.0]`，shape 范围 `[0.003, 0.02]`；
- 当前观测帧完成更新后只影响后续物理步，是严格因果更新；
- 没有 shadow branch、line search、未来观测验证或参数分支选择；
- 第 240 帧冻结已经学到的局部材料场，未来 20% 禁止继续更新。

实际更新统计：

| 任务 | 候选更新 | 成功提交 | 拒绝 |
|---|---:|---:|---:|
| 正面牵拉 | 138 | 137 | 1 |
| 侧面牵拉 | 136 | 135 | 1 |

因此“刚度没有更新”已经不再是当前实验的问题。

主要改动文件：

- `scripts/precompute_sufia_tissue_trajectory.py`
- `scripts/generate_sim_tissue_retraction_dataset.py`
- `scripts/prepare_sim_tissue_gui_asset.py`
- `scripts/prepare_sim_red_marker_boundary.py`
- `scripts/validate_sim_dataset.py`
- `scripts/run_two_free_support_datasets_and_evaluation.sh`
- `scripts/run_sim_ablation_evaluation.sh`
- `scripts/summarize_dual_sim_evaluation.py`
- `src/embodied_gaussians/physics_simulator/online_tissue_stiffness.py`
- `src/embodied_gaussians/physics_simulator/sim_benchmark.py`
- `examples/example_embodied_super_offline.py`
- `评估协议_两套牵拉与SUPER.md`

## 3. 当前使用的数据集

### 3.1 正面牵拉

路径：`data/sim/tissue_retraction_free_support_front_v2`

- 大小约 7.4 GB；
- 300 帧，30 FPS，时长约 10 秒；
- 双目分辨率均为 1536×1536；
- 组织最大位移约 14.27 mm；
- 抓取核心真值节点 14 个，平滑传递节点 660 个；
- 无硬固定节点；
- 数据集完整验证：通过。

### 3.2 侧向牵拉

路径：`data/sim/tissue_retraction_free_support_side_v2`

- 大小约 7.4 GB；
- 300 帧，30 FPS，时长约 10 秒；
- 双目分辨率均为 1536×1536；
- 组织最大位移约 14.27 mm；
- 抓取核心真值节点 14 个，平滑传递节点 654 个；
- 无硬固定节点；
- 数据集完整验证：通过。

早期 PSM 方向错误的数据保留在：

`data/sim/tissue_retraction_free_support_side_v2_misaligned_psm_20260822`

该备份不参与当前 GUI、重建或评估。

### 3.3 数据内容和信息隔离

重建允许读取：

- `videos/` 中双目 RGB 视频；
- 相机内外参和时间戳；
- `task_inputs/` 中 PSM 状态、阶段信息、器械 mask 和 5 点夹持边界；
- `gui_assets/tissue_fixedsuperbest.npz` 首帧重建资产。

只允许评估读取：

- `ground_truth/trajectories_3d.npz`：899 个组织表面点的逐帧 3D 真值，其中固定 240 个为评估点；
- `ground_truth/trajectories_2d/stereo_left.npz` 和 `stereo_right.npz`：逐帧 2D 投影、可见性和有效性；
- `ground_truth/tissue_state.npz`：完整组织节点状态；
- `ground_truth/material.json` 及逐节点/四面体杨氏模量；
- 深度和语义 mask 真值。

区域刚度和深度不会被传给视觉残差或刚度优化。

## 4. 评估协议

- 三组使用完全相同的数据、首帧模型、夹持边界、相机、初始刚度和物理步数。
- 每帧 3 个 PBD 物理步。
- 前 240 帧允许相应方法使用 RGB 同化和在线刚度更新。
- 在索引 240 进入未来段，冻结视觉残差和材料参数。
- 后 60 帧仍输入已知的 PSM/五点边界控制，但不读取任何未来 RGB、深度或轨迹真值。
- 2D/3D 轨迹采用首帧一次性重心映射，后续不做真值配准。
- 主渲染指标只比较组织 Gaussian 层：PSNR、SSIM、LPIPS。

## 5. 最终实验结果

轨迹误差和 LPIPS 越低越好；PSNR、SSIM 越高越好。

| 任务 | 方法 | 区间 | 3D Mean mm ↓ | 2D Mean px ↓ | PSNR dB ↑ | SSIM ↑ | LPIPS ↓ |
|---|---|---|---:|---:|---:|---:|---:|
| 正面 | PBD | 前 80% | 0.707 | 7.125 | 19.835 | 0.8447 | 0.4132 |
| 正面 | PBD | 后 20% | 4.118 | 40.792 | 16.431 | 0.8149 | 0.4529 |
| 正面 | PBD + 视觉残差 | 前 80% | 0.669 | 6.226 | 19.712 | 0.8471 | 0.4147 |
| 正面 | PBD + 视觉残差 | 后 20% | 2.775 | 22.991 | **17.642** | **0.8279** | **0.4362** |
| 正面 | PBD + 视觉残差 + 刚度修正 | 前 80% | **0.660** | **6.168** | 19.739 | 0.8474 | 0.4143 |
| 正面 | PBD + 视觉残差 + 刚度修正 | 后 20% | **2.678** | **22.714** | 17.516 | 0.8264 | 0.4397 |
| 侧面 | PBD | 前 80% | 0.727 | 6.664 | 16.703 | 0.8222 | 0.4225 |
| 侧面 | PBD | 后 20% | 3.894 | 34.481 | 15.694 | 0.8025 | 0.4497 |
| 侧面 | PBD + 视觉残差 | 前 80% | 0.722 | 6.678 | **16.750** | **0.8250** | **0.4226** |
| 侧面 | PBD + 视觉残差 | 后 20% | 2.852 | 25.401 | 16.007 | 0.8075 | 0.4443 |
| 侧面 | PBD + 视觉残差 + 刚度修正 | 前 80% | **0.702** | **6.611** | 16.726 | 0.8243 | 0.4231 |
| 侧面 | PBD + 视觉残差 + 刚度修正 | 后 20% | **2.711** | **24.627** | **16.116** | **0.8083** | **0.4429** |

量化结论：

- 正面未来段：视觉残差相对 PBD 将 3D/2D Mean 分别降低 `32.6%/43.6%`；刚度修正相对视觉残差再降低 `3.5%/1.2%`。
- 侧面未来段：视觉残差相对 PBD 将 3D/2D Mean 分别降低 `26.8%/26.3%`；刚度修正相对视觉残差再降低 `5.0%/3.0%`。
- 侧面未来段第三组的三项渲染指标都略优于第二组；正面未来段则略差于第二组。
- 因此当前最强证据是“视觉残差显著改善轨迹，在线刚度进一步稳定改善未来轨迹”，不是“在线刚度显著改善全部渲染指标”。

## 6. 下一步可视化计划

参考论文：Xiao Liang 等，*Real-to-Sim Deformable Object Manipulation: Optimizing Physics Models with Residual Mappings for Robotic Surgery*，arXiv:2309.11656，尤其是 Fig. 7–9：<https://arxiv.org/abs/2309.11656>。

### 6.1 关键点 2D 真值运动轨迹

目标：直观展示两套仿真任务在左右目中的真实组织运动，而不是只给平均误差。

固定做法：

1. 只从 240 个预定义评估点中选择关键点，排除 5 点控制区及其 9 个邻近评估点。
2. 关键点选择只使用首帧位置和可见性，不使用任何方法的未来误差，避免挑选“对第三组有利”的点。
3. 在满足大部分帧可见的候选中，对首帧 3D 坐标做确定性的 farthest-point sampling，固定选择 12 个点；将点 ID 保存到 `selected_keypoints.json`，两种方法比较时绝不改变。
4. 左右目分别以参考 RGB 为背景，使用相同颜色表示相同点，绘制完整 300 帧真值轨迹；标注起点、终点和运动方向。
5. 另加稀疏时间标记 `0、60、120、180、240、299`，避免轨迹线难以辨认时间方向。

输入：

- `ground_truth/trajectories_2d/stereo_left.npz`
- `ground_truth/trajectories_2d/stereo_right.npz`
- 对应 RGB 帧和 `tissue_visible`

### 6.2 关键点 3D 真值运动轨迹

固定使用与 2D 图完全相同的 12 个 keypoint ID。

- 将首帧组织表面绘制为半透明浅灰网格；
- 以毫米为单位绘制 12 条世界坐标轨迹；
- X/Y/Z 使用等比例坐标轴，不能为视觉效果单独拉伸深度；
- 标注起点、终点和上述六个时间点；
- 正面牵拉和侧向牵拉分别输出一张图，并额外输出一个并排总图。

输入：`ground_truth/trajectories_3d.npz`。

### 6.3 仿照 Fig. 8 的 future-gap 空间图

论文 Fig. 8 的因果含义是：在时间 `a` 取得当前仿真状态和当时的刚度参数，之后不读取未来观测，只输入相同的未来控制，向前仿真 `b` 步，在 `t=a+b` 与真实组织状态计算空间 gap。

我们采用如下对应关系：

- PBD → 纯 PBD；
- PBD-RM → PBD + RGB 视觉残差；
- PBD-RM-ON → PBD + RGB 视觉残差 + 在线刚度修正。

主图布局：

| 行 | 数据与时间 | 列 1 | 列 2 | 列 3 | 列 4 |
|---|---|---|---|---|---|
| 1 | 正面，中段 lift，`a=170, b=10` | `a+b` 真值 RGB/表面 | PBD gap | 视觉残差 gap | 视觉残差+刚度 gap |
| 2 | 正面，lateral pull，`a=220, b=10` | 同上 | 同上 | 同上 | 同上 |
| 3 | 侧面，中段 lift，`a=170, b=10` | 同上 | 同上 | 同上 | 同上 |
| 4 | 侧面，lateral pull，`a=220, b=10` | 同上 | 同上 | 同上 | 同上 |

这里的 10 步按记录帧定义，即 30 FPS 下约 `0.333 s`；每个记录帧内部仍执行 3 个 PBD 步。

gap 定义：

\[
d_i(a,b)=\left\|\hat{\mathbf{x}}_i^{(a\rightarrow a+b)}-\mathbf{x}^{GT}_{i,a+b}\right\|_2,
\]

单位为 mm。使用已有首帧一次性对应关系，在 240 个非控制评估点上计算，不进行 `a+b` 时刻的重新配准。逐点 gap 插值到显示表面，用蓝到红的统一色图展示。

公平性要求：

- 同一行三种方法使用完全相同的 `a、b`、未来 PSM/五点边界控制和 GT；
- 对第三组冻结时间 `a` 已经学到的局部刚度，不允许在 rollout 内继续看 RGB 或更新刚度；
- PBD-RM 也不能在 rollout 内读取未来 RGB；
- 每一行三种方法共享同一个 colorbar 范围；
- 图中同时标注 Mean、RMSE 和 P95 gap，原始逐点数值写入 CSV/JSON；
- 这些复制出来的 future rollout 只用于离线评估，绝不反馈到在线算法、参数更新或分支选择，因此不破坏连续在线运行的定义。

为直接对应现有 80/20 协议，再补充一张 horizon 图：固定最后观测帧 `a=239`，分别使用 `b=10、30、60`，展示直到第 299 帧的误差增长。

### 6.4 计划输出

统一输出目录：`outputs/two_free_support_ablation_v2/visualizations/`

- `selected_keypoints.json`
- `front_pull_gt_keypoints_2d.png/.pdf`
- `side_pull_gt_keypoints_2d.png/.pdf`
- `front_pull_gt_keypoints_3d.png/.pdf`
- `side_pull_gt_keypoints_3d.png/.pdf`
- `gt_keypoints_2d3d_overview.png/.pdf`
- `future_gap_fig8_b10.png/.pdf`
- `future_gap_open_loop_horizons.png/.pdf`
- `future_gap_values.csv`
- `future_gap_values.json`
- `README_可视化.md`

## 7. 实施前必须保持的约束

- 不修改原项目，只在 `_sim` 副本中增加可视化脚本和产物。
- 不重新选择初始刚度，不根据结果挑选分支。
- 不把深度、材料真值或轨迹真值传入在线优化。
- 关键点和 `a、b` 在看三组方法结果之前固定。
- 论文式 future rollout 是离线评价副本，不影响实际在线状态。
- 所有图同时保存位图 PNG 和论文可用的矢量 PDF。

## 8. 绑定面驱动的 Gaussian 尺度更新与重建诊断（2026-08-26）

本轮只在 `_sim` 副本中增加 Gaussian 几何更新，不加入视觉残差到粒子速度的写回：

- 每个 mode-2 Gaussian 保存静止视觉三角面基矩阵的逆；
- 运行时从当前三角面的两条边和单位法向构造表面形变，将静止 Gaussian 完整协方差
  传输到当前状态，再分解得到 quaternion 和三轴 scale；
- 视觉残差的可微渲染使用相同的三角面协方差链路，所以候选物理节点移动会同时影响
  Gaussian 中心和 footprint；RGB、opacity 仍固定；
- `apply_visual_tissue_residual()` 仍只写 `particle_q`，不写 `particle_qd`，因此没有新增
  人工速度或冲量。

针对正式 lift30mm 资产的 CPU 门禁结果：12% 拉伸并带剪切时，29,180 个 Gaussian 的
尺度变化均值/最大值约为 `0.0746/1.249 mm`；Warp 运行时和可微映射的最大 mean/covariance
绝对误差约为 `1.19e-8 m / 6.25e-10 m²`，所有尺度、旋转和协方差有限。可复现实验：

```bash
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/test_sim_gaussian_triangle_scale.py
```

新增前 80% 同化诊断脚本（旧诊断，不是正式 7:1 重建评估）：

```bash
/Media_HDD/jwshan/conda_envs/eg_codex/bin/python \
  scripts/visualize_sim_reconstruction_diagnostics.py \
  --evaluation-root outputs/sim_ablation_lift30mm_10points_v1
```

它只统计 frame `0..287` 的因果同化区间，输出逐帧 3D/2D、PSNR/SSIM/LPIPS 曲线、
四个选定时刻的组织 RGB 与绝对误差图，以及 JSON 均值诊断。后续完整评估脚本会在三组
指标汇总后自动生成该目录。

## 9. 正式六实验协议修正（2026-08-26）

前 80% 同化误差不再称为正式“重建结果”。正式结果拆为 `2×3=6` 次独立运行：

1. `reconstruction_7to1`：三种方法各运行完整 360 帧；frame
   `7,15,23,...,359` 共 45 帧作为均匀测试帧。这些帧禁止视觉残差、刚度提议和未完成
   候选验证，只允许 PBD 状态按已知任务控制前进并保存预测。其余 315 帧为因果训练帧。
2. `future_80to20`：三种方法各运行完整 360 帧；frame `0..287` 连续估计状态/材料，
   frame `288..359` 冻结视觉残差与刚度后开环预测，仅在这 72 帧计分。

两项能力都报告相同 10 个非夹持点的 3D/双目 2D 误差及组织层
PSNR/SSIM/LPIPS。运行入口为：

```bash
bash scripts/run_sim_two_capability_evaluation.sh \
  outputs/sim_two_capability_lift30mm_gaussian_scale_v1
```
