# SUPER grasp5 深度改进计划

本文说明当前项目中两个相互关联、但必须分开处理的问题：

1. 旧双目深度不准确，导致组织和地面平面的三维建模不准确。
2. PSM 器械在相机深度方向上存在系统偏差；修正深度后，需要重新进行视觉位姿矫正。

本文尽量从零解释整个处理过程。最初的约束是“现有整体坐标系不改动”；**2026-07-19 用户作出新决定：以全稠密深度确定的新 ground plane 作为新 table 坐标的 `z=0`，其他已经确定的相机、组织和器械结果统一迁移到新坐标。**

这不是重新独立标定每个对象。当前采用一个唯一的旧 table → 新 table 刚体变换，并把所有 table-frame 资产统一左乘该变换。需要保持不变的是物体之间的相对几何和图像投影，而不再要求 `X_table_camera` 的数值保持不变。

---

## 1. 先理解几个概念

### 1.1 什么是深度

图像像素只有横向位置 `u` 和纵向位置 `v`。深度 `z` 表示这个像素对应的物体离相机有多远。

本项目使用 rectified 左目 OpenCV 相机坐标系：

```text
x：图像向右
y：图像向下
z：从相机向前，也就是离相机越来越远
```

所以：

- `z` 增大：物体远离相机。
- `z` 减小：物体靠近相机。

一个像素有了深度以后，可以反投影成三维点：

```text
x = (u - cx) * z / fx
y = (v - cy) * z / fy
p_camera = [x, y, z]
```

### 1.2 什么是视差

同一个物体在左目和右目图像中的横向位置不同，这个横向差值叫视差 `disparity`。

本项目的深度换算公式是：

```text
z = fx * baseline / (disparity + cx_right - cx_left)
```

其中：

- `fx = 1742.788593 px`。
- `baseline = 0.005316136968 m`，约 `5.316 mm`。
- 当前 rectification 下 `cx_right - cx_left` 约为 0。

视差估计错几个像素，就可能造成毫米级深度误差。因此不能只看深度图颜色是否顺滑，还必须检查左右一致性和几何残差。

### 1.3 相机坐标和世界坐标

项目先在 rectified 左目相机坐标中恢复三维，再通过固定矩阵转换到 table/world 坐标：

```text
p_world = X_table_camera @ p_camera
```

项目统一约定：

```text
X_A_B 表示把 B 坐标中的点变换到 A 坐标
```

`X_table_camera` 同时用于相机、组织、地面、器械、GUI 和物理仿真。v7 迁移没有从头重估这些关系，而是在旧矩阵左侧统一乘 `X_new_table_old_table`，得到新的 `X_table_camera`。

### 1.4 当前的坐标迁移边界

本次统一改变的是：

- `table_frame.json` 中的 `X_table_camera` 和逆矩阵。
- `cameras.json` 中左右相机的 world 位姿。
- 当前 ground、tissue 和运行时 PSM 的 table/world 表达。

仍然禁止独立改变的是：

- `calib_rectified.json` 中的相机内参和双目标定。
- hand-eye、LND DH 参数和 `T_rectified_camera_psm_base`。
- LND 到 URDF link 的 canonical 固定映射。
- 已经确定的 paper CAD 到 GUI CAD 固定注册方式。
- URDF 的 wrist、共同夹爪铰点和对称开合关系。

所有 active PSM pose driver 继续保存 rectified camera-frame 位姿，不直接改 NPZ；运行时由新的 `X_table_camera` 统一转换，避免重复旋转。

---

## 2. 当前已经完成了什么

### 2.1 组织和地面链路

当前链路是：

```text
时间戳同步 FoundationStereo v4 全稠密深度
  -> bodies_v6_dense_camera，相机坐标中的组织和拟合地面
  -> bodies_v6_dense_frozen_table，在旧 table 中审计新平面
  -> X_new_table_old_table 统一坐标迁移
  -> bodies_v7_dense_ground_z0
  -> ground plane=[0,0,1,0] + ground Gaussian + 单刚体 tissue
```

当前 tissue 是一个刚体，包含 `4613` 个半径约 `1 mm` 的球形碰撞代理和 `2863` 个可视 Gaussian。旧 XPBD/PBD 资产保留为历史实验，但不在 runtime 中加载。

### 2.2 器械链路

当前器械链路是：

```text
q7 + LND + hand-eye
  -> 稳定运动学先验
  -> body / jaw_left / jaw_right 三部分图像 mask
  -> 1441 帧有界视觉矫正
  -> 固定 paper CAD / GUI CAD 注册
  -> 按真实 URDF 重建 wrist 和两片 jaw
  -> 重采样成 GUI 使用的 5458 状态 corrected driver
```

当前 GUI 推荐使用 `corrected + tip`。strict、paper、hybrid 等只保留为对照。

---

## 3. 问题一：旧深度导致组织和地面不准确

### 3.1 已确认的根因

提取 ROS bag 时，左目和右目分别独立编号：

```text
000000-left.png  是第 0 个左目消息
000000-right.png 是第 0 个右目消息
```

它们不一定是同一时刻。

但是旧 `generate_super_depth_v2.py` 直接把相同编号的左右图组成一对：

```text
000000-left.png + 000000-right.png
000001-left.png + 000001-right.png
```

例如 left frame 0 的时间为约 `0.02909 s`，最近的右目实际是 right frame 1，时间差约 `-7.66 ms`；同编号 right frame 0 的时间差约 `-27.24 ms`。运动器械、组织边界和高光区域会因此产生错误匹配。

旧深度还有以下问题：

- 没有把左右一致性作为有效深度门槛。
- `valid_fraction=1.0` 只代表数值被裁剪后是 finite，不代表匹配正确。
- bilateral filter 只能平滑噪声，不能修复错误匹配。
- ground 拟合点到平面的 P95 绝对残差约 `10.47 mm`，对于当前尺度明显过大。
- tissue 高度之后又被裁剪到 `3-15 mm`，会把错误深度固化进体积模型。

### 3.2 第一步：冻结旧资产

开始处理前，为以下文件建立 SHA256 清单：

```text
data/super/table_frame.json
data/super/grasp5_offline_demo/cameras.json
data/super/grasp5_native/calib_rectified.json
data/super/grasp5_offline_demo/instruments/psm1_lnd_model.json
data/super/psm_robot/psm_lnd_pose_driver_report.json
```

旧资产全部保留，不原地覆盖：

```text
depth_v2/
bodies_v4/
bodies_v5_table/
soft_tissue_v1/
```

新资产建议写到：

```text
depth_v3_timestamped/
bodies_v6_frozen_world/
soft_tissue_v2/
```

### 3.3 第二步：按时间戳生成新深度

对每一个左目帧执行：

1. 从 `stereo_left.json` 读取左目时间戳。
2. 从 `stereo_right.json` 找到时间上位于它前后的两个右目帧。
3. 分别运行左到右 RAFT-Stereo。
4. 根据时间位置，在右目 correspondence/disparity 上插值。
5. 再运行反向或 flipped-pair 匹配，执行左右一致性检查。
6. 只把通过一致性、范围和遮挡检查的像素标记为有效。

每帧至少保存：

```text
depth.npy
disparity.npy
valid_mask.png
lr_error.npy
depth_preview.png
metadata.json
```

metadata 必须记录：

- 左目帧号和时间戳。
- 前后右目帧号和时间戳。
- 插值系数。
- `fx`、baseline 和单位。
- RAFT checkpoint、迭代次数和分辨率。
- 左右一致性阈值和有效像素比例。

### 3.4 第三步：ground 和 tissue 分开处理

ground 是静态区域，可以利用多个帧提高可靠性：

1. 腐蚀 ground mask，避开组织、器械和图像边缘。
2. 排除器械膨胀 mask、高光和左右不一致像素。
3. 每帧独立拟合 inverse-depth 平面。
4. 比较多个帧的平面法向、offset 和点面残差。
5. 只融合稳定、一致的 ground 观测。

tissue 会运动和形变，因此第一版只使用目标首帧的同步深度：

1. 使用 tissue mask 和新 valid mask 的交集。
2. 排除器械遮挡和组织/地面边界附近的低置信像素。
3. 只填补小型局部空洞，不用全局中值填满大面积无效区域。
4. 检查组织表面投回左目后是否仍与 mask 重合。
5. 用右目重投影检查深度是否满足双目几何。

建议的第一阶段质量门槛：

- ground P95 点面绝对残差降到约 `2-3 mm` 内。
- 至少比当前 `10.47 mm` 改善三倍。
- 多帧 ground 法向和 offset 没有明显漂移。
- tissue 表面没有大块穿入 ground 或跳到器械表面。
- 无效深度必须保留为无效，不能靠平滑伪装成有效。

### 3.5 第四步：从 v6 冻结 world 迁移到 v7 ground-z0 world

v6 阶段先按旧方案保留了原 world，并得到一般平面：

```text
[-0.06261821, 0.19362987, 0.97907427, 0.00031684]
```

该平面相对旧 `+Z` 倾斜约 `11.7419 deg`。根据最新决定，构造唯一变换：

```text
X_new_table_old_table
```

它采用最小旋转把旧坐标中的新平面法向转到 `+Z`，不额外绕 Z 改 yaw；平移只沿新 Z 方向约 `+0.316844 mm`。然后统一执行：

```text
p_new = X_new_table_old_table @ p_old
X_new_table_camera = X_new_table_old_table @ X_old_table_camera
X_new_table_object = X_new_table_old_table @ X_old_table_object
```

相机 `X_WC` 同样左乘该变换。ground/tissue 和 corrected PSM 不允许各自拟合另一套变换。

PSM driver 不修改，因为其中存的是 `poses_rect_camera_xyz_xyzw`；新的 `X_table_camera` 会在加载时自动产生新 table-frame PSM 位姿。

旧坐标和相机清单已备份在 `data/super/coordinate_backups/pre_dense_v7_z0_20260719/`，因此迁移可审计、可回退。

### 3.6 第五步：重建 tissue、ground 和刚体 tissue

新 bodies 构建需要完成：

1. 从新 tissue depth 重建表面点。
2. 在新 ground plane 的局部二维坐标中建立 footprint。
3. 根据有效深度生成局部高度场。
4. 重新生成 tissue Gaussian，颜色仍取首帧真实 RGB。
5. ground Gaussian 直接放在确定的平面上，不复制噪声起伏。
6. 用小球紧密填充 tissue 体积，全部 sphere shape 绑定到同一个刚体。
7. tissue Gaussian 绑定到同一个刚体，不生成 tetra 或 PBD 粒子。

旧 `soft_tissue_v1` 的粒子、tetra 和 Gaussian binding 与旧表面绑定，不能直接套到新组织上；当前也不会加载它。

当前刚体必须通过：

- ground plane、ground Gaussian 和 tissue 使用同一新 table 坐标。
- tissue sphere 最低点不穿过 z=0。
- 1000 帧刚体状态 finite、速度收敛、漂移小于门槛。
- soft particle 和 soft Gaussian 数量都为 0。
- tissue Gaussian 只跟随单一刚体变换。

如果以后恢复软组织，再单独生成基于 v7 坐标的 `soft_tissue_v3`，重新执行 topology、solver、skinning、接触和视觉力门禁；不能复用旧 table 坐标中的 soft NPZ。

---

## 4. 问题二：保留 online_dvrk 主流程，只修正小的器械深度误差

### 4.1 当前正式基线是什么

正式基线不是单独的双目脚本，也不是把机械臂整体平移一个固定距离。当前效果较好的 `corrected` 来自：

```text
online_dvrk_tracking 的 Calibrate online videos / 论文跟踪
  -> LND 时间戳运动学作为稳定主干
  -> body / jaw_left / jaw_right 三部分 SAM2 观测
  -> 每帧有界的 9 参数图像矫正
  -> 时间平滑和人工关键帧 anchor
  -> 固定 paper-CAD 到 GUI-CAD 注册
  -> 按 URDF 重建 wrist 和共用铰点 jaw
  -> psm_part_corrected_pose_driver.npz
```

纯论文视频标定负责提供图像估计起点；最终较好的效果来自整条链，而不是直接播放论文输出。后续深度改进必须进入这条链，不能在最终 driver 外再叠一套独立位姿系统。

### 4.2 为什么 `-5.7106/-5.8471 mm` 结论不可靠

旧诊断把 SAM2 器械区域内的双目深度与少量 CAD 表面点做最近邻比较。金属高光、遮挡、mask 边界和稀疏 CAD 采样会把组织或背景深度错误配给器械。它能提示“可能有深度误差”，但不能证明整件器械应该向相机移动约 `5.8 mm`。

实际 GUI 验收已经证明该解释错误：被拒绝的 `stereo_3d v2` 明显过近，整体投影比原 `corrected` 更差。因此：

- 禁止再使用全局 `D0=-5.8471 mm`；
- 禁止在 GUI 中用 `Manual camera Z=-5.7 mm` 作为方向确认；
- 深度数值门禁不能替代左右目投影和人工视觉验收；
- `stereo_3d v2` 只保留作失败审计，不进入运行选项。

### 4.3 正确的改进边界

当前 rectified 左相机 `+z` 轴在 table/world 坐标中的单位方向为：

```text
R_table_camera @ [0,0,1] = [-0.064771, +0.698724, -0.712453]
```

因此 camera-z 每变化 `+1 mm`，同时包含约 `0.702 mm` 的 ground 平面内移动和 `-0.712 mm` 的 world-z 高度变化。它不是 world-z，也不能投影成 insertion joint。所有更新仍在相机坐标中估计，再由冻结的 `X_table_camera` 唯一转换；ground 的 `z=0` 定义不变。

以原 `corrected` 结果为 A/B 验收基准，但新候选按用户指定顺序生成：

```text
时间戳 LND 运动学 T_kin
  -> FoundationStereo/RAFT 小范围 camera-z 微调，得到 T_depth
  -> 以 T_depth 初始化 online_dvrk 视频视觉矫正
  -> 三部件观测、时序平滑和 URDF 重建
  -> 与原 corrected 对照的新 candidate
```

深度不是求一个固定平移值。每个可靠关键帧从运动学位姿开始，重复“渲染 CAD 深度 -> 比较双目 -> 求一个小增量 -> 更新后重新渲染”。单步 camera-z trust region 建议 `0.2-0.5 mm`，累计量不预先写死；每一步都必须同时通过深度、左右投影和 ground clearance 门禁。x/y、wrist 和 jaw 不参与深度搜索；只有多轮 z 已稳定而器械前后端仍呈相反深度残差时，才开放不超过 `1 deg` 的 pitch/yaw 小增量。

### 4.4 深度怎样加入原 online_dvrk 矫正器

不另写一套全序列 6DoF 跟踪器。深度阶段输出与 `online_dvrk_tracking` 相同定义的 `cTr` 初始化；视觉阶段继续使用其 SAM2、可微 CAD renderer、轮廓/尖端损失和 CMA-ES/Gradient 跟踪，再接现有 `track_super_psm_part_corrected.py` 三部件链路。

对每个通过人工视觉检查的关键帧：

1. 只在腐蚀后的 body 内部使用 Foundation/RAFT 都同意的像素；jaw 深度不主导位姿。
2. 使用真正的 CAD 前表面 z-buffer，而不是少量 Gaussian 最近邻点。
3. 深度阶段只优化 camera-z 小残差，范围 `+/-1 mm`，初值为 `0`。
4. 把深度结果作为视觉标定初值；不能原样使用 README 的 `--use_bo_initializer` 大范围重搜。
5. 视觉阶段 z 冻结，或最多只允许相对深度初值 `+/-0.3-0.5 mm`，防止单目尺度/深度歧义把结果拉走。
6. 保留原 body/jaw silhouette、tip、LND prior、encoder joint、对称 jaw 和时间平滑约束。
7. 若新投影相对原 `corrected` 变大、变小或离开 mask，即使深度数值更小也立即拒绝。
8. 低置信帧回退到运动学/相邻可靠视觉结果，不强行使用深度。

迭代形式为：

```text
T_depth_t^(0) = T_kin_t
T_depth_t^(k+1) = Exp(delta_xi_t^k) @ T_depth_t^k
```

其中第一阶段 `delta_xi=[0,0,0,0,0,delta_z]`。每次更新后必须重新渲染，不能把第一次测得的残差重复相加。停止条件建议同时满足：`|delta_z|<0.1 mm`、深度中值改善 `<0.05 mm`、左右目轮廓不再改善；通常限制为 `2-5` 轮。每个关键帧有自己的累计 `Delta_z(t)`，再做时间正则，不使用一个全序列常数。

深度迭代完成后才运行 online_dvrk 视觉矫正。视觉阶段把 `T_depth` 当初始化并对 z 加硬边界/强先验。视觉结束后再做一次只读深度复核；若视觉把深度拉坏，则拒绝该步或再执行一轮“深度小步 -> 视觉小步”，而不是直接接受。

### 4.5 必须先做的 A/B 门禁

第一批只检查原人工 anchor `141/142/160/320`，再增加 `0/480/800/1120/1280/1440`。每帧必须输出：

- 左目原 `corrected` / 深度微调候选；
- 右目原 `corrected` / 深度微调候选；
- 原始 SAM2 三部分 mask；
- Foundation 与 RAFT 深度一致区；
- camera-z 实际改变量。

接受条件按优先级排序：

1. 人工观察不比原 `corrected` 差；
2. 左右目轮廓和尖端误差不增加；
3. jaw 共用铰点、URDF 链和开合序列完全不变；
4. 深度残差才作为最后一项改善指标。

### 4.6 坐标和资产保护

以下内容全部冻结：v7 table/world、`X_table_camera`、hand-eye、双目标定、LND 参数、固定 CAD registration、URDF 和原 `psm_part_corrected_pose_driver.npz`。新结果必须写到独立候选目录，完成 GUI 全序列人工验收前不得加入正式运行选项。

---

## 5. 两个问题完成后的联合验收

### 5.1 坐标迁移一致性检查

迁移前旧文件必须备份；迁移后的新哈希作为新的权威值：

```text
新 table_frame.json: 6dddc2178cdf816f5dada5febdd528f80e42d52e631076e1f5f4a952297adecf
新 cameras.json: e1e7b7e7e21ca8a9409c88a29409d2e9ad6b783a85b277e71cec0c84340ce4ef
```

必须通过的数值检查：

- 新 ground plane 严格为 `[0,0,1,0]`。
- v7 点等于 v6 点统一左乘迁移矩阵，最大误差接近浮点零。
- 左右相机与 ground/tissue 同步迁移后，归一化图像投影不变。
- active PSM driver 的 SHA256 不变，说明 camera-frame 结果没有被重复变换。
- corrected PSM 的全部 38,206 个 link pose 等于旧 pose 统一左乘迁移矩阵。
- LND、CAD 注册、URDF link 间相对变换和共同夹爪铰点不变。

### 5.2 组织和地面检查

- 新 ground 点面残差明显低于旧结果。
- 新 ground 投影与真实图像中的地面一致。
- tissue 表面投影与首帧 mask 重合。
- tissue 没有穿入 ground，也没有大面积悬空。
- 当前刚体 tissue 是单一 body，soft particle/soft Gaussian 均为 0。
- 1000 帧静置无穿透、无漂移失控；视觉 Gaussian 始终绑定同一刚体。

### 5.3 器械检查

- 双目诊断中的 body 深度残差中值接近 0。
- 残差 MAD 不比当前诊断更差。
- 左目和右目投影同时改善，不能只优化左目截图。
- 当前 16 个接受锚点都已生成左右目旧/新投影和 Foundation 残差对比图；被质量门拒绝的帧不写入候选对比集。
- 两片夹爪共同铰点距离保持数值 0。
- 两片夹爪铰轴保持平行。
- shaft、wrist 和 jaw 开合平面保持 URDF 关系。
- 默认 GUI 手动 camera Z 恢复为 0，避免重复修正。

### 5.4 联合接触检查

v2 的器械 camera-z 公共初值已经由 Foundation 多帧更新为 `D0=-5.847134 mm`。它沿 rectified 左相机 z，不是 world z；逐关键帧 6DoF 还会继续修正，所以不能把这个数值当成最终固定平移。

纯坐标迁移不会改变器械和组织的相对位置或图像投影；v2 新增了独立器械深度修正，因此旧接触报告不能作为新结果的证明。

当前 tissue 已按用户决定改为刚体，PSM 刚体碰撞仍关闭。未来重新启用器械接触或 PBD 时，必须重新运行：

- 前 401 帧 PSM/tissue 接触预检。
- 完整 1441 帧回放。
- finite、tetra 体积、倒置数量、最大速度和接触候选统计。
- GUI 肉眼检查夹爪是否接触、穿透、滑过或错误抬起组织。

本轮 `stereo_3d` 100 步 GPU 冒烟已经验证驱动加载、时间驱动、刚体场景稳定和坐标哈希不变；由于 PSM 碰撞关闭，它不冒充接触验证。

---

## 6. 当前执行顺序与状态

当前已经执行：

```text
1. 时间戳同步 FoundationStereo v4 全稠密深度
2. 用第 0 帧 SAM2 mask 重建 v6 ground/ground plane/tissue
3. 在旧 table 中审计新平面，确认与旧 z=0 相差 11.7419 deg
4. 备份旧 table_frame.json 和 cameras.json
5. 生成唯一 X_new_table_old_table
6. 生成 bodies_v7_dense_ground_z0
7. 同步更新 table_frame.json、左右相机 X_WC 和 runtime 资产路径
8. 保持所有 camera-frame PSM driver 原文件不变
9. 通过点、pose 和像素投影等变性检查
10. 通过 corrected PSM 的 1000 帧刚体场景门禁
11. 运行过独立 `D0 + 6DoF` 双目实验，但 GUI 显示器械明显过近，结论已拒绝
12. 保留失败文件用于审计，从 GUI 的 `stereo_3d` 选项撤下
13. 恢复 `online_dvrk -> LND -> 三部件 -> corrected -> URDF` 为唯一器械主流程
```

### 6.1 当前可用结果

当前器械继续使用原来效果较好的 `corrected`：

```text
data/super/psm_tracking/psm_part_corrected_pose_driver.npz
SHA256=21df09849b08d6cef1ae47c694e7400b6df5228e0805c5c35ecf3d68e2ef648a
```

它仍保持稳定闭合区间 frame `548..1246`、共用 jaw 铰点和完整 URDF 远端链。被拒绝的 `psm_stereo_3d_corrected_v2/` 不用于运行。

### 6.2 下一次深度改进的原则

不再移动整件器械约 `-5.8 mm`。从原 `corrected` 的 camera-z 残差 `0` 开始，只在人工验收关键帧开放 `+/-1 mm`，原 online_dvrk 二维投影必须保持或改善；未通过左右目对比的深度结果一律拒绝。

当前 v7 table/world、hand-eye、双目标定、LND、CAD registration 和 URDF 均未修改。table frame 与 cameras 权威哈希仍分别为 `6dddc217...adecf` 和 `e1e7b7e...ce4ef`。

最终原则可以记成一句话：

```text
用新 ground 定义新的 z=0，但所有对象只使用同一个坐标迁移；
改变坐标数值，不改变相对几何、图像投影、hand-eye、LND、CAD 注册和 URDF 机械关系。
```

---

## 7. 已完成的第一版：多轮深度后再做视觉矫正

这一版已经按上面的原则实现，但目前只做关键帧，目的是先让人看图确认，不直接替换正式器械轨迹。

### 7.1 小白版流程

可以把器械定位理解成三次检查：

1. **先相信机械臂编码器。** 用每张图自己的时间戳找到同时刻 LND 运动学姿态，这是起点，不从图片中重新猜整台机械臂。
2. **再小步调远近。** FoundationStereo 告诉我们器械表面大概离左相机多远。程序一次只沿左相机 z 方向移动最多 `0.3 mm`，重新画一次真实 CAD 深度图，再判断要不要继续；最多 4 次、总共不超过 `1.2 mm`。RAFT-Stereo 如果不支持这个变化，或器械轮廓变差，立即停止。
3. **最后用图片修正横向和角度。** 把第二步的结果交给 online_dvrk_tracking 的可微 CAD 渲染器，对齐 SAM2 的 body、左 jaw、右 jaw。横向 x/y 可以各调整 `3 mm`，因为旧方法确实需要它来对齐轮廓；z 只能再调整 `0.4 mm`，所以视觉优化不能抹掉前面的深度结果。

相机 z 轴与 world z=0 平面不平行不会破坏这个流程。深度修正始终在 rectified 左相机坐标中完成，最后仍使用冻结的 `X_table_camera` 统一转换；没有把 camera-z 当作 world-z，也没有重拟合 table/world。

### 7.2 为什么这次不会再直接拉近 5.8 mm

- 不存在全序列固定平移值。
- 每一帧从自己的 LND 姿态开始，自己决定接受 0、1、2、3 或 4 个小步。
- 每一步都重新计算真实 CAD 前表面 z-buffer。
- 深度改善但 body 轮廓明显变差时也拒绝。
- 视觉阶段 camera-z 只开放 `+/-0.4 mm`，但 x/y 仍能完成原 online_dvrk 方法擅长的二维对齐。
- 最后与原 corrected 比较；不通过就使用原 corrected，不把较差状态写入接受结果。

### 7.3 当前结果怎么看

9 个代表帧中，最终接受新结果的是 `141,160,480,1120,1280`。另外 4 帧保留原 corrected，原因不是程序失败，而是证据不够或图像门禁未通过：

- frame 0/800 的 mask 是 CAD 周期锚，不是真实图像分割；
- frame 320 的 raw 新结果深度更好，但 body 轮廓检查未通过；
- frame 1360 的右 jaw 置信度约 `0.10`，不能拿它证明夹爪对齐。

接受的 5 帧中，body Dice 中位数从 `0.757` 提高到 `0.791`，夹爪尖端平均误差从 `1.59 px` 降到 `1.03 px`；Foundation/RAFT 深度绝对中值分别从 `3.30/3.29 mm` 降到 `2.45/2.42 mm`。

对比图在：

```text
data/super/psm_tracking/psm_depth_then_visual_v1/contact_sheet.png
```

每一帧的原分辨率图也在同一目录。上排从左到右是观测、LND 起点、深度调整后、视觉 raw、新旧 A/B 和最终接受状态；下排是深度残差与右目投影。

### 7.4 夹爪和坐标保护结果

最终状态没有分别平移左右 jaw。程序把视觉结果转换到 GUI 后，重新按真实 URDF 父子链生成 wrist 和两个 jaw，开合始终是同一铰点上的 `+jaw/2` 与 `-jaw/2`。本轮最大共用铰点 gap 为 `0 m`，铰轴误差约 `3.31e-6 deg`。

以下三个文件在运行前有硬哈希检查，任何一个变化程序都会退出：

```text
table_frame.json: 6dddc217...adecf
cameras.json: e1e7b7e7...ce4ef
active corrected driver: 21df0984...648a
```

所以这次没有改 table/world、相机标定、hand-eye、LND、CAD registration、URDF 或正式 corrected driver。

### 7.5 运行命令和仍未做的事

```bash
cd /Media_HDD/jwshan/wad/Embodied_gaussians_fixed_super_best

/Media_HDD/jwshan/conda_envs/online_dvrk/bin/python \
  scripts/refine_super_psm_depth_then_visual_keyframes.py \
  --output-dir data/super/psm_tracking/psm_depth_then_visual_v1
```

当前版本只用来查看关键帧，**没有**生成全序列 driver，也**没有**加入 GUI。正式回放仍使用：

```bash
bash scripts/run_demo_browser_12.sh \
  --psm-pose-driver corrected \
  --psm-visual-mode tip \
  --psm-roll-offset-deg -27
```

关键帧人工确认后，下一步才是把接受的修正按真实时间戳做连续、不过冲的传播，然后检查 1441 帧投影、速度/加速度、闭合区间和 GUI 动画；这些检查完成前不能替换正式 driver。
